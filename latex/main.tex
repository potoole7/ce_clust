% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\PassOptionsToPackage{dvipsnames,svgnames,x11names}{xcolor}
%
\documentclass[
  12pt]{article}

% preamble before setting font theme
\usepackage{mathtools}

\usepackage{amsmath,amssymb}
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math}
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
\usepackage{lmodern}
\ifPDFTeX\else  
    % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{xcolor}
\setlength{\emergencystretch}{3em} % prevent overfull lines
\setcounter{secnumdepth}{5}
% Make \paragraph and \subparagraph free-standing
\makeatletter
\ifx\paragraph\undefined\else
  \let\oldparagraph\paragraph
  \renewcommand{\paragraph}{
    \@ifstar
      \xxxParagraphStar
      \xxxParagraphNoStar
  }
  \newcommand{\xxxParagraphStar}[1]{\oldparagraph*{#1}\mbox{}}
  \newcommand{\xxxParagraphNoStar}[1]{\oldparagraph{#1}\mbox{}}
\fi
\ifx\subparagraph\undefined\else
  \let\oldsubparagraph\subparagraph
  \renewcommand{\subparagraph}{
    \@ifstar
      \xxxSubParagraphStar
      \xxxSubParagraphNoStar
  }
  \newcommand{\xxxSubParagraphStar}[1]{\oldsubparagraph*{#1}\mbox{}}
  \newcommand{\xxxSubParagraphNoStar}[1]{\oldsubparagraph{#1}\mbox{}}
\fi
\makeatother


\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}\usepackage{longtable,booktabs,array}
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
% Scale images if necessary, so that they will not overflow the page
% margins by default, and it is still possible to overwrite the defaults
% using explicit options in \includegraphics[width, height, ...]{}
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
% Set default figure placement to htbp
\makeatletter
\def\fps@figure{htbp}
\makeatother

\addtolength{\oddsidemargin}{-.5in}%
\addtolength{\evensidemargin}{-.1in}%
\addtolength{\textwidth}{1in}%
\addtolength{\textheight}{1.7in}%
\addtolength{\topmargin}{-1in}
\makeatletter
\@ifpackageloaded{caption}{}{\usepackage{caption}}
\AtBeginDocument{%
\ifdefined\contentsname
  \renewcommand*\contentsname{Table of contents}
\else
  \newcommand\contentsname{Table of contents}
\fi
\ifdefined\listfigurename
  \renewcommand*\listfigurename{List of Figures}
\else
  \newcommand\listfigurename{List of Figures}
\fi
\ifdefined\listtablename
  \renewcommand*\listtablename{List of Tables}
\else
  \newcommand\listtablename{List of Tables}
\fi
\ifdefined\figurename
  \renewcommand*\figurename{Figure}
\else
  \newcommand\figurename{Figure}
\fi
\ifdefined\tablename
  \renewcommand*\tablename{Table}
\else
  \newcommand\tablename{Table}
\fi
}
\@ifpackageloaded{float}{}{\usepackage{float}}
\floatstyle{ruled}
\@ifundefined{c@chapter}{\newfloat{codelisting}{h}{lop}}{\newfloat{codelisting}{h}{lop}[chapter]}
\floatname{codelisting}{Listing}
\newcommand*\listoflistings{\listof{codelisting}{List of Listings}}
\makeatother
\makeatletter
\makeatother
\makeatletter
\@ifpackageloaded{caption}{}{\usepackage{caption}}
\@ifpackageloaded{subcaption}{}{\usepackage{subcaption}}
\makeatother


\ifLuaTeX
  \usepackage{selnolig}  % disable illegal ligatures
\fi
\usepackage[]{natbib}
\bibliographystyle{agsm}
\usepackage{bookmark}

\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same} % disable monospaced font for URLs
\hypersetup{
  pdftitle={Title},
  pdfauthor={Author 1; Author 2},
  pdfkeywords={3 to 6 keywords, that do not appear in the title},
  colorlinks=true,
  linkcolor={blue},
  filecolor={Maroon},
  citecolor={Blue},
  urlcolor={Blue},
  pdfcreator={LaTeX via pandoc}}

\newcommand{\anon}{1}

%set the key \texttt{anon} to ``0'' to hide the authors and acknowledgements,
%  producing the required anonymized version. 
%Set the key \texttt{anon} to ``1'' to produce the manuscript with author details and
% acknowledgments. 

%%%% Non-template packages %%%%
\usepackage{tikz}
\usepackage[colorinlistoftodos]{todonotes}
% \usepackage{bm} % don't use with unicode-math
\usepackage{amsfonts}
% \usepackage{mathtools}
\usepackage{natbib} % print author's name and year when citing
\usepackage{bbm}
\usepackage{todonotes}
\usepackage{pdflscape}
\usepackage{caption}
\usepackage{subcaption}
% \usepackage[T1]{fontenc}
\usepackage{authblk}
\usepackage{pdfpages}
\usepackage{setspace} 
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{float}
\usepackage{tikz}
% \usepackage[colorlinks=true,citecolor=blue, linkcolor=blue]{hyperref}
\usepackage{multirow}
\usepackage{algorithm}
\usepackage{algpseudocode}

% Define a custom note command for general notes
\newcommand{\mynote}[1]{\todo[color=yellow!40,inline]{#1}}

% Define custom maths equations 
\DeclareMathOperator{\Tr}{Tr}
\DeclareMathOperator{\KL}{\mathrm{KL}}
\DeclareMathOperator{\JS}{\mathrm{JS}}
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclarePairedDelimiter\abs{\lvert}{\rvert}%
\DeclareMathOperator{\EX}{\mathbb{E}}% expected value
% \newcommand{\JSGa}[1]{\mathrm{JS}^{\mathrm{G}_{#1}}}
% \newcommand{\JSGa}[1]{\mathrm{JS}^{\mathrm{G}_{\alpha}}}
% \DeclareMathOperator{\JSGa}{\mathrm{JS}^{\mathrm{G}_{\alpha}}}
% \newcommand{\JSGa}{\mathop{\mathrm{JS}^{\mathrm{G}_{\alpha}}}\nolimits}
% \newcommand{\JSGa}{\mathop{\mathrm{JS}^{\mathrm{G}_{k}}}\nolimits}
\newcommand{\GJS}{\mathop{{\mathrm{GJS}}\nolimits}
\newcommand{\JSGa}{\mathop{\mathrm{JS}^{\mathrm{G}_{\lambda}}}\nolimits}
% \newcommand{\cJSGa}{\mathop{\mathrm{cJS}^{\mathrm{G}_{\lambda}}}\nolimits}
% \newcommand{\cJSGa}{\mathop{\mathrm{cJS}_{i}^{\mathrm{G}_{\lambda}}}\nolimits}
\newcommand{\cJSGa}[1]{\mathop{\mathrm{cJS}_{i, #1}^{\mathrm{G}_{\lambda}}}\nolimits}
% \newcommand{\eJSGa}{\mathop{\mathrm{eJS}^{\mathrm{G}_{\lambda}}}\nolimits}
\newcommand{\eJSGa}[1]{\mathop{\mathrm{eJS}_{i, #1}^{\mathrm{G}_{\lambda}}}\nolimits}
% \newcommand{\GJS}[1]{\mathrm{JS}^{\mathrm{G}}}
\DeclareMathOperator{\GJS}{\mathrm{JS}^{\mathrm{G}}}
\DeclareMathOperator{\CE}{\mathrm{CE}}
\newcommand{\rhou}[1]{\rho_{\text{#1}}}
\newcommand\numberthis{\addtocounter{equation}{1}\tag{\theequation}}


% \newcommand{\mvmean}[1]{\symbf{\alpha}_{\mid i}(#1) y + y^{\symbf{\beta}_{\mid i}(s)} \symbf{\mu}_{\mid i}(#1)}
\newcommand{\mvmean}[1]{\symbf{\alpha}_{\mid i, #1} y + y^{\symbf{\beta}_{\mid i, #1}} \symbf{\mu}_{\mid i, #1}}
% \newcommand{\mvcov}[1]{\left(y^{\beta_{\mid i}(#1)}\right)^{\mathrm{T}} \Sigma_{\mid i}(#1) y^{\beta_{\mid i}(#1)}}
\newcommand{\mvcov}[1]{\left(y^{\beta_{\mid i, #1}}\right)^{\mathrm{T}} \Sigma_{\mid i, #1} y^{\beta_{\mid i, #1}}}

% \symbf{\alpha}_{\mid i}(s) y + y^{\symbf{\beta}_{\mid i}(s)} \symbf{\mu}_{\mid i}(s)

\newcommand{\rG}[1]{\rho_{\mathrm{Gauss}}_{#1}}
\newcommand{\rT}[1]{\rho_{t}_{#1}}
\newcommand{\RG}[1]{\Sigma_{\mathrm{Gauss}}^{#1}}   % JxJ correlation matrix
\newcommand{\RT}[1]{\Sigma_{t}^{#1}}                % JxJ correlation matrix
\newcommand{\LapTrans}{T_{\mathrm{Laplace}}}

% fix setminus for unicode-math
\setmathfont[range=\setminus]{Asana Math}


% Adjust spacing for sections
\usepackage{titlesec}

% \titlespacing*{\section}{0pt}{1.5ex plus 1ex minus .2ex}{1ex plus .2ex}
% \titlespacing*{\subsection}{0pt}{1ex plus .5ex minus .2ex}{0.5ex plus .2ex}
% \titlespacing*{\subsubsection}{0pt}{0.5ex plus .2ex minus .1ex}{0.3ex plus .1ex}

\begin{document}


\def\spacingset#1{\renewcommand{\baselinestretch}%
{#1}\small\normalsize} \spacingset{1}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\if1\anon
{
  % \title{\bf Title}
  \title{\bf Clustering of multivariate tail dependence using conditional methods}
  \author{Patrick O'Toole\thanks{
    % The authors gratefully acknowledge \textit{please remember to list all relevant funding sources in the version that gives all author information}}\hspace{.2cm}\\
  Patrick O'Toole is supported by a scholarship from the EPSRC Centre for Doctoral Training in Statistical Applied Mathematics at Bath (SAMBa), under the project EP/S022945/1.}\hspace{.2cm}\\
    % Patrick O'Toole is supported by a scholarship from the EPSRC Centre for Doctoral Training in Statistical Applied Mathematics at Bath (SAMBa), under the project EP/S022945/1.}\\
    Department of Mathematical Sciences, University of Bath \\
    and \\
    Jordan Richards \\
    School of Mathematics, University of Edinburgh \\
    Christian Rohrbeck \\
    Department of Mathematical Sciences, University of Bath \\
  }
  \maketitle
} \fi

\if0\anon
{
  \bigskip
  \bigskip
  \bigskip
  \begin{center}
    {\LARGE\bf Title}
\end{center}
  \medskip
} \fi

\bigskip
\todo{Rewrite abstract with Christian}
\begin{abstract}
Conditional extremes (CE) models have proven useful for analysing the joint tail behaviour of random vectors.
  However, when applied across many locations or variables, it can be difficult to interpret or compare the resulting extremal dependence structures, particularly in high dimensions. 
  % \todo{C: add 1-2 sentences before "We propose .." to clearly outline the scenario we are considering, and why it is useful}
  To address this, we propose a novel clustering framework for CE models, which can be used to identify groups with similar multivariate extremal dependence structures.
  Our approach introduces a closed-form, computationally efficient dissimilarity measure for comparing CE models, based on a generalized version of the Jensen-Shannon divergence, applicable in any dimension.
  Applying standard clustering algorithms to the resulting pairwise distances, we obtain interpretable groups with homogeneous tail dependence characteristics.
  Simulation studies demonstrate that our method outperforms existing approaches for bivariate cases and uniquely extends to multivariate settings. 
  Applied to Irish meteorological data, our clustering successfully identifies spatially coherent regions with similar tail dependence between precipitation and wind speed. 
\end{abstract}

\noindent%
% {\it Keywords:} Extremes, multivariate extremes, spatial clustering
{\it Keywords:} Conditional extremes; Clustering; Extreme value analysis; Jensen-Shannon divergence; Multivariate extremes.
\vfill

\newpage
\spacingset{1.8} % DON'T change the spacing!

% \todo{Check equation numbering, especially for 10?}
% \todo{Are all equation numbers for align environments correct?}

\section{Introduction}
\label{sec:intro}
% Introduction to EVT
% Extreme Value Theory (EVT) provides an asymptotically justified framework for projecting beyond the magnitude of observed data into the extreme tails of distributions \citep{Coles2001}.
Extreme Value Theory (EVT) provides asymptotically justified models for estimating the probability of events outside the range of observed values \citep{Coles2001}.
% EVT implicitly assumes that there is an underlying stochastic process generating the data, which is sufficiently smooth to allow extrapolation to unseen levels \citep{Coles2001}.
% EVT is useful anywhere we are interested more in the ``extreme'' tails of the distributions of a phenomenon, rather than the mean or ``bulk'' behaviour.
% This situation can arise in a variety of contexts, and is particularly popular in environmental applications.
% EVT modelling can be used to assess the risk of extreme events, such as floods, droughts, heatwaves and storms, which can have significant impacts on society and the environment.
% Environmental applications are one area where EVT methods are used frequently, because interest lies in estimating the severity of future extreme events. % , such as heatwaves or storms. 
% Environmental applications are one area where EVT methods are used frequently, because interest lies in estimating the severity of future extreme events, to develop risk management strategies and to assess the impacts of climate change. \todo{Keep reference to climate change?}
Environmental applications are one area where EVT methods are used to estimate the severity of future extreme events in order to aid the development of risk management strategies. % and to assess the impact of climate change. 
% For instance, heatwaves can lead to droughts and wild fires \citep{tanarhte2015heat, french2019quantifying, koh2023gradient}.
% % Extreme rainfall contributes to storms and flooding, which can cause major damage and trigger large insurance claims \citep{Rohrbeck_flooding, osei2021estimation}.
% Extreme rainfall and high wind speeds can cause major damage and large insurance claims, and thus it is important to understand their likelihood \citep{Rohrbeck_flooding, osei2021estimation, soukissian2015effect, steinkohl2013extreme}. 
% % High wind speeds can lead to significantly damaging events such as tropical cyclones, and have also seen much focus in the field of EVT \citep{soukissian2015effect, steinkohl2013extreme}.
Examples include the analysis of heatwaves \citep{tanarhte2015heat, french2019quantifying}, extreme rainfall \citep{osei2021estimation}, and wind speeds \citep{steinkohl2013extreme, soukissian2015effect}, as well as the modelling of wildfire risks \citep{Koh2024} and the impact of environmental extremes on insurance claims \citep{Rohrbeck_flooding, Miralles2024}. 

% Dependence modelling 
% \todo{C: Shorten this paragraph}
% \todo{C: de-emphasise environmental examples as focus is methods}
% \todo{Remove reference to climate change (no temporal element)}
% EVT can naturally be extended to a multivariate setting, where we are instead interested in the \emph{joint} tail behaviour of a random vector. 
% Often, the simultaneous or ``concomitant'' occurrence of extreme events in environmental settings leads to significantly more catastrophic events \citep{zscheischler2018future, bevacqua2021guidelines}.
% These rarer and more devastating occurrences can take the form of extreme events occurring at multiple locations, or for multiple variables concurrently exhibiting extremal behaviour at a single site.
% For example, in the UK in 2015 and 2016, widespread flooding as a result of Storms Desmond, Eva and Frank resulted in damages estimated at £1.3–1.9 billion \citep{FloodsWinter, Rohrbeck2021}.
% The co-occurrence of extreme wind speeds and wave heights have led to devastating tropical cyclones \citep{Sando2022}.
% Climate change is exacerbating the effects of compound environmental hazards, and so these phenomena are becoming increasingly important to understand \citep{AghaKouchak2020-ji}.
% To explore multivariate extremes, in addition to an estimate for the marginal extremal distribution for each variable, we also require a model for the tail \emph{dependence} between them.
% \todo{Want to introduce interest in spatial dependence modelling early}
% Extreme Value Theory can naturally be extended to the multivariate setting, where the \emph{joint} tail behaviour of a random vector is of interest. 
% Multivariate extremes naturally arise in spatial applications, where each component of the random vector may represent a different variable at a single location and/or the same variable observed across multiple locations.
% In environmental settings, extreme events rarely occur in isolation, and the compounded risk of simultaneous or ``concomitant'' occurrence of extremes can often lead to significantly more catastrophic events \citep{zscheischler2018future, bevacqua2021guidelines, Rohrbeck2021, Sando2022}. 
% \todo{Do I want to mention marginal models at all here? Probably want to de-emphasise anyway}
% % To analyse such multivariate extremes, one first fits marginal extremal distributions for each component and then specifies a model for their tail \emph{dependence}.
% % The compounded risk associated with concomitant extremal events means that when analysing such multivariate extremes, an important consideration is \citep{Zhang2024}
% These risks can be underestimated unless care is given in modelling the tail \emph{dependence} as part of any multivariate extremes analysis \cite{Zhang2024}.

% % Dependence modelling (revised)
% % EVT can naturally be extended to the multivariate setting, where the \emph{joint} tail behaviour of a random vector is of interest.  
% % EVT extends naturally to the multivariate setting, where the \emph{joint} tail behaviour of a random vector is of interest. 
% In the multivariate setting, sampling the \emph{joint} tail behaviour of a random vector is of interest. 
% % % Multivariate extremes often arise in spatial applications, where each component may correspond to different variables at a single location, the same variable across multiple locations, or both.  
% % Herein, each location is associated with a random vector, and the aim is to understand how the joint tail behaviour of the location-wise random vectors varies across space.
% % In such settings, each location is associated with a random vector. 
% Multivariate extremes frequently arise in spatial applications, where one or more variables are observed at a number of fixed locations, such that each location is associated with a random vector. 
% % % While our methodology applies broadly across these settings, we focus on the latter case where each location is associated with a multivariate observation, and the aim is to understand how joint tail behaviour varies across space.
% % \todo{Move this sentence, doesn't feel fully connected to the last sentence}
% % In environmental settings, extremes rarely occur in isolation, and the compounded impact of simultaneous or ``concomitant'' events can lead to significantly more severe outcomes \citep{zscheischler2018future, bevacqua2021guidelines, Rohrbeck2021, Sando2022}.  
% % These joint risks can be underestimated unless tail \emph{dependence} is explicitly modelled as part of any multivariate extremes analysis \citep{Zhang2024}.
% In environmental applications, extreme events rarely occur in isolation, and the compounded impact of simultaneous or ``concomitant'' extremes can produce substantially more severe outcomes \citep{zscheischler2018future, bevacqua2021guidelines, Rohrbeck2021, Sando2022}.
% Consequently, these joint risks are liable to be underestimated unless tail \emph{dependence} is explicitly modelled \citep{Zhang2024}. 
% Motivated by this, we develop a framework to characterise how the joint tail behaviour of the location-wise random vectors varies across space.

% Dependence modelling (+ AD/AI)
% In the multivariate setting, sampling the \emph{joint} tail behaviour of a random vector is of interest. 
% EVT extends naturally to the multivariate setting, where one is interested in the \emph{joint} tail behaviour of random vectors observed at fixed locations. 
In the multivariate EVT setting, one is interested in the \emph{joint} tail behaviour of random vectors.
% Multivariate extremes frequently arise in spatial applications, where one or more variables are observed at a number of fixed locations, such that each location is associated with a random vector. 
Multivariate extremes frequently arise in spatial applications, where one or more variables are recorded at each site. 
% In environmental applications, extreme events rarely occur in isolation, and the compounded impact of simultaneous or ``concomitant'' extremes can produce substantially more severe outcomes \citep{zscheischler2018future, bevacqua2021guidelines, Rohrbeck2021, Sando2022}.
% ``Concomitant'' extremes, where multiple variables or locations simultaneously exhibit extreme behaviour, can produce substantially more severe outcomes than single-variable extremes \citep{zscheischler2018future, bevacqua2021guidelines, Rohrbeck2021, Sando2022}.
Within such settings, simultaneous or ``concomitant'' extremes, where multiple variables or locations exhibit extreme behaviour together, can produce substantially more severe outcomes than single-variable extremes \citep{zscheischler2018future, bevacqua2021guidelines, Rohrbeck2021, Sando2022}.
% Consequently, these joint risks are liable to be underestimated unless tail \emph{dependence} is explicitly modelled \citep{Zhang2024}. 
Consequently, joint risks may be underestimated unless tail \emph{dependence} is explicitly modelled \citep{Zhang2024}. 
% Tail dependence can be described using the coefficient of asymptotic dependence \(\chi\in[0,1]\) (see e.g.\ \citet{Coles1999}), which for two random variables \(X_1,X_2\) with corresponding marginal distribution functions \(F_1,F_2\) is defined as 
Tail dependence is commonly summarised by the coefficient of asymptotic dependence \(\chi\in[0,1]\) (see e.g.\ \citet{Coles1999}), defined for two random variables $X_1$ and $X_2$ with marginal distribution functions $F_1$ and $F_2$ respectively as
\[
  % \chi \;=\; \lim_{u\to 1}\Pr\big(F_1(X_1)>u \mid F_2(X_2)>u\big).
  % \chi \;=\; \lim_{u\to 1}\Pr\left[F_1(X_1)>u \mid F_2(X_2)>u\right].
  \chi \;=\; \lim_{u\to 1}\Pr\left\{F_1(X_1)>u \mid F_2(X_2)>u\right\}.
\]
% The values \(\chi>0\) indicate asymptotic dependence (AD) and \(\chi=0\) asymptotic independence (AI) for the random vector $\left(X_1, X_2\right)$. 
When $\chi$ > 0, the variables $X_1$ and $X_2$ are termed asymptotically dependent (AD), while $\chi=0$ corresponds to asymptotic independence (AI).
Although \(\chi\) is a useful pairwise summary, it does not fully characterise multivariate tail structure or support inference on joint exceedance probabilities, for which a full probabilistic model is required. 
Many classical models and clustering frameworks assume AD \citep{Vignotto2021,Boulin2025}, but a growing literature argues that environmental data often exhibit AI and that AD models can be inappropriate in such contexts \citep{Opitz2016,Dawkins2018,Huser2024}. 

% Introduction to motivating example
% % Link to previous paragraph
% As a practical motivating example of multivariate observations indexed in space, we consider bivariate precipitation and wind-speed data from a network of 59 meteorological stations in the Republic of Ireland, recorded over the period 1990-2020 \citep{Hersbach2020, metHistoricalData}.
% % Why setting is important:
% Particular interest lies in how the joint tail behaviour of precipitation and wind speed varies spatially, and in identifying regions or groups of sites with similar tail characteristics.
% Capturing these patterns is crucial for assessing the compounding risks of localised flood and wind damage, for example during Atlantic storms. 
% This can inform infrastructure planning, emergency services allocation and insurance pricing \citep{Martius2016}. % and emergency services
% \citet{Vignotto2021} report notable east-west differences in tail dependence between precipitation and wind in Ireland.
% However, theirs and other approaches (e.g.\ \citet{Boulin2025}) may be too restrictive in assuming AD, as both precipitation and wind speed are known to exhibit AD and AI characteristics \citep{Dawkins2018, Huser2024}.
% Hence, there is need for a more flexible modelling approach to reveal additional insights.
We consider a setting with $D$ random vectors of the same dimension, but with potentially different joint tail behaviours. 
For instance, we may observe a set of environmental variables (e.g.\ rainfall and wind speed) at a fixed number of spatial sites; see Section~\ref{sec:application}.
Interest then lies in understanding the variation in tail behaviour across the $D$ vectors, and, where feasible, to pool information to reduce model uncertainty. 
However, we often have limited knowledge on which random vectors exhibit similar tail behaviour.
In this paper, we present a new method for identifying groups of random vectors with similar tail behaviour based on the conditional extremes (CE) approach by \citet{Heffernan2004}, which is a popular model for multivariate extremes.

% % Conditional extremes
% % \item One such model is the CE model, which combines a univariate Generalised Pareto Distribution (GPD) and a semiparametric regression dependence model \cite{Heffernan2004}. \todo{Don't emphasise GPD, not used below}
% % One such dependence model is the Conditional Extremes model of \citep{Heffernan2004}.
% % Herein, we employ the conditional extremes model by \citep{Heffernan2004} to analyse tail dependence. 
% % To model tail dependence, we employ the conditional extremes (CE) framework of \citet{Heffernan2004}.
% % Conditional extremes modelling \citep{Heffernan2004} provides a flexible framework that accommodates both AD and AI regimes, and therefore offers a natural basis for comparing tail behaviour across sites. 
% The conditional extremes model of \citet{Heffernan2004} provides a flexible framework that accommodates both AD and AI regimes, and therefore offers a natural basis for comparing tail behaviour across sites. 
% % It is highly flexible in the type of extremal dependence it can model, and is considered to be conceptually simple relative to other models.
% % It reduces a difficult multivariate estimation problem to a univariate problem, by individually modelling each element of the random vector, which can then be combined to form a joint model, and so is computationally efficient.
% % Its conceptual simplicity, flexibility in capturing a wide range of tail dependence structures, and computational efficiency, reducing a multivariate problem to a series of univariate ones, have made it a popular choice.
% This flexibility, along with its conceptual simplicity and computational efficiency have made it a popular choice for modelling multivariate extremes.
% % The CE model has seen widespread use for analysing spatial extremes in environmental applications \citep{Keef2012_flooding, Winter2016, Tawn2018, Towe2019}.
% The CE model has seen widespread use for analysing spatial extremes in environmental applications \citet{Keef2012_flooding, Winter2016, Tawn2018, Towe2019}.
% % The CE model describes the tail behaviour of a random vector \emph{conditional} on a separate random variable exceeding a suitably high threshold.
% The CE model describes the tail behaviour of a random vector \emph{conditional} on one of its components exceeding a suitably high threshold.
% It achieves this via a semiparametric heteroscedastic regression model conditioned on threshold exceedances, which can be used to ascertain the joint tail dependence structure of the random vector.
% % The CE model provides many advantages over other competing methods. 
% % It involves a robust Gaussian working assumption which facilitates efficient inference, and allows for a closed-form expression for the KL divergence, between two sites, allowing for a rather simple clustering framework to be derived, as we will see in Section~\ref{subsec:div} \citep{Kullback1951}.
% % However, as with many models for extremes, which suffer from the natural
% % rarity of extreme data, the CE model is susceptible to high uncertainty in its
% % parameter estimates. \todo{de-emphasise parameter estimation}
% % However, as with many models for extremes, which suffer from the natural rarity of extreme data, the CE model is susceptible to high uncertainty in its parameter estimates, often making interpretation of the structure of tail dependence difficult.
% % However, as with many models for extremes, which suffer from the natural rarity of extreme data, the CE model is susceptible to high uncertainty in its parameter estimates, often making it difficult to interpret and identify patterns in the tail dependence structure.
% \todo{Give examples of other models}
% However, like many other models for multivariate extremes, the CE model can be difficult to use for identifying patterns in tail dependence due to the sparsity of joint extreme observations, particularly in high dimensions.

% Conditional Extremes
% A popular model for multivariate extremes which fulfils this criterion is the Conditional Extremes (CE) model of \citet{Heffernan2004}. %, which can flexibly capture both AD and AI regimes and is conceptually simple and computationally efficient.
% The Conditional extremes (CE) model of \citet{Heffernan2004} describes the tail behaviour of a random vector \emph{conditional} on one of its components exceeding a suitably high threshold.
The CE model describes the tail behaviour of a random vector \emph{conditional} on one of its components exceeding a suitably high threshold.
% It provides a flexible framework which can capture both AD and AI patterns. % , and other complex extremal dependence patterns. 
% A key advantage is that the CE model provides a flexible framework which can capture both AD and AI patterns. % , and other complex extremal dependence patterns.
% This flexibility, along with its conceptual simplicity and computational efficiency, have made it a popular choice for modelling multivariate extremes. 
This framework can flexibly capture both AD and AI patterns, and is both conceptually simple and computationally efficient.
Spatio-temporal extensions and single-site conditioning methods have been developed to handle high-dimensional settings \citep{Simpson2021, Wadsworth2022}, and adaptations that incorporate covariates, hierarchical structure, and mixture or random-scale constructions further increase the models flexibility \citep{Tawn2018, Richards2023, Tendijck2023}. 
As a result, CE models have been applied across a broad range of environmental problems, where AI as well as AD may be prevalent, including modelling river flow and flood risk \citep{Gilleland2013, Jane2020}, rainfall extremes \citep{Richards2022}, sea-surface temperature extremes \citep{Simpson2023,Kakampakou2024}, and heatwaves \citep{Winter2016}, and so are well suited to this class of problems and our motivating example.
% Despite these strengths, sparsity of joint extremes, particularly in high dimensions, can make pattern discovery and interpretation difficult.
% However, the sparsity of joint extreme observations, particularly in high dimensions, can make pattern discovery and interpretation using the CE model difficult, which requires further addressing to fully exploit its potential.
However, the sparsity of joint extreme observations, particularly in high dimensions, and the number of model parameters often results in CE model estimates being highly uncertain. 
As such, it is difficult to visually compare CE models.

% % Clustering: introduction + motivation
% \todo{Combine with next paragraph}
% \todo{Should I say something here or above about being interested in the spatial domain?}
% % \todo{C: Focus on learning spatial structure in tail dependence, reduction of unceratinty is only mentioned as potential benefit}
% % One method for improved parameter estimation is through clustering.
% % Clustering is an unsupervised learning technique which seeks to group similar data together, such that the data within each group is more similar to each other than to data in other groups \citep{Hastie2009}.
% % Clustering is a powerful tool for exploratory analysis, allowing us to identify patterns in the data and improve interpretability of the extremal dependence structure.
% % Clustering allows us to pool information from multiple sites or variables.
% % This pooling can lead to more robust parameter estimates, as it reduces the uncertainty associated with estimating parameters from a single site or variable.
% % Clustering is a popular method in extremes, where data is naturally scarce, often leading to high uncertainty and difficulty in intrepretation \citep{Ferreira2023-mz, Zheng2023-jf}.
% %   % \item Extremal clustering is chiefly concerned with the problems of exploratory analysis and improved interpretability of extremal dependence structures, and also in reducing parameter uncertainty.
% % new
% % A useful and popular approach to improve interpretability for extremal dependence modelling is clustering, which has a seen widespread use in the analysis of multivariate extremes \citep{Ferreira2023-mz, Zheng2023-jf}.
% A useful and popular approach to improve interpretability for extremal dependence modelling is clustering \citep{Ferreira2023-mz, Zheng2023-jf}. \todo{discuss these references if provided}
% % Clustering is an unsupervised learning technique that groups observations by similarity without prior labels \citep{Hastie2009}, serving as a powerful exploratory tool for uncovering spatial structure in tail dependence.  
% % Clustering is an unsupervised learning technique which seeks to group similar data together, such that the data within each group is more similar to each other than to data in other groups \citep{Hastie2009}.
% Clustering is an unsupervised learning technique which seeks to group similar data \citep{Hastie2009}.
% % By partitioning sites into clusters with homogeneous joint‐tail behaviour, we can gain clearer insight into spatial heterogeneity and complex dependence patterns.  
% % In a spatial setting, we can partition sites into clusters with homogeneous joint‐tail behaviour, to gain clearer insight into spatial heterogeneity and complex dependence patterns.  
% In a spatial context with multivariate observations at each location, we can partition sites into clusters exhibiting similar joint tail behaviour, thereby revealing spatial patterns in complex dependence structures.
% 
% % Clustering: past work (interpretability)
% % Exploratory clustering for improved interpretability, in extremes and
% % elsewhere, generally focuses on deriving some similarity matrix for a given
% % distance, and then using performing some generic clustering algorithm such as k-means or k-mediods on this matrix \citep{Lloyd1957, Kaufman1987}.
% % \todo{C: Be clearer on the aspects considered in clustering}
% % \todo{Cut down on detail? Longest section!}
% \todo{Quite long!}
% Several clustering methods have been proposed for extremes. % \citep{Carreau2017, Dupuis2023, Rohrbeck2021}
% % ~\citet{Rohrbeck2021} proposes a spatial extremal clustering algorithm for hydrological variables, which involves Bayesian hierarchical modelling consisting of an dependence component using the $\chi$ statistic dependence, and a marginal component consisting of a generalised Pareto distribution (GPD) fitted to each cluster.
% % This method uses the Reversible Jump MCMC (RJMCMC) clustering algorithm of \citet{Bottolo2003}, which avoids the need to fix the number of clusters a priori.
% % Alternatively, frequentist approaches have used the Expectation–-Maximisation (EM) algorithm to identify group structure and fit group-specific models for the tails, with the number of clusters typically selected via out-of-sample cross-validation \citep{Carreau2017, Dupuis2023}.
% Bayesian approaches, by \citet{Bottolo2003} and \citet{Rohrbeck2021}, use hierarchical models with Reversible-Jump Monte Carlo Markov Chain (RJMCMC) to jointly capture marginal behaviour and extremal dependence, avoiding the need to fix the number of clusters in advance. 
% Frequentist alternatives employ the Expectation-Maximisation (EM) algorithm to fit group-specific tail models, with the number of clusters selected via cross-validation \citep{Carreau2017, Dupuis2023}. 
% % One common approach for extremal clustering is to define a distance measure quantifying the pairwise similarity in the tail of random variables/vectors, and then apply a generic clustering algorithm such as k-means or k-medoids \citep{Lloyd1957, Kaufman1987}.
% Another common strategy is to define a pairwise dissimilarity measure between sites and then apply a generic clustering algorithm such as $k$-means or $k$-medoids \citep{Lloyd1957, Kaufman1987}. 
% % There is a large body of literature concerning such clustering methods for extremes. 
% % % In \citet{Bernard2013}, a clustering algorithm is derived for weekly
% % % precipitation maxima for sites across France, which uses the F-madogram as a
% % % distance measure. \todo{C: be more clear about the tail characteristics used for clustering}
% % % In financial applications, \citet{deCarvalho2023} proposes a clustering algorithm for stock prices, which uses the extremal index and heteroscedastic function, loosely interpretable as the magnitude and frequency of extreme events, to define a bivariate centroid with which to cluster stocks on the London Stock Exchange. 
% \todo{Bit of a non sequitur given emphasis on envoronmental extremes elsewhere?}
% \todo{Or should I be less focused on purely spatial applications elsewhere?}
% % In financial applications, \citet{deCarvalho2023} proposes a clustering algorithm for grouping stocks on the London Stock Exchange based on their tail behaviour. %, using the extremal index and heteroscedastic function, which can be loosely interpreted as the magnitude and frequency of extreme events.
% % In environmental extremes, \citet{Bernard2013} describes a clustering algorithm of weekly precipitation maxima across France.
% % Focusing on the spatial tail dependence structure of the data, this method uses the F-madogram, an extremal analogue of the classical variogram, as a distance measure.
% % Applications include finance, where clustering has been based on indices of extremal frequency and magnitude \citep{deCarvalho2023}, and environmental extremes, where the F-madogram has been used to group precipitation sites across France \citep{Bernard2013}. 
% Applications include finance, with clustering based on indices of extremal frequency and magnitude \citep{deCarvalho2023}, and environment, where the F-madogram \citep{Cooley2006variograms} was used to group precipitation sites across France \citep{Bernard2013}, and where generalised Pareto distributions (see, e.g.\,~\citet{Coles2001}) fitted to extreme temperatures were compared using the Kolmogorov-Smirnov and $L_1-$Wasserstein distances to cluster autonomous regions in Spain \citep{Alonso2014-rr}. 
% % % \citet{Vignotto2021} proposes a k-mediods algorithm for clustering locations in Great Britain and Ireland for precipitation and wind speed data.
% % % \citet{Vignotto2021} proposes a k-medoids algorithm for clustering locations in Great Britain and Ireland based on the joint tail behaviour of gridded precipitation and wind speed data from ERA5 \citep{Hersbach2020}.
% % % Individually at each location, bivariate data are transformed to Pareto margins.
% % % The maximum across both transformed margins is used to define extreme observations.
% % % This bivariate space is then partitioned into three disjoint sets, corresponding to extremal observations for both precipitation and wind speed, only precipitation, or only wind speed.
% % % An empirical estimate of the Kullback-Leibler divergence for each pairwise comparison of locations is computed based on these sets, which is used to define a dissimilarity matrix \citep{Kullback1951}.
% % % This method is our main competitor, similarly focusing on improving interpretation of tail behaviour in a closely related application to our own, but it must be noted that it is limited to the bivariate case. 
% % % \citet{Vignotto} proposes a clustering algorithm for clustering and Ireland based on bivariate extreme dependence, based on an empirical estimate for the Kullback-Leibler divergence \citep{Kullback1951}.
% % % \citet{Vignotto2021} introduces a non-parametric clustering method which groups locations in Great Britain and Ireland based on the joint tail behaviour of gridded precipitation and wind speed data from ERA5 \citep{Hersbach2020}.
% % % % The bivariate random vectors at each location are transformed to Pareto margins, and the maximum across both transformed margins is used to define extreme observations.
% % % An empirical estimate of the Kullback-Leibler $\left(\KL\right)$ divergence is computed for each pairwise comparison of locations, which is used to define a dissimilarity matrix \citep{Kullback1951}. \todo{no need to define KL below)
% % % % This method is our main competitor, and has a similar focus on interpretation tail dependence interpretation of tail behaviour in a closely related application to our own. 
% % % This method is our main competitor, and has both a similar focus on improving the interpretability of tail dependence structures, and a closely related application to our own, as detailed in Section~\ref{sec:ire}. \todo{Probably still need to highlight why it is our competitor (treats sites separately, ignores space)}
% % % However, we believe it has several notable limitations. 
% % % Firstly, it is limited to the bivariate case, which may be limiting in many applications. 
% % % Secondly, it's non-parametric introduces several possible drawbacks.
% % % While avoiding misspecification, it does not provide a model to perform further inference on the tail dependence structure, such as estimating the probability of jointly extreme events.
% % % Also, covariates such as spatial information cannot be incorporated, which may be useful in many applications. \todo{Should I mention this if we don't do it ourselves?}
% % % Furthermore, reliance on the empirical frequencies of bivariate and univariate extremes, which are inherently rare, may lead to poor clustering performance unless sufficiently large datasets are available.
% % % \citet{Vignotto2021} introduces a non-parametric clustering method that groups locations in Great Britain and Ireland based on the joint tail behaviour of gridded precipitation and wind speed data.
% % Tail dependence at each location is described non-parametrically using empirical counts of extreme events in transformed bivariate observations, ignoring any spatial dependence.
% % These counts are used to estimate the Kullback--Leibler ($\KL{}$) divergence between each pair of locations \citep{Kullback1951}, and $k$-medoids clustering is then applied to group sites with similar bivariate tail structures.
% % This method is our main competitor, as the setting closely resembles the multivariate spatial framework we consider. 
% Most relevant to our work, \citet{Vignotto2021} develop a non-parametric method for clustering bivariate extremes at locations in Great Britain and Ireland by estimating pairwise Kullback-Leibler ($\KL{}$) divergences and applying the $k$-medoids algorithm. 
% % % However, this approach has several limitations, in that it is limited to the bivariate case, and, being non-parametric, lacks a generative model for tail dependence inference, and may perform poorly on small datasets due to reliance on rare empirical extremes.  
% % However, this approach has several limitations, in that it is limited to the bivariate case, and, being non-parametric, lacks a generative model for tail dependence inference, and may perform poorly without large datasets due to reliance on sufficient empirical occurrences of joint and marginal extremes to characterize tail dependence.
% While closely aligned with our application, this approach is limited to the bivariate case and, being non-parametric, lacks a generative model for inference, and may perform poorly without a large sample size.

% \todo{Remove these two paragraphs, and combine into below}
% % Clustering: introduction + motivation
% A useful and popular approach to improve interpretability for extremal dependence modelling is clustering \citep{Ferreira2023-mz, Zheng2023-jf}. \todo{discuss these references if provided}
% Clustering is an unsupervised learning technique which seeks to group similar data \citep{Hastie2009}.
% In a spatial context with multivariate observations at each location, we can partition sites into clusters exhibiting similar joint tail behaviour, thereby revealing spatial patterns in complex dependence structures.
% 
% % Clustering: past work (interpretability)
% Several clustering methods have been proposed for extremes. % \citep{Carreau2017, Dupuis2023, Rohrbeck2021}
% Bayesian approaches, by \citet{Bottolo2003} and \citet{Rohrbeck2021}, use hierarchical models with Reversible-Jump Monte Carlo Markov Chain (RJMCMC) to jointly capture marginal behaviour and extremal dependence, avoiding the need to fix the number of clusters in advance. 
% Frequentist alternatives employ the Expectation-Maximisation (EM) algorithm to fit group-specific tail models, with the number of clusters selected via cross-validation \citep{Carreau2017, Dupuis2023}. 
% Another common strategy is to define a pairwise dissimilarity measure between sites and then apply a generic clustering algorithm such as $k$-means or $k$-medoids \citep{Lloyd1957, Kaufman1987}. 
% Applications include finance, with clustering based on indices of extremal frequency and magnitude \citep{deCarvalho2023}, and environment, where the F-madogram \citep{Cooley2006variograms} was used to group precipitation sites across France \citep{Bernard2013}, and where generalised Pareto distributions (see, e.g.\,~\citet{Coles2001}) fitted to extreme temperatures were compared using the Kolmogorov-Smirnov and $L_1-$Wasserstein distances to cluster autonomous regions in Spain \citep{Alonso2014-rr}. 
% Most relevant to our work, \citet{Vignotto2021} develop a non-parametric method for clustering bivariate extremes at locations in Great Britain and Ireland by estimating pairwise Kullback-Leibler ($\KL{}$) divergences and applying the $k$-medoids algorithm. 
% While closely aligned with our application, this approach is limited to the bivariate case and, being non-parametric, lacks a generative model for inference, and may perform poorly without a large sample size.

% % Clustering: past work (improved parameter estimation)
% % \todo{Add line about that new paper which does have multivariate clustering, but not CE (which one?)}
% % \todo{Do we want to include a paragraph on this? Or de-emphasise it?}
% % \todo{If including, probably need to cut down!} % Better to get something down now though!}
% % Extremal clustering for improved parameter estimation also exists in the literature, and generally focuses on partial-pooling of information across multiple sites or variables using hierarchical models.
% % In the Bayesian setting, \citet{Bottolo2003} is an early example of extremal clustering, defining exceedances over a high threshold as generated by a Poisson process (PP), and proposing a Bayesian hierarchical model with a separate hierarchical mixture prior for each PP parameter, each having a specific latent group structuring.
% % The model is estimated using a Reversible Jump MCMC (RJMCMC) algorithm, which allows for the estimation of the number of clusters, rather than fixing it a priori.
% % Another early example is \citet{Cooley2007}, which modelled return levels for extreme precipitation in Colorado.
% % Separate hierarchical models were used to estimate for intensity and frequency of extremes for a given location using the GPD and the binomial distribution, respectively, both of which incorporated latent spatial processes.
% % ~\cite{Rohrbeck2021} proposes a clustering algorithm utilising Bayesian hierarchical modelling, which consists of a dependence component using the $\chi$ statistic to cluster sites based on their extremal dependence, and a marginal component consisting of a generalised Pareto distribution (GPD) fitted to each cluster.
% % This method also uses a RJMCMC algorithm, and was specifically developed for hydrological variables. 
% % Frequentist alternatives generally use mixture models estimated using the Expectation-Maximisation (EM) algorithm.
% % \citet{Carreau2017} derives a model which utilises a conditional mixture of GPDs with subregions defined by a constant shape parameter.
% % The number of subregions to partition the data into was chosen by out-of-sample cross-validation using three different loss functions, including the Anderson-Darling statistic, which is used to compare probability distributions and is particularly sensitive to tail deviation.
% % In \citet{Dupuis2023}, the EM algorithm is used to identify group structure and group-specific model parameters for generalised extreme value (GEV) distributed panel data.
% % A simulation study using this model was performed, and applications to financial risk, extreme temperature and flood risk data were shown to be effective, and superior to methods using groupings based on domain knowledge.
% Another popular approach to extremal clustering in extremes is to estimate hierarchical and mixture models, which can group multiple sites or variables and pool information across them.
% In the Bayesian setting, Monte Carlo Markov Chain (MCMC) methods are often used, such as the Reversible Jump MCMC (RJMCMC) algorithm, which can estimate both the extremal grouping structure and marginal parameters, and avoids the need to fix the number of clusters a priori \citep{Bottolo2003, Cooley2007, Rohrbeck2021}.
% % Alternatively, frequentist models are often estimated using the Expectation-Maximisation algorithm to identify group structures and group-specific model parameters, with the number of clusters often chosen by out-of-sample cross-validation \citep{Carreau2017, Dupuis2023}.
% Alternatively, frequentist approaches often use the Expectation–Maximisation (EM) algorithm to identify group structure and fit group-specific models for the tails, with the number of clusters typically selected via out-of-sample cross-validation \citep{Carreau2017, Dupuis2023}. \todo{Need to mention selecting k here?}

% Papers to possibly add?
% Clustering (combined)
% NEED TO WRITE AGAIN, NOT GOOD
% Clustering is an unsupervised learning technique which seeks to group similar data \citep{Hastie2009}.
% Clustering approaches for extremes can be grouped into three types.
% Univariate clustering methods group sites by marginal extreme summaries such as return levels tail indices or Generalised Pareto Distribution (GPD) parameter estimates and then apply standard clustering algorithms such as k-means and k-medoids \citep{Coles2001, Lloyd1957, Kaufman1987}.
% % Examples of univariate or marginal clustering include finance applications that group assets by extreme value indices as in \citet{deCarvalho2023} and regional grouping by extreme temperature summaries as in \citet{Alonso2014-rr}.
% Some examples of univariate clustering include \citet{deCarvalho2023}, where financial securities were clustered based on indices of extremal frequency and magnitude, and in environmetal applications \citet{Cooley2007} clustered return levels for extreme precipitation in Colorado. \todo{add additional references here!}
% Other methods perform multivariate clustering by comparing joint tail summaries or fitted multivariate tail models at each site. 
% For example, \citet{Vignotto2021} estimates pairwise KL style divergences from empirical bivariate wind speed and precipitation extremes and clusters with k-medoids.
% \todo{More examples}
% A further strand clusters entire spatial fields or process realizations 
% and recent work develops divergence based spatial clustering for compound precipitation and wind extremes \citep{Boulin2025}. \todo{Check!}
% Our method belongs to the second, multivariate site-wise strand.
% We fit a bivariate conditional extremes model at each site compute a closed-form model based dissimilarity between the fitted conditional tail distributions using the skew geometric Jensen Shannon divergence \(\JSGa{}\) and cluster sites according to that dissimilarity.
% This approach accommodates both asymptotic dependence and asymptotic independence provides parametric models for direct tail inference and offers computationally efficient pairwise comparisons compared with purely empirical methods such as the KL based approach of \citet{Vignotto2021}.

% Clustering: introduction + motivation
% A useful and popular approach to improve interpretability in extremal dependence modelling is clustering, an unsupervised technique that groups observations by similarity without prior labels \citep{Hastie2009}.
% In a spatial context with multivariate observations at each location we can partition sites into clusters that exhibit similar joint tail behaviour and thereby reveal spatial patterns in complex dependence structures.
% We distinguish three types of clustering for extremes across sites.
% \emph{Univariate} methods pool locations by marginal extreme summaries such as return levels, tail indices, or Generalised Pareto Distribution (GPD) parameter estimates and then apply standard clustering algorithms such as $k$-means or $k$-medoids \citep{Coles2001, Lloyd1957, Kaufman1987}.
% GPD parameter estimates have been used to cluster extreme temperatures in Spain \citep{Alonso2014-rr}, and marginal summaries or frequency-based indices have supported clustering in finance and environmental applications \citep{deCarvalho2023, Zheng2023-jf}.
% \emph{Multivariate} approaches compare joint tail summaries or fitted multivariate tail models at each site and include both nonparametric and model-based methods \citep{Carreau2017, Dupuis2023, Bottolo2003}.
% A leading nonparametric example is \citet{Vignotto2021}, who estimate empirical pairwise Kullback--Leibler ($\KL{}$) divergences from bivariate wind and precipitation extremes and then apply $k$-medoids.
% \emph{Spatial} clustering targets regions or processes with similar spatio-temporal tail dependence and typically builds pairwise tail-focused dissimilarities, for example the F-madogram, before applying generic clustering to map coherent regions \citep{Cooley2006variograms, Bernard2013, Rohrbeck2021}.
% Model-based spatial alternatives incorporate spatial information into divergence measures to encourage spatially coherent clusters \citep{Boulin2025}.
% Our problem is bivariate extremes of precipitation and wind at sites across Ireland.
% At each site we fit a multivariate tail model for the two variables and then cluster sites by the fitted multivariate tail characteristics.
% Thus our approach is fundamentally multivariate at each site while remaining applicable to spatial problems. 
% In this context \citet{Vignotto2021} is our main competitor.
% The method they employ has some weaknesses, namely in that it is restricted to the bivariate case, and, being non-parametric, lacks a generative model for tail dependence inference, and may perform poorly without a large dataset due to reliance on sufficient empirical occurrences of joint and marginal extremes to characterize tail dependence.
% Also, it is undefined where no jointly extreme events are observed, which can occur frequently in practice, and so is unsuitable for applications where AI may be prevalent.

% Clustering (final) 
% A popular approach to improve interpretability in extremal dependence modelling is clustering, which is an unsupervised learning technique that groups data with similar statistical characteristics \citep{Hastie2009}.
% One widespread approach to improve interpretability for extremal dependence models is clustering, which is an unsupervised learning technique that groups observations by similarity without prior labels \citep{Hastie2009}. \todo{C: Not sure if this is best start. ``Interpretability'' does not really capture the essence}
% Clustering is an unsupervised learning technique that groups data with similar statistical characteristics \citep{Hastie2009}, reducing data sparsity and model complexity, often leading to more robust inference, reduced parameter uncertainty and simplified interpretation.
One widespread approach to reduce complexity in extremal dependence models is clustering, which is an unsupervised learning technique that groups observations by similarity without prior labels \citep{Hastie2009}.
% We distinguish three types of clustering for extremes across spatial locations. 
% Clustering has seen widespread use in the extremes literature, where we distinguish three types of clustering for extremes across spatial locations. 
Clustering is a popular tool in the extremes literature, where data sparsity provides a natural motivation, and we distinguish three types of extremal clustering across spatial locations.
\emph{Univariate} methods group sites by marginal tail summaries, such as return levels or parameter estimates \citep{Coles2001}, and have been used in environmental and financial applications \citep{Alonso2014-rr, deCarvalho2023, Zheng2023-jf}.
\emph{Spatial} methods cluster over entire spatial fields or process realizations.
They may build pairwise tail dissimilarities such as the F-madogram before clustering \citep{Cooley2006variograms, Bernard2013}, or fit spatially non-stationary extreme value models \citep{Carreau2017, Rohrbeck2021, Dupuis2023}. 
% \emph{Multivariate} methods compare joint tail summaries or fitted multivariate tail models at each site.
Finally, \emph{multivariate} methods compare joint tail summaries or fitted multivariate tail models at each site, which exactly matches our setting. 
% A leading nonparametric example was provided by \citet{Vignotto2021}.
\citet{Vignotto2021} is our main competitor here, also considering bivariate extremes of precipitation and wind speeds at locations in Great Britain and Ireland.
Their nonparametric approach estimated empirical pairwise Kullback--Leibler ($\KL{}$) divergences for bivariate precipitation and wind extremes and then applied $k$-medoids  \citet{Kullback1951}. % \todo{Jordan wants ``extreme value distributions fitted to'' added here, but I thought they didn't fit any?}
% Finally, \emph{spatial} methods target regions or processes with similar spatial tail dependence and often build pairwise tail dissimilarities such as the F-madogram before clustering \citep{Cooley2006variograms, Bernard2013, Rohrbeck2021}.
However, their method has some notable limitations, in that it is restricted to the bivariate case.
% However, their method is restricted to the bivariate case and the AD regime, which may be limiting both in our application and in applying the method more generally to this class of multivariate clustering problem. 
Furthermore, being non-parametric, it lacks a generative model for tail dependence inference, and may perform poorly without a large sample size. 
Also, it is undefined where no joint extreme events are observed, which can occur frequently in practice, and so is unsuitable for applications where AI may be prevalent, which we expect in our motivating example.
Therefore, this approach may be limiting for our motivating example and across this class of problems, indicating the need for a more flexible and general clustering framework within multivariate extremes.

% Clustering: Motivating our clustering algorithm
% % While clustering is evidently not novel within extremes, we hope to identify some opportunities to adapt some of the existing methods in the field to the CE model, which has not yet been explored in the content of clustering.
% % One advantage of focusing on the CE model is that it can be applied to any dimension, which contrasts with existing clustering methods such as \citet{Vignotto2021} for multivariate extremes, which are limited to the bivariate case.
% % Also, as mentioned, the Gaussian assumption in the CE model allows for a simple, closed-form divergence metric upon which to base our clustering framework.
% % As opposed to \citet{Vignotto2021}, our method is parametric, and so we hope that our method will provide a more powerful and principled framework for performing exploratory analysis on multivariate extremes and identifying locations with similar tail dependence structure.
% % Clustering has evidently been widely utilised in EVT. 
% Clustering has evidently seen widespread use in the analysis of multivariate extremes. 
% Our main contributions to this literature are to adapt some of these existing approaches to the Conditional Extremes (CE) model, which has not previously been explored in clustering. 
% Our main contribution to the literature is to perform clustering on the CE model, which has not previously been explored. 

% Clustering: Motivating our clustering algorithm
% Our method performs clustering of multivariate extremes using the CE model.
% In this paper we perform clustering using the CE model. 
% To improve on existing approaches, we develop a novel clustering framework based on the CE model, which has hitherto not been explored. 
In this paper, we seek to address the limitations of existing methods by developing a clustering framework based on the CE model, which has hitherto not been explored.
% This will allow us to flexibly capture tail dependence in arbitrary dimensions, while overcoming 
This enables flexible, parametric exploration of tail dependence structures in arbitrary dimension, in contrast to the existing non-parametric approach of \citet{Vignotto2021}, while also overcoming the data sparsity issues often associated with drawing interpretations from the CE model.
% The Gaussian working assumption in the CE model allows for a simple, closed-form divergence measure upon which to base our clustering framework.
% Using the working conditional Gaussianity assumption of \citet{Heffernan2004} for estimating the CE model, we define a simple, closed-form divergence measure for multivariate extremes which permits clustering.
Using the working conditional Gaussian assumption of \citet{Heffernan2004} for estimating the CE model, we define a closed-form divergence measure for CE models which permits clustering.
Our parametric approach delivers a unified and statistically principled tool for exploratory analysis of multivariate extremes, facilitating the identification of spatial locations with similar tail dependence characteristics.  

% Structure of paper
The remainder of this paper is structured as follows.
% In Section~\ref{sec:ire} of this paper, we introduce a motivating example of the joint tail behaviour of precipitation and wind speed in Ireland, which we will use to illustrate our clustering framework.
% Section~\ref{sec:method} introduces our clustering framework for CE models,
% which includes a novel dissimilarity measure based on the Jensen--Shannon (JS) divergence, and a method for selecting the number of clusters.
Section~\ref{sec:method} introduces our clustering framework for CE models, which includes a novel dissimilarity measure based on the 
% skew-geometric Jensen--Shannon divergence $\left(\text{JS}^{\textrm{G}_{\alpha}}\right)$, and a method for selecting the number of clusters.
skew-geometric Jensen--Shannon divergence $\left(\JSGa{}\right)$ of \citet{Nielsen2019}. %, and a method for selecting the number of clusters.
% A simulation study is described and performed in Section~\ref{sec:sim}, to compare our method to the leading competitor in the bivariate case, and we show that it outperforms this method across a range of extremal dependence structures.
Section~\ref{sec:sim} describes a simulation study, where we demonstrate that our approach outperforms the method by \citet{Vignotto2021} across a range of extremal dependence structures. 
In Section~\ref{sec:application}, we apply our clustering framework to Irish meteorological data, and show how it can be used to identify sites with similar tail dependence between precipitation and wind speed.
Section~\ref{sec:discuss} concludes with a discussion of our findings, the limitations of our method, and potential future work.


\section{Methodology}
\label{sec:method}

% \subsection{Peaks-over-threshold}

% In this section, we introduce our clustering framework for CE models.
% In this section, we introduce our clustering framework for multivariate extremes. 
In this section, we introduce our clustering framework. % for CE models. 
% We begin in Section~\ref{subsec:asym_dep} by defining the concepts of asymptotic dependence and independence, which are used to characterise extremal dependence.
% We introduce the CE model in Section~\ref{subsec:ce_model}, which is used to model the joint tail behaviour of a random vector conditional on one of its components exceeding a suitably high threshold.
% We begin in Section~\ref{subsec:ce_model} by introducing the CE model of \citet{Heffernan2004}, which is used to model the joint tail behaviour of a random vector conditional on one of its components exceeding a suitably high threshold.
Section~\ref{subsec:ce_model} provides background on the conditional extremes (CE) model of \citet{Heffernan2004} and \citet{Keef2013}. %, which is used to model the joint tail behaviour of a random vector conditional on one of its components exceeding a suitably high threshold.
% We explain how inference with this model is equivalent to estimating a multivariate Gaussian distribution. 
% In Section~\ref{subsec:div}, we describe our chosen dissimilarity measure, the skew-geometric Jensen-Shannon divergence, which has analytical form when comparing Gaussian distributions, and allows us to efficiently quantify the pairwise dissimilarity between fitted CE models.
In Section~\ref{subsec:div}, we describe our novel dissimilarity measure for multivariate CE, which uses the skew-geometric Jensen-Shannon divergence, to efficiently quantify pairwise dissimilarity between fitted CE models.
% Finally, Section~\ref{subsec:clustering} outlines how we perform k-medoids clustering using this dissimilarity measure, to identify sites with similar tail dependence structures, and how the number of clusters is selected.
Finally, in Section~\ref{subsec:clustering}, we outline how to perform clustering using this dissimilarity measure, and provide practical advice on choosing the number of clusters.
% Finally, in Section~\ref{subsec:estimation} we describe some of the computational details which are important for estimating the CE model and computing the $\JSGa{}$ divergence.

% \subsection{Asymptotic dependence}~\label{subsec:asym_dep}
% 
% \todo{Need to distinguish between asymptotic and extremal/tail dependence}
% \todo{Use extremal and tail dependence interchangeably? Or just extremal dependence?}
% 
% To characterise tail dependence, we first introduce the concepts of asymptotic dependence and independence, as described in \citet{Coles1999}.
% % The coefficient of asymptotic dependence, $\chi \in [0, 1]$, measures the degree of asymptotic dependence between two random variables $X_1$ and $X_2$ with corresponding CDFs $F_1$ and $F_2$, under the class of asymptotically dependent distributions.
% The coefficient of asymptotic dependence, $\chi \in [0, 1]$, measures the strength of extremal dependence between two random variables $X_1$ and $X_2$ with corresponding distribution functions $F_1$ and $F_2$, and is given by % , under the class of asymptotically dependent distributions, and is given by 
% % It is defined as $\chi = \lim_{u \rightarrow 1}{\chi(u)}$, where
% % \[
% % \chi(u) = 2 - \frac{\log\left(\mathbb{P}(F_1(X_1) < u, F_2(X_2) < u)\right)}{\log \left(\mathbb{P}(F_1(X_1) < u)\right)}.
% % \]
% \[
%   \chi = \lim_{u\to1}\mathbb{P}\bigl[F_1(X_1)>u\mid F_2(X_2)>u\bigr].
% \]
% If $\chi = 0$, then $X_1$ and $X_2$ are said to be asymptotically independent while, for $\chi > 0$, they are termed asymptotically dependent, with greater strength as $\chi$ increases.
% % Thus, $\chi$ gives the limiting probability of $X_1$ being extreme given that $X_2$ is also extreme, and we expect the joint probability of observing extremal events in both variables to be greater when $\chi$ is higher \citep{Rohrbeck2021}.
% % Furthermore, $\chi$ is symmetric in $X_1$ and $X_2$.
% % % Note, two variables can be asymptotically dependent even if they are not dependent in the classical sense.
% % Note, two variables $X_1$ and $X_2$ can exhibit asymptotic dependence even when they are not dependent in the classical sense, i.e.\ they do not exhibit strong correlation in the bulk of their distributions.
% % % Another coefficient, $\bar{\chi} \in [-1, 1]$, is used to measure the strength of dependence in the case of asymptotically independent distributions, where $\chi = 0$.
% % Another coefficient, $\bar{\chi} \in [-1, 1]$, measures the tail dependence between two variables where $\chi = 0$, the case of asymptotically independent variables.
% % This coefficient is defined as $\bar{\chi} = \lim_{u \rightarrow 1}{\bar{\chi}(u)}$, where
% % \todo{Is there a better definition than this, given I've changed chi?}
% % \[
% %   \bar{\chi}(u) = \lim_{u \rightarrow 1} \frac{2\log\mathbb{P}(F_1(X_1) > u)}{ \log{\mathbb{P}(F_1(X_1) > u, F_2(X_2) > u)}} - 1.
% % \]
% % For asymptotic dependence, $\bar{\chi} = 1$.
% % Under asymptotic independence, $\bar{\chi} = 0$, with $\bar{\chi}$ increasing in magnitude as the variables become more positively (for $\bar{\chi} > 0$) or negatively (for $\bar{\chi} < 0$) dependent in their extremes \citep{Vignotto2021}.
% 
% % Together, $(\chi, \bar{\chi})$ provide a useful summary of extremal dependence for any pair of random variables.
% 
% % $\chi$ provides a useful summary of extremal dependence for any pair of random variables. 
% % % However, they do not fully describe the dependence structure of a multivariate distribution, or allow for further inference.
% % However, it does not fully describe the dependence structure of a multivariate distribution, or allow for further inference.
% While $\chi$ provides a useful summary of extremal dependence for any pair of random variables, it does not fully describe the dependence structure of a multivariate distribution, or allow for further inference.
% % For example, probabilities of jointly extreme events for two or more variables cannot be calculated from these coefficients.
% For example, $\chi$ cannot be used to calculate the joint exceedance probability of two or more variables.
% For this, we require a model for extremal dependence.
% % Various approaches to this problem have been proposed, including max-stable processes, Pareto processes, Gaussian processes and copulas.
% % However, these models are limited in their ability to model dependence and their computational feasibility, and are often conceptually difficult. 
% % % For example, max-stable processes can only model $\mathbb{P}(\symbf{X} \in \symbf{C})$ under asymptotic dependence between each pair of variables in $\symbf{X}$, while in contrast Gaussian processes can only model asymptotic independence \citep{Tawn2018, Huser2024}.
% % % Max-stable processes are also quite computationally expensive and conceptually difficult to derive, and so are often not feasible for large datasets.
% % A framework which provides a more flexible approach to modelling extremal dependence is the CE model of \citet{Heffernan2004}.
% % A framework which provides a flexible approach to modelling extremal dependence is the CE model of \citet{Heffernan2004}. 
% One such model which provides a flexible framework for describing tail dependence is the CE model of \citet{Heffernan2004}.

% \subsection{Multivariate conditional extremes}~\label{subsec:ce_model}
\subsection{Multivariate conditional extremes}~\label{subsec:ce_model}

% See Jordan's paper for quick summary (TFR a bit long winded) (and Keef)
% Need to talk about marginals, transformations and then model itself

% Jordan's EEG paper: "Transforming the data to a common margin avoids observing
% spurious dependence caused by differences in magnitude, and can be achieved
% via a simple rank-transformation. Here, we used the Laplace distribution, as
% its tails are known to converge faster to a non-degenerate distribution as we
% increase v" (Rohrbeck and Tawn)

% Transformation
% The CE model, like many other models for multivariate extremes, requires a marginal transformation.
% Standardising to common margins is a widespread technique to ensure differences in variable scale do not affect estimation of the dependence structure. 
% Like many other models for multivariate extremes, the conditional extremes model of \citet{Heffernan2004} is defined on standardised margins, to ensure that differences in variable scale do not affect estimation of the dependence structure.
Like many other models for extremal dependence (see, e.g.\,~\citet{Davison2015}), the conditional extremes framework of \citet{Heffernan2004} is defined for data on standardised margins, which ensures that differences in marginal scale do not affect estimation of the dependence structure.
% A popular choice for CE models is Laplace margins, as introduced in \citet{Keef2013}.
As introduced in \citet{Keef2013}, a useful choice of margins for CE models is standard Laplace, as it allows for modelling of both positive and negative dependence.
% Each marginal random variable $X_i, i = 1, \ldots, d, (X_1, \ldots, X_d) \in \mathbb{R}^d$ is transformed to Laplace margins, as outlined in equation~\eqref{eq:laplace} in Section \ref{subsec:estimation}, and thereafter denoted by $Y_i, \(Y_1, \ldots Y_d\) \in \mathbb{R}^d$. 
% To transform each marginal random variable $X_i$ to Laplace margins, denoted by $Y_i$, we let:
% Each marginal random variable $X_i, i = 1, \ldots, d, (X_1, \ldots, X_d) \in \mathbb{R}^d$ is transformed to Laplace margins, thereafter denoted as $Y_i$, using the probability integral transform:
% As it allows for both positive and negative dependence to be modelled, each marginal random variable $X_i, i = 1, \ldots, d$, is transformed to Laplace margins, thereafter denoted as $Y_i$, using the probability integral transform:
%  \begin{equation}~\label{eq:laplace}
%    Y_i \coloneqq \begin{cases}
%      \log\left\{2F_{X_i}(X_i)\right\}, &\text{ for } X_i < F_{X_i}^{-1}(0.5), \\
%      % -\log\left\{2(1 - F_{X_i}(X_i))\right\} &\text{ for } X_i \ge F_{X_i}^{-1}(0.5), \\
%      -\log\left[2\left\{ 1 - F_{X_i}(X_i)\right\} \right], &\text{ for } X_i \ge F_{X_i}^{-1}(0.5), \\
%    \end{cases}
%  \end{equation}
% \todo{trying to be brief, but would like to have quick line about why we use Laplace!}
Let $\symbf{X} \coloneqq (X_1, \ldots, X_d) \in \mathbb{R}^d$ be a $d$-dimensional random vector representing observed data with arbitrary margins. 
% We apply the probability intergral transformation to obtain a random vector with Laplace margins, denoted by $\symbf{Y} = (Y_1, \ldots, Y_d) \in \mathbb{R}^d$.
We apply the probability integral transformation to transform $\symbf{X}$ to a random vector with standard Laplace margins, denoted by $\symbf{Y} \coloneqq (Y_1, \ldots, Y_d) \in \mathbb{R}^d$.
Formally, for $i = 1, \ldots, d$, 
\begin{equation}~\label{eq:laplace}
  Y_i \coloneqq \begin{cases}
    \log\left\{2F_{X_i}(X_i)\right\}, &\text{ for } X_i < F_{X_i}^{-1}(0.5), \\
    % -\log\left\{2(1 - F_{X_i}(X_i))\right\} &\text{ for } X_i \ge F_{X_i}^{-1}(0.5), \\
    -\log\left[2\left\{ 1 - F_{X_i}(X_i)\right\} \right], &\text{ for } X_i \ge F_{X_i}^{-1}(0.5), \\
  \end{cases}
\end{equation}
where $F_{X_i}$ denotes the distribution function of $X_i$. 

% Rigorous Conditional Extremes definition
% \begin{itemize}
%   \todo{Replace with defition that has alpha and beta instead of a and b (like in Jordan's paper)}
%   \todo{Need to define Y -1, and also what alpha mid i is}
%   \item The conditional extremes model of \citet{Heffernan2004} assumes that there exists vector normalising functions $\symbf{a}_{\mid i}(y_i),\symbf{b}_{\mid i}(y_i), \in \mathbb{R} \rightarrow \mathbb{R}^{(d-1)}$ such that for fixed $\symbf{z}_{\mid i}$,
%   % \begin{equation}~\label{eq:ce_limiting_dist}
%   %       \lim_{y_i \rightarrow \infty}\{\mathbb{P}(\symbf{Y}_{-i} \le \symbf{a}_{\mid i}(y_i) + \symbf{b}_{\mid i}(y_i)\symbf{z}_{\mid i} \mid Y_i = y_i)\} = \symbf{G}_{\mid i}(\symbf{z}_{\mid i}),
%   % \end{equation}
%   where $\symbf{G}_{j \mid i}$ is non-degenerate for all $j \ne i$. 
%   \item Furthermore, to ensure $\symbf{G}_{\mid i}$ is well-defined, we have the constraint that 
%   \[
%     \lim_{z \rightarrow \infty}{\symbf{G}_{j \mid i}(\symbf{z})} = 1, \forall j \ne i,
%   \]
%   ensuring that there is no mass at $+\infty$ for any margin, but allowing for
 %   some mass to be at $-\infty$, in order to ensure uniqueness of the solution
 %   \citep{Keef2013}.
%   \item An important consequence of this formulation is the weak assumption that
%   \begin{equation}~\label{eq:limit_independence}
%   \mathbb{P}(\symbf{Z}_{\mid i} \le \symbf{z}_{\mid i}, Y_i - u_i = y_i \mid Y_i > u_i) \rightarrow \symbf{G}_{\mid i}(\symbf{z}_i) \exp(-y) \text{ as } u_i \rightarrow \infty,
%   \end{equation}
%   so that in the limit as $u_i \to \infty$, the extremal exceedances $Y_i - u_i$ and $\symbf{Z}_{\mid i}$ are independent.
%   % \item Methods for assessing the validity of this assumption and hence the conditional extremes model for a given application are explored in section \ref{subsubsec:ce_diagnostics}.
%   \item 
% \end{itemize}

% say Y in d dimensions
% We use $Y_{-i} = \symbf{Y} \backslash Y_{i}, \symbf{Y} \in \mathbb{R}^d, y \in (1, \ldots, d)$ to denote the vector of all variables in $\symbf{Y}$ except $Y_i$, and assume all operations hereafter are performed component-wise.
% For $d \in \mathbb{N}$, we use $ \symbf{Y} \in \mathbb{R}^d, d \in \mathbb{N}$ and for $i \in \{1,\ldots,d\}$ write $Y_{-i} = \symbf{Y}\setminus\{Y_i\}$ to denote the $(d-1)$-dimensional random vector of all components except $Y_i$, and assume all operations below are component-wise. 
% For $d \in \mathbb{N}$, we consider the random vector $\symbf{Y} \coloneqq \left(Y_1, \ldots, Y_d\right) \in \mathbb{R}^d$ and, for $i \in \{1, \ldots, d\}$, we write $\symbf{Y}_{-i} \coloneq \symbf{Y} \setminus Y_i \in \mathbb{R}^{d-1}$ to denote $\symbf{Y}$ with its $i^{\text{th}}$ component removed. 
Let $\symbf{Y}_{-i} \coloneq \symbf{Y} \setminus Y_i \in \mathbb{R}^{d-1}$ denote $\symbf{Y}$ with its $i^{\text{th}}$ component removed, for $i \in \{1, \ldots, d\}$.
% Assume all operations below are component-wise. 
% \item The CE model of \citet{Heffernan2004} assumes that there exists vector normalising parameters $\alpha_{\mid i} := \symbf{a}_{\mid i}(y_i), \beta_{\mid i} := \symbf{b}_{\mid i}(y_i) \in \mathbb{R} \rightarrow \mathbb{R}^{(d-1)}$ such that for fixed $\symbf{z}_{\mid i} \in \mathbb{R}^{(d-1)}$,
% The CE model of \citet{Heffernan2004} assumes that there exist parameter vectors $\symbf{\alpha}_{\mid i}^{(\CE{})} := \left\{\alpha_{j \mid i}^{(\CE{})} : j\in\{1,\dots,d\}\setminus\{i\}\right\} \in [-1, 1]^{(d-1)}$ and $\symbf{\beta}_{\mid i}^{(\CE{})} := \left\{\beta_{j \mid i}^{(\CE{})} : j\in\{1,\dots,d\}\setminus\{i\} \right\} \in [-\infty, 1]^{(d-1)}$ such that for fixed $\symbf{z}_{\mid i} \in \mathbb{R}^{(d-1)}$, $y_i > 0$,
% The CE model of \citet{Heffernan2004} assumes that for each $i = 1, \ldots, d$ there exist parameter vectors $\symbf{\alpha}_{\mid i} := \left\{\alpha_{j \mid i} : j\in\{1,\dots,d\}\setminus i \right\} \in [-1, 1]^{d-1}$ and $\symbf{\beta}_{\mid i} := \left\{\beta_{j \mid i} : j\in\{1,\dots,d\}\setminus i \right\} \in [-\infty, 1]^{d-1}$ such that for fixed $\symbf{z}_{\mid i} \in \mathbb{R}^{d-1}$ and $y_i > 0$,
% With all operations below taken component-wise, the CE model assumes that there exist parameter vectors $\symbf{\alpha}_{\mid i} \coloneqq \left\{\alpha_{j \mid i} : j\in\{1,\dots,d\}\setminus i \right\} \in [-1, 1]^{d-1}$ and $\symbf{\beta}_{\mid i} \coloneqq \left\{\beta_{j \mid i} : j\in\{1,\dots,d\}\setminus i \right\} \in [-\infty, 1]^{d-1}$ such that for fixed $\symbf{z} \in \mathbb{R}^{d-1}$ and % $y_i > 0$, such that for fixed $\symbf{z} \in \mathbb{R}^{(d-1)}$ and $y > 0$,
With all operations below taken component-wise, \citet{Heffernan2004} assume that there exist parameter vectors $\symbf{\alpha}_{\mid i} \coloneqq \left\{\alpha_{j \mid i} : j\in\{1,\dots,d\}\setminus i \right\} \in [-1, 1]^{d-1}$ and $\symbf{\beta}_{\mid i} \coloneqq \left\{\beta_{j \mid i} : j\in\{1,\dots,d\}\setminus i \right\} \in (-\infty, 1]^{d-1}$ such that, for fixed $\symbf{z} \in \mathbb{R}^{d-1}$, % $y_i > 0$, such that for fixed $\symbf{z} \in \mathbb{R}^{(d-1)}$ and $y > 0$,
% \todo{Check if probably is for equal to yi or greater than yi}
\begin{equation}~\label{eq:ce_limiting_dist}
  % \mathbb{P}\left(\frac{\symbf{Y}_{-i} - \symbf{\alpha}_{\mid i}^{(\CE{})} Y_{i}}{Y_{i}^{\symbf{\beta}_{\mid i}^{(\CE{})}} \le \symbf{z}_{\mid i}, Y_i - u_i > y_i \mid Y_i > u_i \right) \longrightarrow \symbf{G}_{\mid i}(\symbf{z}_{\mid i}) \exp(-y_i) \text{ as } u_i \rightarrow \infty,
  \mathbb{P}\!\left(
  % \frac{\symbf{Y}_{-i} - \symbf{\alpha}_{\mid i}^{(\CE{})} Y_{i}}
  % {Y_{i}^{\symbf{\beta}_{\mid i}^{(\CE{})}}}
  % \frac{\symbf{Y}_{-i} - \symbf{\alpha}_{\mid i} Y_{i}}
  % {Y_{i}^{\symbf{\beta}_{\mid i}}}
  % \le \symbf{z}_{\mid i},\; 
  % Y_i - u_i > y_i \;\Big|\; Y_i > u_i
  % \right)
  \frac{\symbf{Y}_{-i} - \symbf{\alpha}_{\mid i} Y_{i}}
  {Y_{i}^{\symbf{\beta}_{\mid i}}}
  \le \symbf{z},\; 
  Y_i - u > y \;\Big|\; Y_i > u
  \right)
  \longrightarrow
  % \symbf{G}_{\mid i}(\symbf{z}_{\mid i})\,e^{-y_i}
  % \symbf{G}_{\mid i}(\symbf{z}_{\mid i})\,\exp{-y_i}
  % \symbf{G}_{\mid i}(\symbf{z})\,\exp{(-y_i)}
  \symbf{G}_{\mid i}(\symbf{z})\,\exp{(-y)}
  % \quad\text{as } u_i \to \infty.
  \quad\text{as } u \to \infty,
\end{equation}
% \todo{i subscript removed from LHS, but not on RHS, is this ok?}
% where $u_i$ is a suitably high threshold for $Y_i$, and $\symbf{G}_{j \mid i}$ is non-degenerate for all $j \ne i$. 
% \todo{below points needed, or can some of this be removed?}
% To ensure $\symbf{G}_{\mid i}$ is well-defined, we have the constraint that 
% \[
%   \lim_{z \rightarrow \infty}{\symbf{G}_{j \mid i}(\symbf{z})} = 1, \forall j \ne i,
% \]
% ensuring that there is no mass at $+\infty$ for any margin, but allowing for some mass to be at $-\infty$, in order to ensure uniqueness of the solution \citep{Keef2013}.
% where $u_i$ is a suitably high threshold. 
% where the distribution function $\symbf{G}_{\mid i}$ is non-degenerate on $\mathbb{R}^{d-1}$ and, for each margin $j\neq i$, $\lim\limits_{z\to\infty}\symbf{G}_{j\mid i}(z)=1$, so that there is no mass at $+\infty$, while mass at $-\infty$ is permitted to ensure uniqueness \citep{Keef2013}. 
where the distribution function $\symbf{G}_{\mid i}\left(\symbf{z}\right)$ is non-degenerate on $\symbf{z} \in \mathbb{R}^{d-1}$ and, for each margin $j\neq i$, $\lim\limits_{z\to\infty}\symbf{G}_{j\mid i}(z)=1$, so that there is no mass at $+\infty$, while mass at $-\infty$ is permitted to ensure uniqueness \citep{Keef2013}. 
% An important consequence of this formulation is the weak assumption that
% \begin{equation}~\label{eq:limit_independence}
%   \mathbb{P}(\symbf{Z}_{\mid i} \le \symbf{z}_{\mid i}, Y_i - u_i = y_i \mid Y_i > u_i) \rightarrow \symbf{G}_{\mid i}(\symbf{z}_{\mid i}) \exp(-y_i) \text{ as } u_i \rightarrow \infty,
% \end{equation}
% so that in the limit as $u_i \to \infty$, the extremal exceedances $Y_i - u_i$ and the standardised residuals $\symbf{Z}_{\mid i} = \frac{\symbf{Y}_{-i} - \symbf{\alpha}_{\mid i} Y_{i}}{Y_{i}^{\symbf{\beta}_{\mid i}}}, \symbf{Z}_{\mid i} \sim \symbf{G}_{\mid i}$ are independent.
% Equation~\eqref{eq:ce_limiting_dist} implies that, in the limit as $u\to\infty$, the excesses $Y_i-u$ and the standardised residuals $\symbf{Z}_{\mid i} \coloneqq \frac{\symbf{Y}_{-i} - \symbf{\alpha}_{\mid i} Y_{i}}{Y_{i}^{\symbf{\beta}_{\mid i}}} \sim \symbf{G}_{\mid i}$ are independent. 
Equation~\eqref{eq:ce_limiting_dist} implies that, in the limit as $u\to\infty$, the excesses $Y_i-u$ and the standardised residuals 
% $\symbf{Z}_{\mid i} \coloneqq \frac{\symbf{Y}_{-i} - \symbf{\alpha}_{\mid i} Y_{i}}{Y_{i}^{\symbf{\beta}_{\mid i}}} \sim \symbf{G}_{\mid i}$ are independent. 
$\symbf{Z}_{\mid i} \coloneqq Y_i^{-\symbf{\beta}_{\mid i}}\left(\symbf{Y}_{-i} - \symbf{\alpha}_{\mid i} Y_{i}\right)$ are independent
% Equation~\eqref{eq:ce_limiting_dist} implies that, in the limit $u_i\to\infty$, the exceedances $Y_i-u_i$ and the standardised residuals $\symbf{Z}_{\mid i} = \frac{\symbf{Y}_{-i} - \symbf{\alpha}_{\mid i}^{(\CE{})} Y_{i}}{Y_{i}^{\symbf{\beta}_{\mid i}^{(\CE{})}}}$, where $\symbf{Z}_{\mid i} \sim \symbf{G}_{\mid i}$, are asymptotically independent.

% heteroscedastic regression
% The limiting distribution in equation~\eqref{eq:ce_limiting_dist} can be written as a heteroscedastic regression equation defined by
Assuming that the limit in Equation~\eqref{eq:ce_limiting_dist} holds exactly for a large threshold $u_i > 0$ gives the heteroscedastic regression model
% \todo{Should we be explicit that the equals here really should be an equality in distribution?}
\begin{align}~\label{eq:ce_model}
  % \bigl(\symbf{Y}_{-i} \mid Y_i = y_i \bigr) = \symbf{\alpha}_{\mid i}^{(\CE{})}\symbf{y}_i + \symbf{y}_i^{\symbf{\beta}_{\mid i}^{(\CE{})}}\symbf{Z}_{\mid i}, \text{ for } y_i > u_{_i},
  % \bigl(\symbf{Y}_{-i} \mid Y_i = y_i \bigr) = \symbf{\alpha}_{\mid i}\symbf{y}_i + \symbf{y}_i^{\symbf{\beta}_{\mid i}}\symbf{Z}_{\mid i}, \text{ for } y_i > u_{_i},
  % \bigl(\symbf{Y}_{-i} \mid Y_i = y \bigr) &= \symbf{\alpha}_{\mid i}y + y^{\symbf{\beta}_{\mid i}}\symbf{Z}_{\mid i}, \text{ for } y > u_i, \\
  \bigl(\symbf{Y}_{-i} \mid Y_i = y \bigr) &= \symbf{\alpha}_{\mid i}y + y^{\symbf{\beta}_{\mid i}}\symbf{Z}_{\mid i},\qquad \text{ for } y > u_i,
\end{align}
with residual vector $\symbf{Z}_{\mid i} \sim \symbf{G}_{\mid i}$.
% The slope or linear regression coefficient $\symbf{\alpha}_{\mid i}^{(\CE{})} \in [-1, 1]$ describes the strength of association between $Y_{-i}$ and large $Y_i$ above the threshold $u_i$.
% The vector $\symbf{\alpha}_{\mid i} \in [-1, 1]^{d-1}$ describes the strength of extremal association between components of $\symbf{Y}_{-i}$ and the conditioning variable $Y_i$. 
The parameter vector $\symbf{\alpha}_{\mid i}$ describes the strength of extremal association between components of $\symbf{Y}_{-i}$ and the conditioning variable $Y_i$. 
% \item $\symbf{\beta}_{\mid i} \in [-\infty, 1]$ can be interpretated as a ``spread'' parameter, controlling the levels of heteroscedasticity in the model.
% The levels of heteroscedasticity in the model is controlled by $\symbf{\beta}_{\mid i}^{(\CE{})} \in [-\infty, 1]$, and can be loosely interpreted as a ``spread'' parameter.
% The degree of heteroscedasticity in the model is controlled by entries to $\symbf{\beta}_{\mid i} \in [-\infty, 1]^{d - 1}$, and can be loosely interpreted as a ``spread'' parameter.
% The degree of heteroscedasticity in the model is controlled by entries to $\symbf{\beta}_{\mid i}$, and can be loosely interpreted as a ``spread'' parameter.
The degree of heteroscedasticity in the model is controlled by entries to $\symbf{\beta}_{\mid i}$, and can be loosely interpreted as a parameter that determines the degree of spread induced by large values of $Y_i$.
  % \item Several choices for $\symbf{G}_{\mid i}$, the distribution of
% $\symbf{Z}_{\mid i}$, were proposed in \citet{Heffernan2004}, before ultimately
% settling on a multivariate normal distribution with vector nuisance parameters
% $\symbf{\mu}_{\mid i} \in \mathbb{R}^{(d-1)}$ and $\Sigma_{\mid i} \in \mathbb{R}^{(d-1)}$.
% The distribution of $\symbf{Z}_{\mid i}$, the residuals of the model, is assumed
% to be multivariate normal with vector nuisance parameters $\symbf{\mu}_{\mid i}
% \in \mathbb{R}^{(d-1)}$ and $\Sigma_{\mid i} \in \mathbb{R}^{(d-1)}$.
% \todo{Jordan says that below isn't quite right, focus on components!}
% Some special cases of the CE model are:
% \begin{itemize}
%   \item $\symbf{\alpha}_{\mid i} = 0, \symbf{\beta}_{\mid i} = 0$, for which we have perfect independence between $Y_i$ and $Y_{-i}$.
%   \item $\symbf{\alpha}_{\mid i} = 0, \symbf{\beta}_{\mid i} = 0$, for which we have perfect independence between $Y_i$ and $Y_{-i}$.
%   \item $\symbf{\alpha}_{\mid i} = -1(+1), \symbf{\beta}_{\mid i} = 0$, which corresponds to perfect positive/negative dependence, 
%   \item $-1 < \symbf{\alpha}_{\mid i} < 1$, which suggests asymptotic independence between $Y_{-i}$ and large $Y_{i}$.
% \end{itemize}
% The values $\symbf{\alpha}_{j \mid i}$ and $\symbf{\beta}_{j \mid i}$ for $j \ne i$ are the components of the vectors $\symbf{\alpha}_{\mid i}$ and $\symbf{\beta}_{\mid i}$ corresponding to the variable $Y_j$, $j \in \{1,\ldots,d\}\setminus\{i\}$. % Defined so next bit makes sense
Some special cases (for $j\in\{1,\ldots,d\}\setminus i$) of the CE model are:
% \begin{itemize}
%   \item $\symbf{\alpha}_{j \mid i} = 0$ and  $\symbf{\beta}_{j \mid i} = 0$, for which we have perfect independence, between $Y_j$ and $Y_i \mid Y_i > u_i, j \ne i$, corresponding to $\chi= 0$;
%   \item $\symbf{\alpha}_{j \mid i} = 1(-1)$ and $\symbf{\beta}_{j \mid i} = 0$, which corresponds to positive (negative) asymptotic dependence, equivalent to having $\chi = 1 (\chi = 0)$, and 
%   \item $-1 < \symbf{\alpha}_{j \mid i} < 1$, which suggests asymptotic independence between $Y_j$ and $Y_i$, with dependence weakening as $Y_i \to \infty$, equivalent to $\chi < 1$. 
% \end{itemize}
% $\alpha_{j\mid i}=0$ and $\beta_{j\mid i}=0$, for which we have perfect independence between $Y_j$ and $Y_i\mid Y_i>u_i$, corresponding to $\chi=0$;
$\alpha_{j\mid i}=0$ and $\beta_{j\mid i}=0$, which implies perfect independence between $Y_j$ and $Y_i\mid Y_i>u_i$, corresponding to $\chi=0$;
$\alpha_{j\mid i}=1\ ( -1)$ and $\beta_{j\mid i}=0$, which corresponds to positive (negative) asymptotic dependence, equivalent to $\chi=1$ ($\chi=0$); and
% $-1<\alpha_{j\mid i}<1$, which suggests asymptotic independence between $Y_j$ and $Y_i$, with dependence weakening as $Y_i\to\infty$, equivalent to $\chi<1$.
$-1<\alpha_{j\mid i}<1$, which suggests asymptotic independence between $Y_j$ and $Y_i$, with the strength of extremal dependence weakening with $\alpha_{j \mid i}$, equivalent to $\chi<1$.

% We fit the CE model at different spatial sites. 
In our motivating spatial environmental application, we consider a CE model at different spatial sites and across conditioning variables $i = 1, \ldots, d$.
% To allow for spatial variation, we may index by site $s \in \mathcal{S}$, and allow regression parameters to vary by site. 
% To denote spatial variation, we index the random vectors by spatial location $\mathcal{s} \in \left(s_1, \ldots, s_D\right)$, where $s_i \in \mathbb{R}^2, i = 1, \ldots, D$, and $D$ is the number of spatial locations. 
To describe spatial variation, we add the additional subscript $s \in \{1, \ldots, D\}$, where $D$ is the number of spatial locations, so that $Y_{i,s}$ denotes the variable $Y_i$ observed at site $s$.
% This leads to the spatially indexed extension of Equation~\eqref{eq:ce_model}, where we have the site-specific random vector $\symbf{Y}(s) \in \mathbb{R}^d$ for $\mathcal{s} \in (s_1, \ldots, s_d)$, and
% \begin{equation} \label{eq:ce_model_spat}
%   \bigl(\symbf{Y}_{-i}(s) \mid Y_i(s) = y\bigr) 
%     = \symbf{\alpha}_{\mid i}(s) \, y 
%       + y^{\symbf{\beta}_{\mid i}(s)} \, \symbf{Z}_{\mid i}(s), 
%       \quad y > u_i,
% \end{equation}
% This leads to a spatial extension of Equation~\eqref{eq:ce_model}, where we have the site-specific random vector $\symbf{Y}_{s} \in \mathbb{R}^d$ for $s \in \mathcal{S}$, and
% This leads to a spatial extension of Equation~\eqref{eq:ce_model}, with site-specific random vector $\symbf{Y}_{s} \in \mathbb{R}^d$, and 
This leads to a spatial extension of Equation~\eqref{eq:ce_model}, with site-specific random vector $\symbf{Y}_{s} \coloneq \left(Y_{1,s}, \ldots, Y_{d,s}\right) \in \mathbb{R}^d$, and, for $i = 1, \ldots, d$,
\begin{equation} \label{eq:ce_model_spat}
  \bigl(\symbf{Y}_{-i, s} = y \mid Y_{i, s} = y\bigr) 
    = \symbf{\alpha}_{\mid i, s} \, y 
      + y^{\symbf{\beta}_{\mid i, s}} \, \symbf{Z}_{\mid i, s}, 
      \quad y > u_{i, s}, 
\end{equation}
% with location-specific residuals $\symbf{Z}_{\mid i}(s) \sim \symbf{G}_{\mid i}(s)$. \todo{Do I need to define alpha(s), beta(s), ui(s) again?}
% where $\symbf{Y}_{-i}(s) \coloneqq \symbf{Y}(s) \setminus Y_i(s), \symbf{\alpha}_{\mid i}(s), \symbf{\beta}_{\mid i}(s)$ and $\symbf{Z}_{\mid i}(s) \sim \symbf{G}_{\mid i}(\cdot ; s)$ are the site-specific counterparts of the regression parameters and residuals introduced above.  
where $\symbf{Y}_{-i, s} \coloneqq \symbf{Y}_{s} \setminus Y_{i, s}$, and $\symbf{\alpha}_{\mid i, s}$, $\symbf{\beta}_{\mid i, s}$ $\symbf{Z}_{\mid i, s} \sim \symbf{G}_{\mid i, s}$ and $u_{i, s}$ are the site-specific counterparts of the regression parameters, residuals and exceedance threshold introduced above in Equations~\eqref{eq:ce_limiting_dist} and~\eqref{eq:ce_model}.
Note that we make no assumptions about spatial smoothness in the regression model. 

% Intro: CE gives distributions, which need to be compared
% Definition of Z moved here to be impactful than after eq:ce_model
% The residual process $\symbf{Z}_{\mid i}$ in equation~\eqref{eq:ce_model} is
% assumed to follow a multivariate Gaussian distribution with vector nuisance
% parameters $\symbf{\mu}_{\mid i} \in \mathbb{R}^{(d-1)}$ and $\Sigma_{\mid i} \in \mathbb{R}^{(d-1)}$.
% The residual vector $\symbf{Z}_{\mid i}$ in Equation~\eqref{eq:ce_model} is assumed to follow a multivariate Gaussian distribution with mean vector $\symbf{\mu}_{\mid i}\in\mathbb{R}^{d-1}$ and covariance matrix $\Sigma_{\mid i}\in\mathbb{R}^{(d-1)\times(d-1)}$. 
% \todo{Use spatial index here, or first explain without and then add in at the end?}
% The residual vector $\symbf{Z}_{\mid i}(s)$ in Equation~\eqref{eq:ce_model_spat} is assumed to follow a multivariate Gaussian distribution with mean vector $\symbf{\mu}_{\mid i}(s)\in\mathbb{R}^{d-1}$ and covariance matrix $\Sigma_{\mid i}(s)\in\mathbb{R}^{(d-1)\times(d-1)}$. 
% As a working assumption that permits inference on $\symbf{\alpha}_{\mid i}(s)$ and $\symbf{\beta}_{\mid i}(s)$, the residual vector $\symbf{Z}_{\mid i}(s)$ in Equation~\eqref{eq:ce_model_spat} is assumed to be multivariate normally distributed with mean vector $\symbf{\mu}_{\mid i}(s)\in\mathbb{R}^{d-1}$ and covariance matrix $\Sigma_{\mid i}(s)\in\mathbb{R}^{(d-1)\times(d-1)}$.
% As a working assumption that permits inference on $\symbf{\alpha}_{\mid i, s}$ and $\symbf{\beta}_{\mid i, s}$, the residual vector $\symbf{Z}_{\mid i, s}$ in Equation~\eqref{eq:ce_model_spat} is assumed to be multivariate normally distributed with mean vector $\symbf{\mu}_{\mid i, s} \in\mathbb{R}^{d-1}$ and covariance matrix $\Sigma_{\mid i, s}\in\mathbb{R}^{(d-1)\times(d-1)}$.
As a working assumption that permits inference on $\symbf{\alpha}_{\mid i, s}$ and $\symbf{\beta}_{\mid i, s}$, we follow \citet{Heffernan2004} and assume that the residual vector $\symbf{Z}_{\mid i, s}$ in Equation~\eqref{eq:ce_model_spat} is multivariate Gaussian with mean vector $\symbf{\mu}_{\mid i, s} \in\mathbb{R}^{d-1}$ and covariance matrix $\Sigma_{\mid i, s}\in\mathbb{R}^{(d-1)\times(d-1)}$ for $i = 1, \ldots, d$ and $s = 1, \ldots, D$.
% \todo{Use (CE) superscript for mu and Sigma to distinguish from clustering}
% The residual vector $\symbf{Z}_{\mid i}$ in Equation~\eqref{eq:ce_model} is assumed to follow a multivariate Gaussian distribution with mean vector $\symbf{\mu}_{\mid i}^{(\CE{})}\in\mathbb{R}^{d-1}$ and covariance matrix $\Sigma_{\mid i}^{(\CE{})} \in\mathbb{R}^{(d-1)\times(d-1)}$. 
% \todo{Heff04 assumes diagonal sigma, should we say we use full covariance?}
% This is a working assumption proposed in \citet{Heffernan2004} after testing several candidate distributions. %, and is still most commonly used in the literature \todo{citations?}
% This is the key assumption of the CE model on which we derive our divergence measure, and was chosen in \citet{Heffernan2004} after testing several candidate distributions.
% This is the key aspect of the CE model with which we derive our divergence measure, and is a working assumption chosen in \citet{Heffernan2004} after testing several candidate distributions.
% This Gaussian working assumption, proposed in \citet{Heffernan2004} after testing several candidate distributions, is the key property we exploit when deriving a closed-form divergence measure for computationally efficient clustering.
% This Gaussian working assumption, proposed in \citet{Heffernan2004}, is the key property we exploit when deriving a closed-form divergence measure for computationally efficient clustering; see Section~\ref{subsec:div}.
This working assumption is the key to deriving a closed-form divergence measure for computationally efficient clustering; see Section~\ref{subsec:div}.
% Inference (doesn't need new paragraph)
% \citet{Heffernan2004} shows that inference can be made about the extremal dependence of a random vector $\symbf{Y}_{-i} = \symbf{Y} \backslash Y_i$ on $Y_i$ by conditioning on the exceedance of a high threshold $u$ by $Y_i$, and assuming that this conditional distribution is a diagonal multivariate Gaussian distribution: % with vector mean and standard deviation:
% An important consequence following from this assumption is that inference can be made about the extremal dependence of a random vector $\symbf{Y}_{-i} = \symbf{Y} \setminus \{Y_i\}$ on $Y_i$ by conditioning on the exceedance of a high threshold $u_i$ by $Y_i$, and assuming that this conditional distribution is a multivariate Gaussian distribution, i.e.\ 
% Consequently, inference can be made about the extremal dependence of a random vector $\symbf{Y}(s)$ at site $s$ by conditioning on the exceedance of a high threshold $u_i$ by $Y_i(s)$, and assuming that this conditional distribution is multivariate Gaussian, i.e.,\@
% Consequently, inference on $\alpha_{\mid i}$ and $\beta_{\mid i}$ by conditioning on the exceedance of a high threshold $u_i$ by $Y_{i, s}$, and assuming that this conditional distribution is multivariate Gaussian, i.e.,\@
% Consequently, inference on $\alpha_{\mid i}$ and $\beta_{\mid i}$ by conditioning on the exceedance of a high threshold $u_i$ by $Y_{i, s}$.
It follows that 
\begin{equation}~\label{eq:ce_norm}
  % \bigl( \symbf{Y}_{-i} \mid Y_i  = y_i \bigr) \sim N\left(\symbf{\alpha}_{\mid i}^{(\CE{})} y_i + y_i^{\symbf{\beta}_{\mid i}^{(\CE{})}} \symbf{\mu}_{\mid i}^{(\CE{})}, y_i^{\symbf{\beta}_{\mid i}^{(\CE{})}} \Sigma_{\mid i}^{(\CE{})}\right), Y_i > u_i,
  % \bigl( \symbf{Y}_{-i} \mid Y_i  = y_i \bigr) \sim N\left(\symbf{\alpha}_{\mid i} y_i + y_i^{\symbf{\beta}_{\mid i}} \symbf{\mu}_{\mid i}, y_i^{\symbf{\beta}_{\mid i}} \symbf{\Sigma_{\mid i}}\right), Y_i > u_i,
  % \bigl( \symbf{Y}_{-i}(s) \mid Y_i(s)  = y) \sim \mbox{MVN}_{d-1}\left(\symbf{\alpha}_{\mid i}(s) y + y^{\symbf{\beta}_{\mid i}(s)} \symbf{\mu}_{\mid i}(s), y^{\symbf{\beta}_{\mid i}(s)} \Sigma_{\mid i}(s)\right), y > u_i(s),
  % \bigl( \symbf{Y}_{-i}(s) \mid Y_i(s)  = y) \sim \mbox{MVN}_{d-1}\left(\symbf{\alpha}_{\mid i}(s) y + y^{\symbf{\beta}_{\mid i}(s)} \symbf{\mu}_{\mid i}(s), \left(y^{\beta_{\mid i}(s)}\right)^{\mathrm{T}} \Sigma_{\mid i}(s) y^{\beta_{\mid i}(s)} \right), y > u_i(s),
  % \bigl( \symbf{Y}_{-i}(s) \mid Y_i(s)  = y) \sim \mbox{MVN}_{d-1}\left(\mvmean{s}, \mvcov{s}\right), y > u_i,
  \bigl(\symbf{Y}_{-i, s} \mid Y_{i, s} = y\bigr) \sim \mbox{MVN}_{d-1}\left(\mvmean{s}, \mvcov{s}\right), y > u_{i, s},
\end{equation}
% where $\symbf{\alpha}_{\mid i, s}, \symbf{\beta}_{\mid i, s}, \symbf{\mu}_{\mid i, s}, \Sigma_{\mid i, s}$ are the site-specific parameters of the CE model for the $i^{\text{th}}$ conditioning variable, $i = 1, \ldots, d$. \todo{Already defined alpha, beta, i above?} \todo{is u i correct? Should it be u i s?}
where $\symbf{\alpha}_{\mid i, s}, \symbf{\beta}_{\mid i, s}, \symbf{\mu}_{\mid i, s}, \Sigma_{\mid i, s}$ are the site-specific parameters for the $i^{\text{th}}$ conditioning variable, $i = 1, \ldots, d$. % \todo{Already defined alpha, beta, i above?} \todo{is u i correct? Should it be u i s?}
% where $\symbf{\alpha}_{\mid i}^{(\CE{})}, \symbf{\beta}_{\mid i}^{(\CE{})}, \symbf{\mu}_{\mid i}^{(\CE{})}, \Sigma_{\mid i}^{(\CE{})} \in \mathbb{R}^{(d-1)}$ are the parameters of the CE model. 
% Therefore, when seeking to compare the extremal dependence structure of two locations, a sensible approach is to compare the conditional distributions given by Equation~\ref{eq:ce_norm} for each variable at both sites. 
% Therefore, under this model, when comparing the extremal dependence structure between variables at any given sites, we are effectively comparing the conditional distributions given by Equation~\eqref{eq:ce_norm}.
% Therefore, under this model, when comparing the multivariate tail dependence structure at different sites, we are effectively comparing the conditional distributions given by Equation~\eqref{eq:ce_norm}.
% Therefore, to compare tail dependence at different sites, we can compare the conditional distributions in \eqref{eq:ce_norm}, i.e.\ the induced multivariate Gaussian distributions parameterised by $(\symbf{\alpha}_{\mid i}^{(\CE{})},\symbf{\beta}_{\mid i}^{(\CE{})},\symbf{\mu}_{\mid i}^{(\CE{})},\Sigma_{\mid i}^{(\CE{})})$ (or a chosen subset thereof).
% Therefore, to compare tail dependence at different sites, we can compare the conditional distributions in \eqref{eq:ce_norm}, i.e.\ the induced multivariate Gaussian distributions parameterised by $(\symbf{\alpha}_{\mid i},\symbf{\beta}_{\mid i},\symbf{\mu}_{\mid i},\Sigma_{\mid i})$ (or a chosen subset thereof).
To compare tail dependence across different sites $s$, we compare the conditional distributions in Equation~\eqref{eq:ce_norm}, i.e.,\@ the assumed multivariate Gaussian distributions parameterised by $\symbf{\alpha}_{\mid i, s},\symbf{\beta}_{\mid i, s},\symbf{\mu}_{\mid i, s}$ and $\Sigma_{\mid i, s}$. %(or a chosen subset thereof).

% Estimation
% The model parameters $\symbf{\alpha}_{\mid i, s}, \symbf{\beta}_{\mid i, s}, \symbf{\mu}_{\mid i, s}, \Sigma_{\mid i, s}$ in Equation~\eqref{eq:ce_norm} are estimated at each site $s$ and for each conditioning variable, $Y_i, i = 1, \ldots, d$, using maximum likelihood estimation. \todo{Again, i already defined above?}
% The model parameters $\symbf{\alpha}_{\mid i, s}, \symbf{\beta}_{\mid i, s}, \symbf{\mu}_{\mid i, s}, \Sigma_{\mid i, s}$ in Equation~\eqref{eq:ce_norm} are estimated at each site-specific conditioning variable, $Y_{i, s}, i = 1, \ldots, d, s = 1, \ldots, D$, using maximum likelihood estimation. \todo{Again, i and s already defined above?}
We estimate parameters $\symbf{\alpha}_{\mid i, s}, \symbf{\beta}_{\mid i, s}, \symbf{\mu}_{\mid i, s}, \Sigma_{\mid i, s}$ for each site $s = 1, \ldots, D$ and conditioning 
variable $i = 1, \ldots, d$, using maximum likelihood estimation. \todo{Again, i and s already defined above?}
% In practice, for each $i = 1, \ldots, d$ and $s \in (s_1, \ldots, s_D)$, we set the threshold $u_i(s)$ to the $q^{\text{th}}$ Laplace quantile.  
% In practice, for each $i$ and $s$, we set the threshold $u_{i, s}$ to the $q^{\text{th}}$ Laplace quantile of $Y_{i, s}$. \todo{Need to use th superscript throughout if keeping}
% In practice, we set the threshold $u_{i, s} \coloneqq u$ for all $i$ and $s$, to the $q^{\text{th}}$ standard Laplace quantile. \todo{Need to use th superscript throughout if keeping}
In practice, we set the threshold $u_{i, s} \coloneqq u_i$ to the $q^{\text{th}}$ standard Laplace quantile for all $s$, as this is required to build a symmetric divergence measure for clustering (see Section~\ref{subsec:div}).
% We choose $q$ such that there are a sufficient number of exceedances above $u_{i, s}$ to fit the CE model, while keeping $q$ high enough so that the limiting assumption in Equation~\eqref{eq:ce_limiting_dist} is reasonable.
Choosing $q$ involves the classical bias-variance tradeoff: $q$ must be sufficiently low as to leave enough excesses above $u_i$ for reliable estimation, but high enough to reduce bias induced when the asymptotic arguments in Equation~\ref{eq:ce_limiting_dist} are not satisfied. 
To pick $q$, we use threshold stability plots \citep{Scarrott2012}. 
% The bootstrapping procedure of \citet{Heffernan2004} is used to obtain standard errors for the parameter estimates, and we choose the highest value of $q$ such that the parameter estimates are stable, and the bootstrap standard errors are reasonably small.
With the bootstrap procedure of \citet{Heffernan2004}, we obtain standard errors for parameter estimates and choose the highest value of $q$ such that the parameter estimates are stable and the bootstrap standard errors are reasonably small. \todo{Briefly describe this bootstrap method?}

\subsection{A Divergence measure for multivariate conditional extremes}~\label{subsec:div}

% \todo{Need some references for applications of this metric! In intro, or here?}
We now define our divergence measure for multivariate conditional extremes models. 
There are several qualities we desire from a divergence measure based on the conditional distribution in Equation~\eqref{eq:ce_norm}.
% We would like to exploit the fact that the CE model is a multivariate Gaussian distribution to provide some gains in computational efficiency, and for our divergence to more generally be cheap to compute.
% We particularly want to exploit the multivariate Gaussian nature of the CE model, to provide computational efficiency through a closed-form solution, and for our divergence to be generally cheap to compute.
% The measure must be non-negative and symmetric, as the ``distance'' between two sites as defined by the divergence between their CE models should be the same regardless of the order in which they are compared.
The measure should be non-negative and symmetric, as the ``distance'' between the tails of random vectors, as defined by the divergence between their corresponding CE models, so that it can be interpreted as a distance and be the same regardless of the order in which they are compared \citep{Vijendra2015}.
% Ideally, our measure should also be bounded above by a finite value, as smaller divergences lead to more well-defined clusters, and very large divergences can lead to numerical instability.
% Ideally, our measure should also have some form of upper bound, as smaller divergences lead to more well-defined clusters, and very large divergences can lead to numerical instability. \todo{Citation!}
Ideally, our measure should also have an upper bound, as large divergences can lead to numerical instability during clustering \citep{Thiagarajan2025}. % , and less stable and well-defined clusters \citep{Thiagarajan2025}. 
% Finally, we would like our measure to be a true metric, so that it satisfies the triangle inequality and is non-negative, and this is known to improve the performance of clustering algorithms \todo{Again, citation?}.
We also desire provide computational efficiency through a closed-form solution our divergence, which is cheap to compute. 
We achieve this using the Gaussian assumption in Equation~\eqref{eq:ce_norm}.
% \todo{Any other properties? Convex (as KL is?) (which may be better?) (need citation)}

% Introduce JSGa
% One divergence measure introduced in \citet{Nielsen2019} which satisfies these properties is the skew-geometric Jensen--Shannon divergence ($\JSGa{}$), which is a generalisation of the Jensen--Shannon ($\JS{}$) divergence \citep{Lin1991}. 
\citet{Nielsen2019} introduced the skew-geometric Jensen--Shannon divergence ($\JSGa{}$), which is a generalisation of the Jensen--Shannon ($\JS{}$) divergence \citep{Lin1991}. 
\todo{Switch to F and F star to match below?}
\todo{Say why Deasy uses dual form? or that it's the same?}
Specifically, we use the dual form of the $\JSGa{}$ divergence \citep{Deasy2020}, which is defined between two continuous $d$-variate distribution functions, say $H_1$ and $H_2$, as 
\begin{equation}~\label{eq:jsga}
  % \JSGa{(P \Vert Q)} = \frac{1}{2} \left\{\KL{(P \Vert G_{\alpha}(P, Q))} + \KL{(Q \Vert G_{\alpha}(P, Q))} \right\},
  % \JSGa{(F_1 \Vert F_2)} = \frac{1}{2} \left\{\KL{(F_1 \Vert G_{\lambda}(F_1, F_2))} + \KL{(F_2 \Vert G_{\lambda}(F_1, F_2))} \right\},
  % dual-version
  \JSGa{(H_1 \Vert H_2)} = (1- \lambda) \KL{\left\{G_{\lambda}(H_1, H_2) \Vert H_1\right\}} + \lambda \KL{\left\{G_{\lambda}(H_1, H_2) \Vert H_2\right\}},
\end{equation}
% where $G_{\alpha}(P, Q) = P^{\alpha} Q^{1 - \alpha}, \alpha \in [0, 1]$ is the weighted geometric mean of $P$ and $Q$, with $\alpha$ controlling the weighting between them, and $\KL{(P \Vert Q)}$ is the Kullback--Leibler divergence between $P$ and $Q$ \citep{Kullback1951}. 
% where $G_{\lambda}(H_1, H_2) = H_{1}^{1-\lambda} H_{2}^{\lambda}, \lambda \in [0, 1]$, is the weighted geometric mean of $H_1$ and $H_2$, and $\KL{(H_1 \Vert H_2)}$ is the Kullback--Leibler divergence between $H_1$ and $H_2$ \citep{Kullback1951}, given by 
where $G_{\lambda}(H_1, H_2) = H_{1}^{1-\lambda} H_{2}^{\lambda}$ is the weighted geometric mean of $H_1$ and $H_2$, with $\lambda \in [0, 1]$, and $\KL{(H_1 \Vert H_2)}$ is the Kullback--Leibler divergence between $H_1$ and $H_2$ \citep{Kullback1951}, given by 
\begin{equation}~\label{eq:kl}
  % KL(F_1 \Vert F_2) = \int_{-\infty}^{\infty} f_1(y) \log\left(\frac{f_1(y)}{f_2(y)}\right) dy,
  \KL{(H_1 \Vert H_2)} = \int_{\mathbb{R}^d} h_1(\symbf{y}) \log\left(\frac{h_1(\symbf{y})}{h_2(\symbf{y})}\right) d\symbf{y},
\end{equation}
where $h_1$ and $h_2$ are the continuous density functions of $H_1$ and $H_2$, respectively. 
% Here, $P$ and $Q$ represent the conditional multivariate Gaussian distributions induced by the CE model at two different sites.
% In our context, $H_1$ and $H_2$ will represent the conditional multivariate Gaussian distributions induced by the CE model at two different sites, say $s_1$ and $s_2$. \todo{Change to s and s star to match below?}
In our context, we replace $H_1$ and $H_2$ by the conditional multivariate Gaussian distributions induced by the CE framework at two different sites, say $s$ and $s^*$, see Equation~\eqref{eq:ce_norm}.
% The parameter $\lambda$ skews the geometric mean $G_{\lambda}(H_1,H_2)\propto H_1^{1-\lambda}H_{2}^{\lambda}$ towards $H_1$ (when $\lambda$ is close to 0) or towards $H_2$ (when $\lambda$ is close to 1). 
% The weight $\lambda$ skews the geometric mean $G_{\lambda}(H_1,H_2) = H_1^{1-\lambda}H_{2}^{\lambda}$ towards $H_1$ (when $\lambda$ is close to 0) or towards $H_2$ (when $\lambda$ is close to 1). 
The weight $\lambda$ skews the geometric mean $G_{\lambda}(H_1,H_2) = H_1^{1-\lambda}H_{2}^{\lambda}$ towards $H_1$ as $\lambda$ goes to 0 (and conversely for $H_2$ as $\lambda \rightarrow 1$). 
% When one site has a more reliable CE fit, we set $\alpha$ to a reliability weight $w\in[0,1]$ (e.g.\ proportional to effective sample size), and apply the symmetrised divergence so that the final dissimilarity remains symmetric.
% One possible advantage for having a skew parameter such as $\lambda$ is that in our divergence measure we can favour sites with a more reliable CE fit, as determined by, for example, the effective sample size.
One possible advantage of specifying $\lambda$ is that we can design our divergence measure to place more weight on the CE model at sites with more reliable fit, as determined by, for example, the effective sample size.
In this paper, we set $\lambda = 0.5$ throughout, as all sites have equal number of observations in both our simulation and case studies.
% As such, for the sake of brevity we refer to $\JSGa{}(H_1 \Vert H_2)$ as simply $
\todo{Refer to JSGa as GJS from now on, since lambda is fixed?}
% For our application, we set $\alpha = 0.5$, so that $G_{\alpha}(P, Q)$ is unweighted, but note that $\alpha$ 
% The Kullback--Leibler (KL) divergence, $KL(P \Vert Q)$, is defined as
% \begin{equation}~\label{eq:kl}
%   KL(P \Vert Q) = \int_{-\infty}^{\infty} p(y) \log\left(\frac{p(y)}{q(y)}\right) dy.
% \end{equation}
% The $\JSGa{}$ divergence is derived from the popular Kullback--Leibler ($KL$) divergence \citep{Killback1951}. 

% Explain why it is a good dissimilarity metric to use
For two multivariate Gaussian distributions, the $\KL{}$ divergence in Equation~\eqref{eq:kl} has a closed-form solution \citep{Duchi2007}.
The $\JSGa{}$ divergence exploits the property that the (weighted) product of two members of the same exponential family is itself a member of the same exponential family distribution. 
% It then follows that $G_{\lambda}(H_1, H_2)$ is multivariate Gaussian, and so the $\KL{}$ divergences in Equation~\eqref{eq:jsga} are between multivariate Gaussian distributions and can be computed in closed-form.
It then follows that $G_{\lambda}(H_1, H_2)$ is multivariate Gaussian, and so the $\KL{}$ divergences in Equation~\eqref{eq:jsga} are evaluated on multivariate Gaussian distributions and can thus have in closed-form.
Hence, the principle advantage of the $\JSGa{}$ is that it has a closed-form solution for two multivariate Gaussian distributions, and so is computationally efficient to compute \citep{Nielsen2019}, which has led to its use in several applications, including clustering, deep learning, variational inference, and remote sensing \citep{Nielsen2019, Deasy2020, Ni2023-cd, Thiagarajan2025}.
% Furthermore, it is clearly symmetric and non-negative, as the sum of two $KL$ divergences, which is also important for clustering.
% Furthermore, as the average of two $\KL{}$ divergences, it is clearly symmetric and non-negative.
% Finally, when $\lambda = 0.5$, $\JSGa{}$ is bounded above by $\JSGa{(H_1 \Vert H_2)} \leq \frac{1}{4} \mathrm{J}(H_1 \Vert H_2)$,
% JSGa form
% For two multivariate normals, \citet{Nielsen2019} shows that the $\JSGa{}$ divergence introduced in Equation~\eqref{eq:js_norm} has the closed-form solution
% \todo{eq:js_norm can't be found}
For two generic multivariate Gaussian distributions $H \sim \mbox{MVN}(\symbf{m}, \Omega)$ and $H^* \sim \mbox{MVN}(\symbf{m}^*, \Omega^*)$ in $\mathbb{R}^d$ with mean vectors $\symbf{m}, \symbf{m}^* \in \mathbb{R}^d$ and covariance matrices $\Omega, \Omega^* \in \mathbb{R}^{d \times d}$, \citet{Nielsen2019} showed that the dual $\JSGa{}$ divergence introduced in Equation~\eqref{eq:jsga} has the closed-form solution 
\begin{align*}%~\label{eq:js_norm_mvn}
  % JS^{G_{\lambda}}(F \Vert F^*) &= \frac{1}{2} \left( (1 - k) m^T \Omega^{-1} m + k m^*^T \Omega^*^{-1} m^* - m_{\lambda}^T \Omega_{\lambda}^{-1} m_{\lambda} + \log{\left( \frac{\abs{\Omega}^{1-k} \abs{\Omega^*}^{\lambda}}{\abs{\Omega_{\lambda}}} \right)} \right), \\
  \JSGa{}(H \Vert H^*) &= \frac{1}{2} \left\{ (1-\lambda) \symbf{m}^T \Omega^{-1} \symbf{m} + \lambda \symbf{m}^*^T \Omega^*^{-1} \symbf{m}^* - \symbf{m}_{\lambda}^T \Omega_{\lambda}^{-1} \symbf{m}_{\lambda} + \log{\left( \frac{\abs{\Omega}^{1-\lambda} \abs{\Omega^*}^{\lambda}}{\abs{\Omega_{\lambda}}} \right)} \right\}, \\
  % \Omega_{\lambda} &= \left( (1 - k) \Omega^{-1} + k \Omega^*^{-1} \right)^{-1}, \\
  \Omega_{\lambda} &= \left\{ (1-\lambda) \Omega^{-1} + \lambda \Omega^*^{-1} \right\}^{-1}, \\
  % m_{\lambda} &= \Omega_{\lambda} \left( (1 - k) \Omega^{-1} m + k \Omega^*^{-1} m^* \right), \numberthis~\label{eq:js_norm_mvn1}
  \symbf{m}_{\lambda} &= \Omega_{\lambda} \left\{(1-\lambda) \Omega^{-1} \symbf{m} + \lambda \Omega^*^{-1} \symbf{m}^* \right\}, \numberthis~\label{eq:js_norm_mvn1}
\end{align*} 
% where:
% \begin{itemize}
%   \item $m_1 = (m_{1,1}, m_{1,2}, \ldots, m_{1,d})^\top$ and $m_2 = (m_{2,1}, m_{2,2}, \ldots, m_{2,d})^\top$ are the mean vectors,
%   \item $\Omega_1 = \text{diag}(\sigma_{1,1}^2, \sigma_{1,2}^2, \ldots, \sigma_{1,d}^2)$ and $\Omega_2 = \text{diag}(\sigma_{2,1}^2, \sigma_{2,2}^2, \ldots, \sigma_{2,d}^2)$ are diagonal covariance matrices, \todo{Are they diagonal??}
%   \item $\sigma_{1,i}^2$ and $\sigma_{2,i}^2$ denote the $i$-th diagonal element of $\Omega_1$ and $\Omega_2$, respectively.
% \end{itemize}
Finally, the $\JSGa{}$ is bounded above by $\JSGa{(H_1 \Vert H_2)} \leq \frac{1}{2}\max\{\lambda,\,1-\lambda\}\mathrm{J}(H_1 \Vert H_2)$, 
% \begin{equation}~\label{eq:jsga_bound}
%   \JSGa{(F_1 \Vert F_2)} \leq \frac{1}{4} \mathrm{J}(F_1 \Vert F_2),
% \end{equation}
where the Jeffreys' divergence $\mathrm{J}(H_1 \Vert H_2) = \KL{(H_1 \Vert H_2)} + \KL{(H_2 \Vert H_1)}$ is simply the sum of the forward and backward $\KL{}$ divergences \citep{Nielsen2025}. \todo{this follows short derivation from this paper, include in appendix?}
% Therefore, $\JSGa{}$ provides more well-defined clusters than the Jeffreys' divergence, motivating the higher conceptual complexity by comparison. 
% Therefore, the $\JSGa{}$ divergence is less susceptible to numerical instability than this simple summation. %, and also provides more tightly defined clusters.
% Hence, we identify the $\JSGa{}$ divergence as a suitable measure of dissimilarity for clustering CE model fits.

% We may also assume that our covariance matrices are diagonal, as is often the case in practice.
% A common simplifying assumption is to assume that the covariance matrices $\Sigma_1$ and $\Sigma_2$ are diagonal.
% In this case, \citet{Thiagarajan2025} shows that the $\JSGa{}$ divergence simplifies to
% \todo{Double check this formula is correct (R should give same output as above non-diagonal method)}
% \todo{Standardise use of brackets, varies a lot}
% % \begin{align*}
% %   JS-G(P \Vert Q) = \frac{1}{2} \sum^n_{i=1} \left[ \frac{\alpha \sigma_{1, i}^2 + (1-\alpha)\sigma_{2, i}^2}{\sigma_{\alpha, i}^2} + \log{\left( \frac{\sigma_{\alpha, i}^2}{\sigma_{1, i}^{(2\alpha)}\sigma_{2, i}^{2(1 - \alpha)}} \right)} + \alpha \frac{(\mu_{\alpha, i} - \mu_{1, i})^2}{\sigma_{\alpha, i}^2} + (1 - \alpha) \frac{(\mu_{\alpha, i} - \mu_{2, i})^2}{\sigma_{\alpha, i}^2} -1 \right],
% % \end{align*}
% % \todo{Should I use a different notation here, since I also use mu, sigma for the CE model?}
% \begin{align}~\label{eq:js_norm_mvn_diag}
% JS^{G_{\alpha}}(F_1 \Vert F_2) = \frac{1}{2} \sum_{i=1}^d \Bigg[
%     & \frac{\alpha \sigma_{1,i}^2 + (1 - \alpha) \sigma_{2,i}^2}{\sigma_{\alpha,i}^2} 
%     + \log\left( \frac{\sigma_{\alpha,i}^2}{\sigma_{1,i}^{2\alpha} \sigma_{2,i}^{2(1 - \alpha)}} \right) \nonumber \\
%     & + \alpha \frac{(\mu_{\alpha,i} - \mu_{1,i})^2}{\sigma_{\alpha,i}^2} 
%     + (1 - \alpha) \frac{(\mu_{\alpha,i} - \mu_{2,i})^2}{\sigma_{\alpha,i}^2}
%     - 1 \Bigg],
% \end{align}
% where $\sigma_{\alpha, i}^2 = \frac{\sigma_{1, i}^2 \sigma_{2, i}^2}{\alpha\sigma_{1, i}^2 + (1 - \alpha)\sigma_{2, i}^2}$, and $\mu_{\alpha, i} = \sigma_{\alpha, i}^2 \left( \frac{\alpha\mu_{1, i}}{\sigma_{1, i}^2} + \frac{(1 - \alpha) \mu_{2, i}}{\sigma_{2, i}^2} \right)$, respectively.

% Paragraph defining conditional JSGa for sites (s, s*) in cond variable
% Now suppose we have fitted the CE model at two different sites $s$ and $s^*$, and we want to compare the extremal dependence structure of the random vector $\symbf{Y}_{s}$ at site $s$ with that of $\symbf{Y}_{s^*}$ at site $s^*$, for a given conditioning variable $Y_{i, s}$ and $Y_{i, s^*}$, respectively. %, where $i \in \{1, \ldots, d\}, s, s^* \in \{1, \ldots, D\}$.
% Now suppose we have fitted the CE model at two different sites $s$ and $s^*$, and we want to compare the extremal dependence structure of the random vector $\symbf{Y}_{s}$ at site $s$ with that of $\symbf{Y}_{s^*}$ at site $s^*$, for conditioning variable $i$ (i.e.\ $Y_{i, s}$ and $Y_{i, s^*}$).%, where $i \in \{1, \ldots, d\}, s, s^* \in \{1, \ldots, D\}$.
% Now suppose we have fitted the CE model at two different sites $s$ and $s^*$, and we want to compare the extremal dependence structure for the conditioning variable $i$ (i.e.\ $Y_{i, s}$ and $Y_{i, s^*}$).%, where $i \in \{1, \ldots, d\}, s, s^* \in \{1, \ldots, D\}$.
Now, suppose that, for a specified conditioning variable $i \in \{1, \ldots, d\}$, we have fitted the CE model at two different sites, $s$ and $s^*$, and we want to compare their induced extremal dependence (i.e.\ the joint tails of $Y_{-i, s}$ and $Y_{-i, s^*}$; see Equation~\eqref{eq:ce_norm}). \todo{have bold Y here?}
% Using the $\JSGa{}$ divergence in Equation~\eqref{eq:js_norm_mvn1}, we define the \textit{conditional skew-geometric Jensen--Shannon divergence} ($\cJSGa{s, s^*}$) between the two sites $s$ and $s^*$ for the conditioning variable $Y_i$ as
% Using the $\JSGa{}$ divergence in Equation~\eqref{eq:js_norm_mvn1}, we define the \textit{conditional skew-geometric Jensen--Shannon divergence} ($\cJSGa{s, s^*}$) between the two sites $s$ and $s^*$ for the conditioning variable $Y_i$ as
Using the $\JSGa{}$ divergence in Equation~\eqref{eq:js_norm_mvn1}, we define the \textit{conditional skew-geometric Jensen--Shannon divergence} ($\cJSGa{s, s^*}$) between the two sites $s$ and $s^*$, conditional on the $i$-th variable being extreme, defined as
\begin{equation} \label{eq:cjsga}
  % \cJSGa{}\bigl(y; s \Vert s^*\bigr) = \JSGa{}\left(H_{\mathbf{Y}_{-i}(s)|Y_i(s)=y} \Vert H_{\mathbf{Y}_{-i}(s^*)|Y_i(s^*)=y}\right),
  \cJSGa{s, s^*}(y) = \JSGa{}\left(H_{i, s}(y) \Vert H_{i, s^*}(y)\right),\qquad y > u_i,
\end{equation}
% where $H_{\mathbf{Y}_{-i}(s)|Y_i(s)=y}$ and $H_{\mathbf{Y}_{-i}(s^*)|Y_i(s^*)=y}$ are the conditional multivariate Gaussian distributions induced by the CE model at sites $s$ and $s^*$, respectively, for a given value of $y > \max(u_i(s), u_i(s^*))$. \todo{Is this max correct?}
% where $H_{i, s}(y)$ and $H_{i, s^*}(y)$ are the conditional multivariate Gaussian distributions for variable $i$ induced by the CE model at sites $s$ and $s^*$, respectively, for a given value of $y > \max(u_{i, s}, u_{i, s^*})$. \todo{Is this max correct? Has it been talked about enough before?} \todo{Should this be mid i? What does Heff2004 do?}
where $H_{i, s}(y)$ and $H_{i, s^*}(y)$ are the conditional $(d-1)$-variate Gaussian distributions at sites $s$ and $s^*$, respectively, see Equation~\eqref{eq:ce_norm}.
% Substituting the means and covariances of these conditional distributions from Equation\eqref{eq:ce_norm} into Equation~\eqref{eq:js_norm_mvn1} gives the closed-form expression
% \todo{Need help tidying up}
% \todo{move equation number to right}
% \todo{What is random word at very end of last line?}
% \begin{align*}% \label{eq:cjsga}
% \cJSGa{}\left(y; s \Vert s^*\right)
%   &= \tfrac{1}{2}\left\{
%     \lambda\,\left(\mvmean{s}\right)^{T}\left(\mvcov{s}\right)^{-1}\left(\mvmean{s}\right) \\
% &\qquad + (1-\lambda)\,
%       \left(\mvmean{s^*}\right)^{T} \\
% &\qquad\qquad\qquad\quad
%       \left(\mvcov{s^*}\right)^{-1} \\
% &\qquad\qquad\qquad\quad
%       \left(\mvmean{s^*}\right) \\
% &\qquad - \symbf{m}_{\lambda}^{T}\Omega_{\lambda}^{-1}\symbf{m}_{\lambda} \\
% &\qquad + \log\!\left(\frac{\left\lvert \mvcov{s}\right\rvert^{\lambda}\,\left\lvert \mvcov{s^*}\right\rvert^{1-\lambda}}{\left\lvert \Omega_{\lambda}\right\rvert}\right) \right\}, \numberthis~\label{eq:cjsga_ce} \\
% \Omega_{\lambda}
%   &= \left(\lambda\,\left(\mvcov{s}\right)^{-1} + (1-\lambda)\,\left(\mvcov{s^*}\right)^{-1}\right)^{-1}, \\
% \symbf{m}_{\lambda}
%   &= \Omega_{\lambda}\left(\lambda\,
%       \left(\mvcov{s}\right)^{-1}\left(\mvmean{s}\right) \\
% &\qquad\quad + (1-\lambda)\,
%       \left(\mvcov{s^*}\right)^{-1}\left(\mvmean{s^*}\right)\right).
% \end{align*}
% We can substitute the multivariate Gaussian distribution in Equation~\eqref{eq:ce_norm} into the formula for the $\JSGa{}$ divergence in Equation~\eqref{eq:js_norm_mvn1} to compare the CE models fit at two sites $s$ and $s^*$, with 
Equation \eqref{eq:cjsga} has a closed-form expression: from Equation~\eqref{eq:ce_norm}, this can be derived by substituting, into Equation~\eqref{eq:js_norm_mvn1}, the conditional parameters $\symbf{m_1} = \mvmean{s}$, $\symbf{m_2} = \mvmean{s^*}$,  $\Omega_1 = \mvcov{s}$, and $\Omega_2 = \mvcov{s^*}$, 
% $\Omega_{\lambda} = \left(\lambda\,\left(\mvcov{s}\right)^{-1} + (1-\lambda)\,\left(\mvcov{s^*}\right)^{-1}\right)^{-1}$, and \\
% $\symbf{m}_{\lambda} = \Omega_{\lambda}\left(\lambda\,
% \left(\mvcov{s}\right)^{-1}\left(\mvmean{s}\right) + (1-\lambda)\,
% \left(\mvcov{s^*}\right)^{-1}\left(\mvmean{s^*}\right)\right)$.
% \begin{align*}
% \symbf{m}_1 &=\mvmean{s}, \\
% \symbf{m}_2 &=\mvmean{s^*}, \\
% \Omega_1    &=\mvcov{s}, \\
% \Omega_2    &=\mvcov{s^*},
% \end{align*}
and 
\todo{Need to include?}
\begin{align*}
\Omega_{\lambda}
  &= \left[\,\lambda\,\left\{\mvcov{s}\right\}^{-1}
           +(1-\lambda)\,\left\{\mvcov{s^{*}}\right\}^{-1}\right]^{-1},\\[6pt]
\symbf{m}_{\lambda}
  &= \Omega_{\lambda}
     \begin{aligned}[t]
       \Biggl[&\lambda\,\left\{\mvcov{s}\right\}^{-1}\mvmean{s}\\
            &\quad +(1-\lambda)\,\left\{\mvcov{s^{*}}\right\}^{-1}\mvmean{s^{*}}\Biggr].
     \end{aligned}
\end{align*}

% % Introduce KL divergence 
% One common measure of dissimilarity between two distributions $P$ and $Q$ is the Kullback-Leibler (KL) divergence, defined as
% \begin{equation}~\label{eq:kl}
%   KL(F_1 \Vert F_2) = \int_{-\infty}^{\infty} p(y) \log\left(\frac{p(y)}{q(y)}\right) dy.
% \end{equation}
% \todo{define mid mid!?}
% where $p(y)$ and $q(y)$ are the densities of $P$ and $Q$, respectively \citep{Kullback1951}.
% % \item The KL divergence is always non-negative and zero if and only if $P =
% % Q$, and for two multivariate normal distributions $P \sim N(\mu_1,
% % \Sigma_1)$, Q \sim N(\mu_2, \Sigma_2)$ in $\mathbb{R}^n$ has closed-form
% % solution.
% The KL divergence is always non-negative and zero if and only if $P = Q$.
% % For two multivariate normal distributions $P \sim N(\mu_1, \Sigma_1), Q \sim N(\mu_2, \Sigma_2) \in \mathbb{R}^n$ with diagonal covariance matrices, $KL(F_1 \Vert F_2)$ has closed-form.
% A well known result is that the KL divergence between two multivariate normal distributions $P \sim N(\mu_1, \Sigma_1)$ and $Q \sim N(\mu_2, \Sigma_2)$ in $\mathbb{R}^n$ has a closed-form solution.
%   % \item In the case where both covariance matrices are diagonal, this simplifies to
%     % \begin{equation}~\label{eq:kl_mvn_diag}
%     %   KL(F_1 \Vert F_2) = \frac{1}{2} \sum_{i=1}^{n} \left( \frac{\sigma_{1,i}^2}{\sigma_{2,i}^2} + \frac{(\mu_{1,i} - \mu_{2,i})^2}{\sigma_{2,i}^2} - 1 - \log\left(\frac{\sigma_{1,i}^2}{\sigma_{2,i}^2}\right) \right),
%     % \end{equation}
%     % where: \todo{Should I use a different notation here, since I also use mu, sigma for the CE model?}
%     % \begin{itemize}
%     %     \item $\mu_1 = (\mu_{1,1}, \mu_{1,2}, \ldots, \mu_{1,n})^\top$ and $\mu_2 = (\mu_{2,1}, \mu_{2,2}, \ldots, \mu_{2,n})^\top$ are the mean vectors,
%     %     \item $\Sigma_1 = \text{diag}(\sigma_{1,1}^2, \sigma_{1,2}^2, \ldots, \sigma_{1,n}^2)$ and $\Sigma_2 = \text{diag}(\sigma_{2,1}^2, \sigma_{2,2}^2, \ldots, \sigma_{2,n}^2)$ are diagonal covariance matrices,
%     %     \item $\sigma_{1,i}^2$ and $\sigma_{2,i}^2$ denote the $i$-th diagonal element of $\Sigma_1$ and $\Sigma_2$, respectively.
%     % \end{itemize}
% However, the KL divergence is asymmetric, unbounded and not a true divergence metric, making it unsuitable for clustering. \todo{need to be more specific here, kinda wishy washy}
% 
% \todo{Do we need to use the backward Geometric mean, as described in Deasy and Thiagarajan? Fine if using alpha = 0.5?}
% The JS divergence is a simple extension of the KL divergence, defined as
% \begin{equation}~\label{eq:js}
%   % JS(F_1 \Vert F_2) = \frac{1}{2} KL(P \Vert M) + \frac{1}{2} KL(Q \Vert M),
%   JS(F_1 \Vert F_2) = \frac{1}{2} \left\{ KL(P \Vert M) + KL(Q \Vert M) \right\},
% \end{equation}
% % where $M = \frac{1}{2}(P + Q)$ is a mixture distribution \citep{Lin1991}.
% where $M$ is the two-component mixture of $P$ and $Q$. 
% The JS divergence provides a symmetric measure of dissimilarity between two distributions.
%   % \item The JS divergence is a true metric bounded by $[0, \log{(2)}]$, unlike the simple sum of the forward and backward KL divergences $KL(F_1 \Vert F_2) + KL(Q \Vert P)$.
% The JS divergence is a true metric unlike the simple sum of the forward and backward KL divergences $KL(F_1 \Vert F_2) + KL(Q \Vert P)$. % don't reference bounds, as JS-G doesn't have any
%   % \item A problem arises from the fact that the mixture distribution $M$ of two normals is not itself normal.
%   % \item In fact, $M$ becomes increasingly bimodal as $P$ and $Q$ become more dissimilar, and so the JS divergence does not have a closed-form solution for multivariate normals.
% A problem arises from the fact that the mixture distribution $M$ of two normals is not itself normal, and so the KL divergence does not have a closed-form solution for multivariate normals.
% \citet{Nielsen2019} overcomes this problem by using the weighted geometric mean $G_{\alpha}(P, Q) = P^{\alpha} Q^{1 - \alpha}, \alpha \in [0, 1]$, instead of the arithmetic mean. 
% This allows us to exploit the property that the weighted product of exponential family distributions is itself a member of the same family, and so $G_{\alpha}(P, Q)$ will also be a multivariate normal distribution.
% \todo{Need to use backward version instead?}
% From this, we define the skew-geometric JS divergence as
% \begin{equation}~\label{eq:js_norm}
%   % JS^{G_{\alpha}}(F_1 \Vert F_2) = \frac{1}{2} KL(P \Vert G_{\alpha}(P, Q)) + \frac{1}{2} KL(Q \Vert G_{\alpha}(P, Q)).
%   JS^{G_{\alpha}}(F_1 \Vert F_2) = \frac{1}{2} \left\{ KL(P \Vert G_{\alpha}(P, Q)) + KL(Q \Vert G_{\alpha}(P, Q)) \right\}.
% \end{equation}
% The parameter $\alpha$ is called a skew parameter, and controls the divergence skew between P and Q. 
% For our purposes, we will set $\alpha = 0.5$, which corresponds to giving equal weight to both distributions. \todo{Need to explore this further!}
% The exact form of this divergence for two multivariate normals is given in Equation~\eqref{eq:js_norm_mvn1}, and the simplified form for the case of diagonal covariance matrices is given in Equation~\eqref{eq:js_norm_mvn_diag}. 
%   % \item Thus we have a closed-form, computationally efficient and symmetric measure of dissimilarity between any two conditional distributions as given by Equation~\ref{eq:ce_norm} for two sites, with which we can cluster locations based on their extremal dependence structure. 
%   % \item Thus we have a closed-form, computationally efficient and symmetric measure of dissimilarity between any conditional multivariate normal distributions as given by \eqref{eq:ce_norm}, representing the extremal dependence structure at each site. 

% Expectation definition
% \todo{Hard/confusing to describe using Yi, switch to another notation?}
% Since the models $P$ and $Q$ depend on the value of $Y_i$, the conditioning variable, we define the expected value
% Since the conditional distributions $P$ and $Q$ are functions of the conditioning variable $Y_i$, the divergence between them depends on the specific value of $Y_i$. 
% Since the conditional distributions $P$ and $Q$ depend on the value of the conditioning variable $Y_i(s)$, the divergence between them also depends on $Y_i(s)$. 
% To obtain a single measure of dissimilarity, we define the expected divergence
% \todo{Need to clean up expectation?}
% \begin{equation}~\label{eq:jsga_integral}
% % \mathbb{E}\!\left[ JS^{G_{\alpha}}(P(y_i)\, \| \, Q(y_i)) \,\middle|\, Y_i > u_i \right] 
% % = \int_{u_i}^\infty JS^{G_{\alpha}}(P(y_i)\, \| \, Q(y_i)) \, f(y_i \mid Y_i > u_i)\, dy_i,
%   % \mathbb{E}\!\left[ \JSGa{(P(y_i)\, \| \, Q(y_i))} \,\middle|\, Y_i > u_i \right] = \int_{u_i}^\infty \JSGa{(P(y_i)\, \| \, Q(y_i))} \, f(y_i \mid Y_i > u_i)\, dy_i,
%   \mathbb{E}\!\left[ \JSGa{(P(y_i)\, \| \, Q(y_i))} \,\middle|\, Y_i(s) > u_i \right] = \int_{u_i}^\infty \JSGa{(P(y_i)\, \| \, Q(y_i))} \, f(y_i \mid Y_i(s) > u_i(s))\, dy_i,
% \end{equation}

% Since the conditional $\JSGa{}$ divergence, $\cJSGa{}$, depends on the value of the conditioning variable $Y_i(s)$, we obtain a single measure of dissimilarity, by defining the expected divergence 
Our conditional $\JSGa{}$ divergence, $\cJSGa{s, s^*}$, depends on the value of the corresponding conditioning variable. 
% Therefore, to obtain a single measure of dissimilarity between the two sites $s$ and $s^*$ for the conditioning variable $Y_i$, we define the expected divergence
Therefore, to obtain a single measure of dissimilarity between the CE models fitted at the two sites $s$ and $s^*$ for the $i$-th conditioning variable, we define the expected divergence
\begin{align*}%~\label{eq:jsga_integral}
  % eJS^{G_\lambda}_i(s || s^*) &= \mathbb{E}(cJS^{G_\lambda}_i(Y_i(s); s || s^*)| Y_i(s) > u_i) \\
  % \eJSGa{}(s || s^*) &= \mathbb{E}(cJS^{G_\lambda}_i(Y_i(s); s || s^*)| Y_i(s) > u_i) \\
  %                             &= \int_{u_i}^{\infty} \cJSGa{(y; s || s^*)} f(y \mid Y_i(s) > u_i) dy, \numberthis~\label{eq:jsga_integral}
  % \eJSGa{s, s^*} &= \mathbb{E}\left(\cJSGa{s, s^*}(Y_{i,s}) \mid Y_{i,s} > u_{i}\right) \\
  \eJSGa{s, s^*} &= \mathbb{E}\left(\cJSGa{s, s^*}(Y_{i,s}) \mid Y_{i,s} > u_i\right) \\
                 &= \mathbb{E}\left[\cJSGa{s, s^*}(Y_{i,s^*}) \mid Y_{i,s^*} > u_i\right] \\
                 &= \int_{u_i}^{\infty} \cJSGa{s, s^*}(y) h(y \mid Y_{i,s} > u_i) dy, \numberthis~\label{eq:jsga_integral}
\end{align*}
% where $h(y \mid Y_{i, s} > u)$ is the conditional density of $Y_{i, s}$ given exceedance of the threshold. 
where $h(y \mid Y_{i, s} > u_i)$ is the conditional density of $Y_{i, s} \mid Y_{i, s} > u_i$.
% This integral gives us our dissimilarity measure or ``distance'' between the two CE models, averaged over the distribution of $Y_{i, s} \mid Y_{i, s} > u_i$.
We here exploit our standardised Laplace margins and note that $h(y \mid Y_{i, s} > u_i) = h(y \mid Y_{i, s^*} > u_i)$, ensuring symmetry of our dissimilarity measure.
% We can estimate this integral using Monte Carlo (MC) methods, noting that the distribution of $Y_i$ is given by the standard Laplace distribution. 

% Monte Carlo estimation
We estimate the integral in Equation~\eqref{eq:jsga_integral} using Monte Carlo methods, noting that $Y_{i, s}$ follows a standard Laplace distribution.
% We average across the individual scores for each individual realisation of $Y_i(s)$ to derive the divergence for a pair of sites, weighted by the distribution of $Y_i(s)$.
In practice, very large values of $Y_{i, s}$ can lead to unstable estimates, and we also want to avoid extrapolating beyond the range of the observed $Y_{i, s}$ used to fit the CE model. 
% Hence, we truncate the expectation at the 99th percentile of the empirical distribution of $Y_i(s)$ across all sites.
% Hence, we truncate the integral in Equation~\eqref{eq:jsga_integral} at the 99th percentile of the empirical distribution of $Y_{i, s}$ across all sites.
Hence, we truncate the limits in integral~\eqref{eq:jsga_integral}, so that the upper limit is the 99th percentile of the empirical distribution of $Y_{i, s}$ pooled across all sites.

% Matrix
% We can repeat this procedure for each pair of sites $(s, s^*)$ and for each conditioning variable $Y_i$, to obtain a matrix of pairwise dissimilarities between all sites for each conditioning variable, given by
We evaluate the pairwise $\eJSGa{s, s^*}$ for all pairs of sites $(s, s^*)$ and for each conditioning variable $i \in \{1, \ldots, d\}$. 
These are then collected in a dsimmilarity matrix
% \begin{align*} %\label{eq:distance_matrix}
% %   % M_i=\left( eJS^{G_\lambda}_i(s || s^*)\right)_{s, s^* \in \{s_1,\dots,s_D\}},
%   % M_i=\left(\eJSGa{}(y; s\Vert s^*)\right)_{s, s^* \in \{s_1,\dots,s_D\}},
%   % M_i=\left[ M_{i, s, s*} \right]_{s, s^* = \{1, \ldots, D\}}, \\
%   M^{(i)}&=\left[ M^{(i)}_{s, s*} \right]_{s, s^* = \{1, \ldots, D\}}, \numberthis~\label{eq:distance_matrix} \\
%   % \text{where } M_{i, s, s^*} = \eJSGa{s, s^*},
%   M^{(i)}_{s, s^*} &= \eJSGa{s, s^*},
% \end{align*}
\begin{equation} %\label{eq:distance_matrix}
  M^{(i)} \coloneq \left( eJSGa{s, s^*} \right)_{s, s^* = \{1, \ldots, D\}},
\end{equation}
% where $D$ is the total number of sites.
% where $D$ is the total number of sites, and $M^{(i)}_{s, s^*}$ is the dissimilarity between sites $s$ and $s^*$ for conditioning variable $Y_i$. \todo{Should I say cond var Yi or just i? Throughout}
where $D$ is the total number of sites, and $i = 1, \ldots, d$.

% If we have $d$ conditioning variables, we can also aggregate the $d$ distance matrices $M_1, \ldots, M_d$ into a single matrix $M$, by taking the element-wise average across all matrices, i.e. \todo{Do we like this representation of the elementwise average?}
% If we have $d$ conditioning variables, we can also aggregate the $d$ distance matrices $M^{(1)}, \ldots, M^{(d)}$ into a single matrix $M$, by taking the element-wise average across all matrices, i.e. \todo{Do we like this representation of the elementwise average?}
If desired, we can aggregate the $d$ dissimilarity matrices $M^{(1)}, \ldots, M^{(d)}$ into a single dissimilarity matrix $M$, by taking their element-wise average, i.e. 
\begin{equation} \label{eq:aggregate_matrix}
  M = \frac{1}{d} \sum_{i=1}^d M_i.
\end{equation}
This aggregated matrix $M$, or the individual matrices $M_i$, are then be used as input to a clustering algorithm, which we describe in Section~\ref{subsec:clustering}.
Optionally, other forms of aggregation, such as a weighted average or median, could be used here instead. 
% Aggregating across multiple conditioning variables allows us to pool information across multiple comparisons and forms of information, and has some precedence in the extremal clustering literature \citep{Mornet2017-js}.
Aggregating allows us to pool information across multiple comparisons and forms of information, and is supported in the extremal clustering literature \citep{Mornet2017-is}.

% % Summary
% % In summary, we define a dissimilarity measure between any two CE models based on the $\JSGa{}$ divergence, which is symmetric, non-negative, and computationally efficient. 
% \todo{Rewrite}
% In summary, we define a dissimilarity measure between any two CE models based on the $\JSGa{}$ divergence, which is computationally efficient, symmetric, non-negative and bounded above. \todo{Can we say its bounded above when its only by another metric?}
% For each pair of sites, the divergence depends on the conditional distributions of $\symbf{Y}_{-i} \mid Y_i$ and we integrate over the distribution of $Y_i$ above the threshold to obtain an expected divergence. 
% % This procedure allows us to construct a matrix of pairwise dissimilarities, or distances, between all sites, representing differences in their extremal dependence structures and providing a basis for clustering.
% This procedure allows us to construct matrices of pairwise dissimilarities, or distances, between all sites for a given conditioning variable, representing differences in their extremal dependence structures, which together or separately provide a basis for clustering.

% \todo{Revisit this!}
% \begin{itemize}
%   % \item However, this version of the $JS^{G_{\alpha}}$ is also unbounded! I need to use the version derived in \citet{Deasy2020} and \citet{Thiagarajan2025}, which does have a finite upper bound! Work in progress!
% However, $JS^{G_{\alpha}}$ is also unbounded. The generalized JS divergence from \citet{Thiagarajan2025} is bounded, but must be estimated using MC, as it has no closed-form solution. Should I use/try it out?
% \end{itemize}

\subsection{Clustering of multivariate extremes}~\label{subsec:clustering}

% Describe clustering process via JS divergence and k-medoids/PAM algorithm (give?
% See Vignotto)

% Using Equation~\eqref{eq:jsga_integral}, we can now contruct a dissimilarity measure to compare two conditional distributions, representing our pairwise ``distances'' between sites. 
% Using Equation~\eqref{eq:jsga_integral}, we can now construct a dissimilarity matrix $M_i$ (Equation~\eqref{eq:distance_matrix}) or an aggregated dissimilarity matrix $M$ (Equation~\eqref{eq:aggregate_matrix}), to compare the conditional distributions at each pair of sites, representing our pairwise ``distances'' between sites for a given conditioning variable $Y_i$ or aggregated across multiple conditioning variables, respectively.
We perform clustering using the matrix $M_i$, or the aggregated matrix $M$, defined in Equations~\eqref{eq:distance_matrix} and \eqref{eq:aggregate_matrix}, respectively.
% We can also aggregate these distances across multiple conditioning variables to pool information across multiple comparisons. 
% PAM (K-means not suitable as clusters cannot be assumed to be spherical) (see
% Vignotto)
% Now we have a distance measure over which to compute the dissimilarity matrix of pairwise ``distances'' between sites.
The choice remains as to which algorithm to use to perform clustering. 
As in \citet{Vignotto2021}, we use the Partitioning Around Medoids (PAM) algorithm, which is a variant of the popular k-medoids algorithm of \citet{Kaufman1987}.
A brief description of the algorithm is as follows:
\todo{Have as algorithm/pseudocode block? Dicuss with Christian}
\begin{itemize} 
  \item The algorithm begins by randomly selecting $k$ medoids from the dataset as our cluster centroids. 
  \item Each observation is then assigned to the cluster of the nearest medoid, using the dissimilarity measure defined in Section \ref{subsec:div}.
  \item The centroids are updated by selecting the observation in each cluster which minimises the sum of dissimilarities to all other observations in that cluster.
  \item This process is repeated until convergence, when no observations change clusters.
\end{itemize}
% \begin{algorithm}
%   \caption{K-medoids (using \(\eJSGa{}\))} \label{alg:kmedoids_ejsg_op}
%   \begin{algorithmic}[1]
%     \Require dataset $D=\{x_1,\dots,x_n\}$, number of clusters $k$, dissimilarity $\eJSGa(\cdot,\cdot)$ (see Sec.~\ref{subsec:div})
%     \Ensure medoids $M=\{m_1,\dots,m_k\}$ and clusters $\{C_1,\dots,C_k\}$
%     %
%     \State \textbf{Initialize:} randomly choose $k$ distinct medoids $M \subset D$
%     \Repeat
%       \For{each observation $x \in D$}
%         \State assign $x$ to cluster $C_j$ where
%         \[
%           j = \arg\min_{i\in\{1,\dots,k\}} \eJSGa{}\big(x, m_i\big)
%         \]
%       \EndFor
%       %
%       \For{each cluster $C_j,\, j=1,\dots,k$}
%         \State update medoid
%         \[
%           m_j \gets \arg\min_{y \in C_j} \sum_{x \in C_j} \eJSGa{}(x,y)
%         \]
%       \EndFor
%     \Until{no observation changes cluster assignment}
%   \end{algorithmic}
% \end{algorithm}
% \begin{algorithm}
%   \caption{K-medoids using the $\eJSGa{}$ dissimilarity} \label{alg:kmedoids_ejsg_integral}
%   \begin{algorithmic}[1]
%     \Require spatial locations $S=\{s_1,\dots,s_D\}$ (with associated observations), number of clusters $k$, threshold $u_i$
%     \Ensure medoids indices $M=\{m_1,\dots,m_k\}$ (indices into $S$) and clusters $\{C_1,\dots,C_k\}$
%     %
%     \State \textbf{Precompute pairwise dissimilarities:}
%     \For{each pair $(p,q)$ with $p,q=1,\dots,n$}
%       \State compute
%       \[
%         D_{pq} \;:=\; \eJSGa\big(s_p \,\|\, s_q\big)
%         \quad\text{(see Eq.~\eqref{eq:jsga_integral})}
%       \]
%       \Statex where, using your definition,
%       \[
%         \eJSGa(s \,\|\, s^*) \;=\; \int_{u_i}^{\infty} \cJSGa\big(y; s \,\|\, s^*\big)\, f\big(y \mid Y_i(s)>u_i\big)\, dy.
%       \]
%       \Comment{If the integral has no closed-form, approximate numerically (quadrature) or by Monte Carlo:}
%       \Statex \(\;D_{pq}\approx \dfrac{1}{T}\sum_{t=1}^{T} \cJSGa\big(y^{(t)}; s_p \,\|\, s_q\big),\quad y^{(t)}\stackrel{\text{iid}}{\sim} f(\cdot\mid Y_i(s_p)>u_i).\)
%     \EndFor
%     %
%     \State \textbf{Initialize:} randomly choose $k$ distinct medoid indices $M \subset \{1,\dots,n\}$
%     \Repeat
%       \For{each observation / location index $p=1,\dots,n$}
%         \State assign $p$ to cluster $C_j$ where
%         \[
%           j = \arg\min_{i\in\{1,\dots,k\}} D_{p,m_i}
%         \]
%       \EndFor
%       %
%       \For{each cluster $C_j,\, j=1,\dots,k$}
%         \State update medoid index
%         \[
%           m_j \gets \arg\min_{q \in C_j} \sum_{p \in C_j} D_{p q}
%         \]
%       \EndFor
%     \Until{no location changes cluster assignment}
%   \end{algorithmic}
% \end{algorithm}

We choose this algorithm over alternatives such as k-means for several reasons. 
Firstly, since our clustering is designed to be performed in a spatial setting, it is preferable to have actual locations rather than summaries as cluster centres, for visualisation.
Secondly, as we are clustering over a dissimilarity matrix defined over a functional space, we cannot assume that the clusters are spherical or equally sized, as k-means does. \todo{cite}
Furthermore, using the median instead of the mean to define the cluster centre is more robust to outliers, which is important in an extremal setting where data for some or many sites may be scarce. 

\subsubsection{Choice of k}~\label{subsubsec:choose_k}

% Describe methods for choosing k (AIC, TWGSS) (silhouette not suitable)
The PAM algorithm takes a single hyperparameter, namely the number of clusters, $k$.
% There is a rich body of literature on methods for selecting $k$, including the silhouette method of \citet{Rousseeuw1987} and the Akaike Information Criterion (AIC) introduced in \citet{Akaike1974}.
% However, the silhouette method is not suitable for our application, as it is designed for spherical clusters. \todo{Cite, have reference for this somewhere?}
% The AIC is also unsuitable and has been found to perform poorly for our algorithm.
% This may be because it requires aggregating over each location, which if unweighted assumes independence between the models fit for each site.
% However, this assumption is often violated in spatial applications.
% We revert, therefore, to the simple method of \citet{Thorndike1953-ie}, which uses the total within-cluster sum of squares (TWGSS) to select $k$. \todo{Fix citation?}
% This method has been found to perform well in both our simulation study in Section~\ref{sec:sim} and our Irish meterological application in Section~\ref{sec:application}, making clear suggestions as to the value of $k$ to use. 
We find that the method of \citet{Thorndike1953-ie}, which uses the total within-cluster sum of squares (TWGSS) to select $k$, performs well in our setting.
Namely, in both our simulation study in Section~\ref{sec:sim} and our Irish meteorological application in Section~\ref{sec:application}, this method provides clear suggestions for the value of $k$ to use.
Specifically, the TWGSS is defined as
\todo{Change notation here, i and j clash with earlier notation}
% \begin{equation}~\label{eq:twgss}
%   % \mathrm{TWGSS}(k) = \sum_{i=1}^{k} \sum_{j=1}^{n_i} d_{ij}^2,
%   \mathrm{TWGSS}(k) = \sum_{i=1}^{k} \sum_{j=1}^{n_i} \delta_{ij}^2,
% \end{equation}
% where $\delta_{ij}$ is the dissimilarity between site $j$ and the centroid of cluster $i$, which we measure using the $\JSGa{}$ divergence defined in section \ref{subsec:div}, and $n_i$ is the number of observations in cluster $i$.
\begin{equation}~\label{eq:twgss}
  % \mathrm{TWGSS}(k) = \sum_{c=1}^{k} \sum_{s \in S} \eJSGa{}(s \Vert m_c),
  \mathrm{TWGSS}(k) = \sum_{c=1}^{k} \sum_{s =1}^{D} \eJSGa{s, s_c},
\end{equation}
% where $\eJSGa{}(s \Vert m_c)$ is the dissimilarity between site $s$ and the medoid of cluster $c$, measured using the $\eJSGa{}$ divergence defined in Section \ref{subsec:div}, and $S$ is the set of sites in cluster $c$. \todo{Is this right?}
where $\eJSGa{s, s_c}$ is the dissimilarity between site $s$ and the medoid site $s_c$ of cluster $c$, and $D$ is the total number of sites. \todo{Is this right?}
We then plot $\mathrm{TWGSS}(k)$ vs.\ $k$, and choose $k^*$, the optimal number of clusters, as the value of $k$ at which the TWGSS reduction sharply slows, or equivalently, the point at which the curve has an ``elbow''.

\section{Simulation study}~\label{sec:sim}
% Introduce section
% Throughout this section, we evaluate the effectiveness of our clustering method using a simulation study.
% Throughout this section, we assess the performance of our clustering method through a simulation study. 
We illustrate performance of our clustering approach using simulated data. 
Clustering accuracy is evaluated using the adjusted Rand index (ARI) of \citet{Hubert1985-ys}, which measures similarity between the true and estimated clustering solutions, adjusted for chance.
% In Section~\ref{subsec:sim_gauss}, a simple simulation design using a Gaussian copula to generate data is described. 
% This data has the advantage of having theoretical asymptotic values for the CE parameters, to which we can directly compare our finite simulation estimates, as a test to ensure that our clustering is working as desired, reducing uncertainty in parameter estimates. 
% Section~\ref{subsec:sim_gauss} introduces a simple design based on the Gaussian copula, which provides known asymptotic values for the CE parameters. 
In Section~\ref{subsec:sim_gauss}, we generate data from a Gaussian copula for which the parameters of the CE model are available in closed-form \citep{Keef2013}.
% These allow us to benchmark our finite-sample estimates and verify that clustering reduces estimation uncertainty. 
This allows us to verify that our clustering approach reduces the estimation uncertainty and to investigate the sensitivity to the threshold.
% In Section~\ref{subsec:sim_mixture}, we extend this study to a more complex simulation design using a mixture of Gaussian and t-copulas, for a variety of simple and more complex dependence structures and scenarios. 
Section~\ref{subsec:sim_mixture} extends the study to more complex scenarios, using mixtures of Gaussian and $t$-copulas to generate a range of dependence structures. 
% We evaluate our clustering solution using the adjusted rand index (ARI), and in Section~\ref{subsubsec:boot} ascertain the improvement of parameter estimation post-clustering using a bootstrapping scheme. 
% We evaluate clustering accuracy using the adjusted Rand index (ARI), and in Section~\ref{subsubsec:boot} examine improvements in parameter estimation through a bootstrap resampling scheme \citet{Hubert1985-ys}.

\subsection{Gaussian copula}~\label{subsec:sim_gauss}

% Single location
% We first consider a simple simulation design based on the Gaussian copula.  
% % Our Gaussian copula simulation design for a single ``location'', nomenclature chosen to match the spatial nature of our applications, is as follows:
% % We generate multivariate data from a bivariate Gaussian copula, where we control the dependence structure through its correlation parameter, $\rhou{Gauss}$. 
% For a single ``location'', terminology chosen to reflect our spatial application, we generate bivariate data from a Gaussian copula with correlation parameter $\rho^{\text{Gauss}}$, which controls the dependence structure.  
% \citet{Keef2013} show that for a bivariate Gaussian copula, the asymptotic CE parameters are $\alpha = \text{sign}(\rho^{\text{Gauss}}) (\rho^{\text{Gauss}})^2$ and $\beta = 1/2$, implying asymptotic independence.  
% % \citet{Keef2013} show that for a bivariate Gaussian copula, the asymptotic CE parameters are $\alpha^{(\CE{})} = \rhou{Gauss}^2$ and $\beta^{(\CE{})} = 1/2$, corresponding to asymptotic independence.
% % \item We sample from this copula and transform to GPD margins with shape $\xi = -0.05$, scale $\sigma = 1$, to generate our data. \todo{mention why we chose these?}
% % \item Each variable in this multivariate dataset may represent, for example, a meteorological or air pollution variable at a given location.
% % \item We can then transform to Laplace margins and use the CE model to estimate the multivariate extremal dependence structure at this location.
% % We sample from this copula and transform to Laplace margins and use the CE model to estimate the bivariate extremal dependence structure at this location.
% We sample from this copula, transform to Laplace margins, and then fit the CE model to estimate the extremal dependence structure at the location.

% % Single location
% We first consider a simple simulation design based on the Gaussian copula.  
% % Our Gaussian copula simulation design for a single ``location'', nomenclature chosen to match the spatial nature of our applications, is as follows:
% % We generate multivariate data from a bivariate Gaussian copula, where we control the dependence structure through its correlation parameter, $\rhou{Gauss}$. 
% For a single ``location'', terminology chosen to reflect our spatial application, we generate bivariate data from a Gaussian copula with correlation parameter $\rho^{\text{Gauss}}$, which controls the dependence structure.  
% \citet{Keef2013} show that for a bivariate Gaussian copula, the asymptotic CE parameters are $\alpha = \text{sign}(\rho^{\text{Gauss}}) (\rho^{\text{Gauss}})^2$ and $\beta = 1/2$, implying asymptotic independence.  
% % \citet{Keef2013} show that for a bivariate Gaussian copula, the asymptotic CE parameters are $\alpha^{(\CE{})} = \rhou{Gauss}^2$ and $\beta^{(\CE{})} = 1/2$, corresponding to asymptotic independence.
% % \item We sample from this copula and transform to GPD margins with shape $\xi = -0.05$, scale $\sigma = 1$, to generate our data. \todo{mention why we chose these?}
% % \item Each variable in this multivariate dataset may represent, for example, a meteorological or air pollution variable at a given location.
% % \item We can then transform to Laplace margins and use the CE model to estimate the multivariate extremal dependence structure at this location.
% % We sample from this copula and transform to Laplace margins and use the CE model to estimate the bivariate extremal dependence structure at this location.
% We sample from this copula, transform to Laplace margins, and then fit the CE model to estimate the extremal dependence structure at the location.
% 
% For a single ``location'', terminology chosen to reflect our spatial application, we generate bivariate data from a Gaussian copula with correlation parameter $\rho_{\text{Gauss}}$, which controls the dependence structure.  
% Let
% \[
% \Sigma(\rho_{\text{Gauss}})=
% \begin{pmatrix}
% 1 & \rho_{\text{Gauss}}\\[4pt]
% \rho_{\text{Gauss}} & 1
% \end{pmatrix}.
% \]
% We sample
% \[
%   (Z_1,Z_2)^\top \sim \mbox{MVN}\big(0,\Sigma(\rho_{\text{Gauss}})\big),
% \qquad
% X_i=\Phi(Z_i),\ i=1,2
% \]
% where $\Phi$ is the standard normal CDF. 
% \citet{Keef2013} show that the asymptotic CE parameters for the bivariate Gaussian copula are $\alpha=\operatorname{sign}(\rho_{\text{Gauss}})\,(\rho_{\text{Gauss}})^2$ and $\beta=1/2$, which imply asymptotic independence.  
% We sample from this copula, transform to Laplace margins as in \eqref{eq:laplace_transform}, and then fit the CE model to estimate the extremal dependence structure at the location.
% 
% % Multiple locations
% % We can easily extend this design by generating multiple locations with different $\rhou{Gauss}$ values.
% This design extends naturally to multiple locations by varying $\rho_{\text{Gauss}}$ across sites.  
% % The knowledge of these parameters provides our ``known'' clustering solution.
% The collection of correlation values defines the ``true'' clustering structure.  
% % For example, we could generate data for four locations, the first cluster being two locations with $\rhou{Gauss} = 0.1$, and the second having $\rhou{Gauss} = 0.9$ for its two locations.
% For example, we may generate data for four locations: two with $\rho_{\text{Gauss}} = 0.1$ and two with $\rho_{\text{Gauss}} = 0.9$, corresponding to two clusters.  
% % We can use the CE model to estimate the bivariate extremal dependence structure at each location, and then apply the $\JSGa{}$ divergence to cluster these locations based on the similarity of the estimated CE parameters.
% We then estimate the CE parameters at each site and apply the $\JSGa{}$ divergence to cluster locations based on the similarity of their extremal dependence structures.  
% % These simulations can be repeated many times and we can evaluate the clustering solution using the ARI, and observe the bias and variance of our simulation estimates around their theoretical values.
% Repeating this procedure many times allows us to assess clustering performance using the ARI and quantify the bias and variance of the finite-sample CE parameter estimates relative to their theoretical asymptotic values.
% 
% % Results
% % To this end, we generated data for 12 locations each with 1000 observations of two variables using the above design.
% To this end, we generated data for 12 locations, each with 1000 observations of two variables, using the design described above.  
% % Three known clusters of four locations each were designated as having $\rhou{Gauss}$ values of 0.1, 0.5, and 0.9, respectively.
% Three known clusters of four locations each were assigned correlation values $\rho_{\text{Gauss}} = 0.1, 0.5,$ and $0.9$, respectively.  
% Conditional quantiles of 0.9 and 0.99 were used to estimate the CE model parameters before and after clustering. 
% % The number of clusters $k$ was ascertained using the method described in Section~\ref{subsubsec:choose_k}, and was found to be $k = 3$.
% The number of clusters $k$ was selected using the method in Section~\ref{subsubsec:choose_k}, yielding $k = 3$.  
% Simulations were repeated 500 times, and the results are shown in Figure~\ref{fig:00_gauss_cop}.  

% Sim design
We first consider a simulation design based on the Gaussian copula.  
We consider $S=12$ ``locations'', each associated with two variables. 
Data are generated independently across sites, with $n=1000$ bivariate observations per location.  
% At site $s=1,\dots,S$, dependence is introduced through a Gaussian copula with correlation parameter $\rho_{\text{Gauss},s}$.
At each site $s=1,\dots,S$, dependence is introduced through a Gaussian copula with correlation parameter $\rho_{\text{Gauss}}^s$.

% Per-site sampling
For each site, let
\[
\Sigma(\rho_{\text{Gauss}}^s)=
\begin{pmatrix}
1 & \rho_{\text{Gauss}}^s\\[4pt]
\rho_{\text{Gauss}}^s & 1
\end{pmatrix}.
\]
We draw
\[
  (Z_{1},Z_{2})^\top \sim \mbox{MVN}\big(0,\Sigma(\rho_{\text{Gauss}}^s)\big),
\qquad
U_i=\Phi(Z_i),\ i=1,2,
\]
where $\Phi$ is the standard normal CDF.  
The uniforms $(U_1,U_2)$ are then transformed to standard Laplace margins as in Equation~\eqref{eq:laplace}, yielding the observed pair $(X_1,X_2)$ at site $s$.

% Parameter choices and true clustering
The sites are arranged into three latent clusters of equal size, defined by their copula correlations. 
As a proof of concept, we choose well-separated values to ensure clear clustering.
Specifically, the first cluster, comprising sites 1 to 4, has $\rho_{\text{Gauss}}^s=0.1, s = 1, \ldots, 4$, the second cluster has $\rho_{\text{Gauss}}^s=0.5, s = 5, \ldots, 8$ 
and the third cluster has $\rho_{\text{Gauss}}^s=0.9, s = 9, \ldots, 12$.
These values determine the ground-truth clustering structure against which methods are evaluated.

% Estimation
% At each site the CE model is fitted using conditional quantile levels $q=0.9$ and $q=0.99$, equivalent to taking the largest 100 and 10 observations of the conditioning variable, respectively, to estimate the dependence structure between the two variables as described by the CE parameters $\alpha$ and $\beta$.
After data generation, the conditional extremes (CE) model is fitted at each site using conditional quantile levels \(q=0.9\) and \(q=0.99\), equivalent to using the largest 100 and 10 observations of the conditioning variable, respectively.
% Pairwise dissimilarities between sites are computed using the $\JSGa{}$ divergence, and clusters are identified using PAM, following the procedure in Section~\ref{subsec:clustering}.
% Pairwise dissimilarities between sites are computed using the $\JSGa{}$ divergence between the fitted CE models for $X_1 \mid X_2$ and $X_2 \mid X_1$, which are averaged to obtain a single dissimilarity measure. 
Pairwise dissimilarities between sites are computed using the expected divergence from Equation~\eqref{eq:jsga_integral}, averaged over both conditioning directions, \(X_1 \mid X_2\) and \(X_2 \mid X_1\), to obtain a single dissimilarity measure.
Clusters are identified using the PAM algorithm, following Section~\ref{subsec:clustering}. 
\citet{Keef2013} show that for a bivariate Gaussian copula, the asymptotic CE parameters are $\alpha = \text{sign}(\rho_{\text{Gauss}}) (\rho_{\text{Gauss}})^2$ and $\beta = 1/2$, implying asymptotic independence.  
These theoretical values serve as the reference for the CE estimates.
% Simulations are repeated 500 times, with performance assessed via the ARI and by comparing finite-sample CE estimates to these asymptotic values.
Simulations are repeated 500 times, with performance assessed via the ARI and by comparing finite-sample CE estimates to these asymptotic benchmarks. 
% Across all simulations, the correct number of clusters, $k=3$, was identified using the elbow method described in Section~\ref{subsubsec:choose_k}, and the ARI was always equal to 1, indicating perfect clustering, as expected given such well-separated clusters.
% Figure~\ref{fig:00_gauss_cop} summarises the results.

% The estimation of $k$ through the elbow method and the ARI for this simple, well-defined example were perfect across all simulations.
% The elbow method consistently recovered the correct number of clusters, and the ARI was perfect across all simulations.  
% Several interesting points can be made from the results.
% Pre-clustering, the CE model estimates have greater uncertainty, especially for $\beta$, where the bias is also larger.
% Pre-clustering, CE parameter estimates exhibit considerable uncertainty, particularly for $\beta$, where bias, defined as the difference between the finite-sample estimate and the asymptotic parameter, is also more pronounced. \todo{beta hat?}
% This effect is strongest at the higher conditional quantile (0.99), where data are scarcer.  
% However, estimates at this level display less bias overall, with true values typically lying between the first and third quartiles of the boxplots, reflecting improved convergence to the asymptotic parameters further into the tail.  
% % Bias and variance are lower for higher $\rhou{Gauss}$, which appears to also be associated with better convergence to these theoretical asymptotic values.
% Bias and variance both decrease with increasing $\rhou{Gauss}$, suggesting faster convergence under stronger dependence. \todo{Need to switch to s superscript in plots and here?}
% These same patterns are present post-clustering, but with reduced variability in the estimates, especially in the problematic cases of estimating $\beta$ and for the higher conditional quantile level, as desired. \todo{Quote any specific values? Or are plots self-evident?}

Figure~\ref{fig:00_gauss_cop} shows that our approach is able to reduce the uncertainty in the estimates for both $\alpha$ and $\beta$ for all considered conditioning quantiles. 
Bias, as defined as the difference between the finite-sample estimate and the asymptotic parameter, and variance also decrease with increasing $\rho_{\text{Gauss}}$, indicating faster convergence under stronger dependence. 
We observe a bias-variance tradeoff in the choice of quantile for the CE model, with the higher quantile ($q=0.99$) yielding less biased estimates, as expected due to improved convergence to the asymptotic parameters further into the tail, but with greater variance due to data scarcity.

% \begin{figure}[H]
\begin{figure}[h]
    \centering
    % \includegraphics[width = 0.9\linewidth]{plots/sim_01_gauss_cop.png}
    \includegraphics[width = 0.9\linewidth]{plots/sim_01_gauss_cop_man.png}
    % \caption{Boxplots of \emph{$\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$ parameter estimates for the conditional extremes model pre- and post-clustering for a bivariate Gaussian copula simulation of 12 datasets, or ``locations'', each with 1000 observations of each variable, belonging equally to three known clusters. Simulations were repeated 500 times. The x-axis represents the conditional quantile used to fit the conditional extremes model, and the boxes are coloured by their Gaussian copula correlation parameter, $\rhou{Gauss}$. Dotted horizontal lines indicate the theoretical asymptotic values of $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$ for a bivariate Gaussian copula, coloured by the same $\rhou{Gauss}$ values for $\alpha^{(\CE{})}$ and black for $\beta^{(\CE{})}$ to indicate the same value across all clusters, at $1/2$.}}
    \caption{\emph{Boxplots of $\alpha$ and $\beta$ parameter estimates for the conditional extremes model pre- and post-clustering for a bivariate Gaussian copula simulation of 12 datasets, or ``locations'', each with 1000 observations of each variable, belonging equally to three known clusters. Simulations were repeated 500 times. The x-axis represents the conditional quantile used to fit the conditional extremes model, and the boxes are coloured by their Gaussian copula correlation parameter, $\rhou{Gauss}$. Dotted horizontal lines indicate the theoretical asymptotic values of $\alpha$ and $\beta$ for a bivariate Gaussian copula, coloured by the same $\rhou{Gauss}$ values for $\alpha$ and black for $\beta$ to indicate the same value across all clusters, at $1/2$.}}
   ~\label{fig:00_gauss_cop}
\end{figure}


\subsection{Mixture models}~\label{subsec:sim_mixture}

% In this section, we extend our simulation design to a mixture of Gaussian and t-copulas, where we control the dependence structure through their respective correlation parameters, $\rhou{Gauss}$ and $\rhou{t}$, with each t-copula having 3 degrees of freedom.
% The idea behind this design is that the Gaussian copula generates observations exhibiting extremal independence, whereas the t-copula induces extremal dependence, the strength of which is determined by their respective correlation parameters.
% While there are no theoretical guarantees about our estimates, we can still evaluate our clustering solution for this more difficult setting using the ARI.

We next extend our simulation design to mixtures of Gaussian and $t$-copulas \citep{Demarta2007-ap}.
% The idea is that the Gaussian copula induces asymptotic independence, while the $t$-copula induces asymptotic dependence, with strength determined by $\rho^{t}$.  
The idea is that the Gaussian copula will induce asymptotic independence, while the $t$-copula will induce asymptotic dependence, whose strength is determined by $\rho_{t}$, the correlation parameter of the $t$-copula.
Although no theoretical CE parameter values are available in this setting, clustering performance can still be assessed via the ARI.  

% We next extend our simulation design to mixtures of Gaussian and $t$-copulas.
% More formally, the mixture copula is defined as
% \[
%   C_{\text{mix}}(u_1,u_2)= \tfrac{1}{2}\,C_{\text{t},\nu=3}(u_1,u_2;\Sigma(\rho_{\text{t}}^s)) +
%   \tfrac{1}{2}\,C_{\text{Gauss}}(u_1,u_2;\Sigma(\rho_{\text{Gauss}}^s)),
% \]
% where $C_{\text{Gauss}}$ and $C_{\text{t},\nu=3}$ are the bivariate Gaussian and $t$ copulas with correlation matrices $\Sigma(\rho_{\text{Gauss}}^s)$ and $\Sigma(\rho_{\text{t}}^s)$ respectively, and $\nu$ is the degrees of freedom parameter of the $t$-copula, fixed at $\nu=3$ throughout.

% Formally, for site $s=1,\dots,S$ we define the mixture copula
% \[
%   C_{\mathrm{\text{mix}}}^{s}(u_1,u_2)
%   \;=\; \tfrac{1}{2}\,C_{t,\nu=3}\big(u_1,u_2;\Sigma(\rT{s})\big)
%        \;+\; \tfrac{1}{2}\,C_{\mathrm{Gauss}}\big(u_1,u_2;\Sigma(\rG{s})\big),
% \]
% where \(C_{\mathrm{Gauss}}\) and \(C_{t,\nu=3}\) denote the bivariate Gaussian and $t$ copulas, respectively, with correlation matrices
% \[
% \Sigma(\rG{s})=\begin{pmatrix}1 & \rG{s}\\[4pt]\rG{s} & 1\end{pmatrix},\qquad
% \Sigma(\rT{s})=\begin{pmatrix}1 & \rT{s}\\[4pt]\rT{s} & 1\end{pmatrix},
% \]
% and the $t$-copula degrees of freedom are fixed at \(\nu=3\).
% 
% We implement an exact 50/50 composition of Gaussian and $t$-copulas at each site.  At site $s$, we draw \(m=n/2\) observations from the bivariate Gaussian copula with correlation matrix \(\Sigma(\rG{s})\) and independently draw \(m=n/2\) observations from the bivariate $t$-copula with correlation matrix \(\Sigma(\rT{s})\).  The two halves are then pooled to give \(n\) observations at site \(s\). 
% 
% More precisely, for the Gaussian half we draw
% \[
%   (Z_{ij}^{s},Z_{i2}^{s})^\top \sim \mbox{MVN}\big(0,\Sigma(\rG{s})\big),\quad i=1,\dots,m, 
% \]
% and for the $t$ half
% \[
% (Z_{i1}^{s},Z_{i2}^{s})^\top \sim \mathrm{MVT}_{\nu=3}\big(0,\Sigma(\rT{s})\big),\quad i=m+1,\dots,n.
% \]
% We then transform these latent draws to uniform margins using the appropriate CDFs:
% \[
% U_{ij}^{(s)} =
% \begin{cases}
% \Phi\big(Z_{ij}^{(s)}\big), & i=1,\dots,m,\\[6pt]
% F_{t_3}\big(Z_{ij}^{(s)}\big), & i=m+1,\dots,n,
% \end{cases}
% \qquad j=1,2,
% \]
% where \(\Phi\) and \(F_{t_3}\) are the standard normal and \(t_3\) CDFs.  
% We then apply the Laplace quantile map to every uniform:
% Finally, we apply the Laplace transformation as described in~\eqref{eq:laplace} to obtain the Laplace-scale pairs \((Y_{1}^{(s)},Y_{2}^{(s)})\) at site \(s\).

More formally, for site $s=1,\dots,S$ and dimension $J\ge2$, let $\RG{s},\RT{s}\in\mathbb{R}^{J\times J}$ be correlation matrices where $\RG{s}$ has all off-diagonal entries equal to $\rhou{Gauss}^s$ and $\RT{s}$ has all off-diagonal entries equal to $\rhou{t}^s$, and all diagonal entries equal to 1 for both.
We define the $J$-variate mixture copula
\begin{equation} ~\label{eq:mix_copula}
  C_{\mathrm{mix}}^{(s)}(\mathbf{u})
  \;=\; \tfrac{1}{2}\,C_{t,\nu=3}\!\big(\mathbf{u};\RT{s}\big)
       \;+\; \tfrac{1}{2}\,C_{\mathrm{Gauss}}\!\big(\mathbf{u};\RG{s}\big),
  \qquad \mathbf{u}\in[0,1]^J,
\end{equation}
i.e.\ an equal-weight combination of the $J$-variate $t$-copula (with $\nu=3$ degrees of freedom) and the $J$-variate Gaussian copula.

At site $s$, we generate an exact half–half sample of size $n$ by drawing $m=n/2$ observations from each component. 
Let $\mathbf{Z}_{i}^{(s)}\in\mathbb{R}^J$ denote latent vectors. We sample
\todo{Check if each line is numbered}
\begin{equation} ~\label{eq:mix_latent}
\begin{aligned}
&\text{Gaussian half:}\quad &&\mathbf{Z}_{i}^{(s)} \sim \mbox{MVN}_J\!\big(\mathbf{0},\RG{s}\big),
&& i=1,\dots,m,\\[2pt]
&\text{$t$ half:}\quad      &&\mathbf{Z}_{i}^{(s)} \sim \mbox{MVT}_{J,\nu=3}\!\big(\mathbf{0},\RT{s}\big),
&& i=m+1,\dots,n,
\end{aligned}
\end{equation}
where $\mbox{MVT}_{J, \nu = 3}$ denotes the $J$-variate $t$ distribution with $\nu=3$ degrees of freedom.
We map these latent draws to uniforms componentwise using the appropriate univariate CDF:
\[
U_{ij}^{(s)} =
\begin{cases}
\Phi\!\big(Z_{ij}^{(s)}\big), & i=1,\dots,m,\\[4pt]
F_{t_3}\!\big(Z_{ij}^{(s)}\big), & i=m+1,\dots,n,
\end{cases}
\qquad j=1,\dots,J,
\]
where $\Phi$ and $F_{t_3}$ are the standard normal and univariate $t$ CDFs, respectively.
Pooling the $n$ uniforms $\{\mathbf{U}_{i}^{(s)}\}_{i=1}^n$ yields a size-$n$ sample from the 50/50 mixture copula $C_{\mathrm{mix}}^{(s)}$, up to ordering. 
Finally, we apply the Laplace transformation as described in Equation~\eqref{eq:laplace} to obtain the Laplace-scale pairs \((Y_{1}^{(s)},Y_{2}^{(s)})\) at site \(s\).

% Below, we will describe the results of this simulation study in a variety of scenarios, as defined by a grid search over possible $\rhou{Gauss}$ and $\rhou{t}$ values. 
We consider a range of scenarios defined by a grid search over $\rhou{Gauss}$ and $\rhou{t}$ values, and for different numbers of sites and variables. 
% In Section~\ref{subsubsec:sim_competing_methods}, we compare our method to the leading competitor in the bivariate case, \cite{Vignotto2021}, and also show how our clustering algorithm uniquely provides a tool for clustering in the multivariate extremal dependence setting in  Section~\ref{subsubsec:sim_extension}.
% In Section~\ref{subsubsec:sim_competing_methods}, we compare our method to the leading competitor in the bivariate case \citep{Vignotto2021}, and in Section~\ref{subsubsec:sim_extension} we show how our approach uniquely handles clustering in the multivariate extremal dependence setting.  
In Section~\ref{subsubsec:sim_competing_methods}, we compare our method to the leading competitor in the bivariate case \citep{Vignotto2021}, and in Section~\ref{subsubsec:sim_extension} we show how our approach uniquely handles clustering in higher dimensions.
% A more realistic setting is considered in Section~\ref{subsubsec:sim_realistic}, where we simulate data from 60 locations with three unequally sized clusters, and perturb the correlation parameters to make the clustering solution more challenging.
% A more realistic experiment is presented in Section~\ref{subsubsec:sim_realistic}, where data are simulated from 60 locations in three unequally sized clusters, with perturbed correlation parameters to make clustering more challenging.  
Section \ref{subsubsec:sim_realistic} then considers a setting with 60 locations across three unequally sized clusters, and locations in the same cluster exhibiting perturbations in their correlation parameters.
% We can also evaluate the improvement in parameter estimation post-clustering using the bootstrapping scheme described in \cite{Heffernan2004}, as done in Section~\ref{subsubsec:boot}.
Finally, in Section~\ref{subsubsec:boot} we assess improvements in parameter estimation post-clustering using the bootstrap scheme of \citet{Heffernan2004}.  

\subsubsection{Comparison to \citet{Vignotto2021}}~\label{subsubsec:sim_competing_methods}

% We compared our method to the leading competitor in the bivariate case, \citet{Vignotto2021}, using the max risk function which they propose.
We compared our method to the leading competitor in the bivariate case, \citet{Vignotto2021}.
As in Section~\ref{subsec:sim_gauss}, we generated data for $S = 12$ locations, each with $n = 1000$ observations of $J = 2$ variables.
However, this time we use the mixture of Gaussian and t-copulas with Laplace margins, as described above in Section~\ref{subsec:sim_mixture}. 
Two known clusters were defined using different values of $\rhou{t}$, keeping $\rhou{Gauss}$ the same for both clusters. % (but varying across simulations).
  % \item These values were varied across a grid search.
% The CE model was fit at the $90^{th}$ quantile, i.e., the $90^{th}$ quantile of $Y_{i} \mid Y_{j} = y_j$, and clustering solutions were obtained using the $\JSGa{}$ divergence. 
The CE model was fit at the $90^{th}$ quantile, i.e., the $90^{th}$ quantile of $Y_{i} \mid Y_{j} = y_j$, and clustering solutions were obtained using the divergence in Equation~\eqref{eq:jsga_integral}.
% The clustering solutions were evaluated using the ARI, with results shown in Figure \ref{fig:01_ce_vs_vi} for a grid search over different $\rhou{Gauss}$ and $\rhou{t}$ values, with 500 simulations for each setting.
Performance was evaluated with the ARI, based on a grid search over values of $\rhou{Gauss}$ and $\rhou{t}$, with 500 simulations per setting.
This grid search was repeated 500 times, the results of which are shown in Figure~\ref{fig:01_ce_vs_vi}. 

% The proposed method is shown to be superior to the method of \cite{Vignotto2021}, across a range of possible bivariate dependence structures, as defined by $\rhou{t}$.
Across this range of bivariate dependence structures, our method consistently outperformed that of \citet{Vignotto2021}. 
% Both models perform better when the difference in the t-copula correlation parameters between the two clusters is larger, as the clusters are naturally more distinct when there is greater disparity in dependence.
Both methods had higher ARI when the difference in t-copula correlation parameters between the clusters was larger, since greater disparity naturally makes clusters more distinct. 
% Both models also perform better when Gaussian and t-copula correlation are higher, as the signal of extremal dependence is stronger, with more extremal data available.
% Similarly, both performed better when the Gaussian and t-copula correlations were higher, as stronger extremal dependence increases the availability of extremal data. \todo{Is this why performance was better for higher Gaussian correlation?}
Also, both performed better when the Gaussian and t-copula correlations were higher.
This may be because the data from the Gaussian copula becoming more similar to that of the t-copula for higher correlation, thus allowing the clustering approach to focus on differences in the t-copula.

% \todo{Come up with better acronym than CE}
% \begin{figure}[H]
\begin{figure}[h]
    \centering
    \includegraphics[width = 0.9\linewidth]{plots/sim_01b_ce_vs_vi_dqu_0.9_man.png}
    % \caption{\emph{Comparison of \citep{Vignotto2021} clustering with our proposed method (labelled ``Cond. Extremes''), for two variables and two equally sized clusters for simulations of 12 ``locations'', each with 1000 observations of each variable, from a mixture of Normal and Gaussian copulas. A grid search was performed, with the x-axis representing the Gaussian correlation parameter used for both clusters, and the facet labels showing the t-copula correlation parameters for each ``known'' cluster. This grid search was repeated 500 times. The smoothed lines show the smoothed median of the Adjusted Rand Index for both clustering methods, with smoothing performing using a generalised additive model.}} % The lines show the median of the Adjusted Rand Index for both clustering methods, with uncertainty coming from the 90\% credible interval.}} % The proposed method is shown to be superior to the leading competitor in the bivariate case, that of \cite{Vignotto2021}. Both models perform better when the difference in the t-copula correlation parameters between the two clusters is larger, and when Gaussian correlation is higher.
    % \caption{\emph{Comparison of our clustering method based on the Conditional Extremes model and the method of \citet{Vignotto2021} for extremal clustering.  
    \caption{\emph{Comparison of performance for CE clustering and \citet{Vignotto2021}. 
    The x-axis represents the Gaussian correlation parameters, and facet labels indicating the t-copula correlation parameters for each ``known'' cluster, generated from a mixture of Gaussian and t-copulas with Laplace margins.
    % Bivariate data from two equally sized clusters were simulated for 12 ``locations,'' each with 1000 observations of each variable, from a mixture of Gaussian and t-copulas with Laplace margins. 
    % A grid search was performed, with the x-axis representing the Gaussian correlation parameter, and facet labels indicating the t-copula correlation parameters for each ``known'' cluster. 
    % This grid search was repeated 500 times. 
    The smoothed lines show the Adjusted Rand Index (ARI) for both methods, with smoothing performed using a generalised additive model.}} 
    ~\label{fig:01_ce_vs_vi}
\end{figure}

\subsubsection{Extension to $>2$ dimensions}~\label{subsubsec:sim_extension}

While the method of \citet{Vignotto2021} is restricted to two dimensions, our approach can be extended to three dimensions and beyond. 
% To illustrate this, we can repeat the simulation study and grid search of Section~\ref{subsubsec:sim_competing_methods}, but now for three variables, the results of which are shown in Figure \ref{fig:02_3d}.
% To illustrate this, we repeated the simulation study and grid search of Section~\ref{subsubsec:sim_competing_methods} for three variables, with results shown in Figure~\ref{fig:02_3d}. 
To illustrate this, we repeat the simulation study and grid search of Section~\ref{subsubsec:sim_competing_methods} by simulating $S = 12$ locations with two equally sized latent clusters of sites each with $n = 1000$ observations, but this time for $J = 3$ variables.
Now the mixture copula $C_{\mathrm{mix}}^{(s)}$ described in equation has three dimensions, and we fit six CE models at each site, averaging the $\JSGa{}$ divergence over all of these models between each pair of sites to obtain a single dissimilarity matrix over which we perform PAM clustering.

This grid search was again repeated 500 times, with the results shown and compared to the bivariate case in Figure \ref{fig:02_3d}.
% The proposed method is shown to work well for three dimensions, and the same patterns in the ARI emerge as in the bivariate case.
These clearly show that our proposed method performs well in three dimensions, with the same patterns in ARI emerging as in the bivariate case in Section~\ref{subsubsec:sim_competing_methods}.
% The method is shown to actually perform better in three dimensions than in two.
In fact, performance is markedly stronger in three dimensions than in two. 
% This comes with the caveat that in these simulations, the variables all have the same margins, and so including three variables naturally increases the amount of data available to estimate the same dependence structure as in the bivariate case, and does not introduce additional noise which might be present in real multivariate applications.
% This improvement comes with the caveat that, in these simulations, all variables have the same margins. 
% Thus, including three variables naturally increases the amount of data available to estimate the same dependence structure as in the bivariate case, without introducing the additional noise that might arise in real multivariate applications. 
However, this is not surprising, as the CE models in this setup are the same in both two and three dimensions, and the $\JSGa{}$ divergence and its integral from Equation~\eqref{eq:jsga_integral} are computed in the same way, just averaged over more models with more data.
\begin{figure}[h]
    \centering
    % \includegraphics[width = 0.9\linewidth]{plots/sim_01c_js_sens_3_var_dqu_0.9.png}
    % \includegraphics[width = 0.9\linewidth]{plots/sim_01d_js_sens_3_var_dqu_0.9_man.png}
    % \caption{\emph{Evaluation of clustering performance for three variables and two equally sized clusters for simulations of 12 ``locations'', each with 1000 observations for each variable, from a mixture of Gaussian and t-copulas. 
    % A grid search was performed,  with the x-axis representing the Gaussian correlation parameter used for both clusters, and the facet labels showing the t-copula correlation parameters for each ``known'' cluster. 
    % This grid search was repeated 500 times. 
    % The smoothed lines show the smoothed median of the Adjusted Rand Index for both clustering methods, with smoothing performing using a generalised additive model.}} % Points show individual values of the ARI for a given simulation.}} 
    \includegraphics[width = 0.9\linewidth]{plots/sim_01d_js_sens_2_3_var_dqu_0.9_man.png}
    \caption{\emph{Comparison of clustering perfomance for two and three variables and two equally sized clusters for simulations of 12 locations from a mixture of Gaussian and t-copulas.
    The smoothed lines show the smoothed median of the Adjusted Rand Index for both dimensions, with smoothing performing using a generalised additive model.}} % Points show individual values of the ARI for a given simulation.}} 
   ~\label{fig:02_3d}
\end{figure}

% Expand to up to 5 dimensions!
% To further illustrate how our method can be extended to higher dimensions and why this is useful, let us consider a specific example from our grid search, where we have $\rho_{\text{t}_1}, \rho_{\text{t}_2} = 0.5$.
To further illustrate the extension of our method to higher dimensions, we consider a specific case from the grid search where $\rho_{\text{t}_1} = 0.4$ and $\rho_{\text{t}_2} = 0.5$. 
% This is a relatively difficult clustering scenario in both the bivariate and three-dimensional cases, as the difference in the t-copula correlation parameters between the two clusters is small.
This setting presents a challenging clustering scenario in both two and three dimensions, as the difference in the t-copula correlation parameters between clusters is small. 
% In Figure~\ref{fig:02b_5d}, we show the results of our clustering algorithm when the dimensions of the mixture of copula is progressively increased from two to five, for 500 simulations at each $\rhou{Gauss}$ value. 
Figure~\ref{fig:02b_5d} displays the results when the dimensionality of the copula mixture is increased from two to five, based on 500 simulations at each $\rhou{Gauss}$ value. 
% We clearly see that the ARI increases as the number of dimensions increases, with the clustering solution improving as we include more variables.
% At 5 dimensions, the ARI is close to 1 for all $\rhou{Gauss}$ values. 
Clustering accuracy, measured by the ARI, improves steadily with dimension, reaching values close to 1 across all $\rhou{Gauss}$ settings at five dimensions. 
% Again, we must be cautious in interpreting these results, as in this example we merely increase the amount of data in our simulation and keep the dependence structure the same across each pair of variables, 
These gains should be interpreted cautiously, since the simulations increase the amount of data without altering the dependence structure across variable pairs. 
% However, we can still conclude that these results show that our method performs well for extremal clustering in higher dimensions, and motivates such a use. 
Nevertheless, the results demonstrate that the proposed method scales effectively to higher dimensions and performs well in multivariate settings. 

% \todo{Look into this, is this correct?? Shouldn't the clustering performance be much worse for 0.4 and 0.5?}
\begin{figure}[h]
    \centering
    % \includegraphics[width = 0.9\linewidth]{plots/sim_01c_js_sens_3_var_dqu_0.9.png}
    \includegraphics[width = 0.9\linewidth]{plots/sim_01d_js_sens_3_var_dqu_0.9mult_var_man.png}
    \caption{\emph{Evaluation of clustering performance for two to five variables and two equally sized clusters for simulations of 12 ``locations'', each with 1000 observations for each variable, from a mixture of Gaussian and t-copulas. The t-copula correlation parameters for each ``known'' cluster were set to $\rho_{t_1} = 0.4, and \rho_{t_2} = 0.5$. A grid search was performed over the values of the Gaussian correlation parameter, as represented on the x-axis. This grid search was repeated 500 times. The smoothed lines show the smoothed median of the Adjusted Rand Index for both clustering methods, with smoothing performing using a generalised additive model. Points show individual values of the ARI for a given simulation.}} 
   ~\label{fig:02b_5d}
\end{figure}

\subsubsection{Extension to 60 sites}~\label{subsubsec:sim_realistic}

% We also design a more realistic example to somewhat mimic the structure of the Irish dataset introduced in Section~\ref{sec:ire}.
% We also design a more realistic example to somewhat mimic the structure of the Irish dataset in our application in Section~\ref{sec:application}.
% To mimic the structure of the Irish dataset introduced in Section~\ref{sec:application}, we designed a more realistic simulation study. 
We designed another simulation study to mimic the structure of the Irish dataset introduced in Section~\ref{sec:application}. 
% We generated data from 60 locations, each with 1000 observations of two variables, using a mixture of Gaussian and t-copulas with Laplace margins.
Data were generated from $S = 60$ locations, each with $n = 1000$ observations of $J = 2$ variables, using a mixture of Gaussian and t-copulas with Laplace margins. 
% The 60 locations had three known clusters, with 10, 20, and 30 locations in each cluster, respectively.
The 60 locations were partitioned into three unequally sized clusters of 10, 20, and 30 sites, respectively. 
% A slight perturbation via a uniform sample between $-0.05$ and $0.05$ was added to the correlation parameters of the t-copula for each location, to make the clustering solution more challenging.
The $\rho_{t}^s$ values which make up the off-diagonal elements of $\Sigma_{t}^s$, the correlation matrix of the $t$-copula at site $s$ as described in Equation~\eqref{eq:mix_copula}, were perturbed for each location by a uniform random value between $-0.05$ and $0.05$, making the clustering task more challenging. 
% A constant $\rhou{Gauss}$ of 0.5 was used for all locations and simulations, to reduce the dimensionality of the grid search.
To reduce the dimensionality of the grid search, a constant $\rhou{Gauss} = 0.5$ was used for all locations and simulations. 
% Again, clustering was done on the CE model parameters using the $\JSGa{}$ divergence, and the ARI for a grid search over different $\rhou{t}$ values is shown in Figure~\ref{fig:03_realistic}.
% Clustering was performed on the CE model parameters using the $\JSGa{}$ divergence, and the ARI for a grid search over $\rhou{t}$ values is shown in Figure~\ref{fig:03_realistic}. 
The CE model was again fit at the 90th quantile, i.e., the 90th quantile of $Y_{i} \mid Y_{j} = y_j$, and clustering solutions were obtained using the expected divergence from Equation~\ref{eq:jsga_integral}, averaged across all models. 
The results of 500 repetitions of this simulation study are shown in Figure~\ref{fig:03_realistic}.
% The algorithm is again shown to perform well, particularly where the difference in the t-copula correlation parameters between the clusters is largest, as expected.
Similarly to the previous simulation studies, the method performs well, particularly when the differences in t-copula correlation parameters between clusters are larger. 
% The variability in ARI across simulations is also quite low, indicating that the clustering solution is robust.
Moreover, variability in ARI across simulations is low, indicating that the clustering solution is robust. 

% \todo{Have x-axis ticks match unique values in grid search}
\todo{Remove points}
\todo{Add rho t3 to top of plot as well!}
\begin{figure}[h]
    \centering
    % \includegraphics[width = 0.9\linewidth]{plots/sim_01d_js_sens_3_var_dqu_0.9.png}
    \includegraphics[width = 0.9\linewidth]{plots/01c2_js_sens_sim_60_locs_dqu_0.9_smooth_man_no_points.png}
    \caption{\emph{Evaluation of clustering performance for two variables and three clusters of 60 locations for simulations from a mixture of Gaussian and t-copulas.
    % 60 locations with 1000 observations for each variable were simulated, with 10, 20 and 30 belonging to each respective cluster. 
% A grid search was performed, with the x-axis and facet labels showing the t-copula correlation parameters for each of the three ``known'' clusters, for a Gaussian copula correlation of 0.5. 
    The x-axis and facet labels show the t-copula correlation parameters for each of the three ``known'' clusters, for a Gaussian copula correlation of 0.5. 
     % Perturbations were added to these parameters. 
     % This grid search was repeated 500 times. 
    The smoothed lines show the smoothed Adjusted Rand Index for both clustering methods, with smoothing performing using a generalised additive model.}}
     % Points show individual values of the ARI for a given simulation.}} 
 % The line shows the median of the Adjusted Rand Index for both clustering methods, with uncertainty coming from the 90\% credible interval. Points show individual values of the ARI for a given simulation.}} 
   ~\label{fig:03_realistic}
\end{figure}

\subsubsection{Parameter estimation pre- and post-clustering}~\label{subsubsec:boot}

% Describe bootstrapping
% Finally, we recall that an important reason for spatial clustering is to reduce the uncertainty in parameter estimates while minimising the introduction of bias by grouping similar locations together.
An important motivation for spatial clustering is to reduce the uncertainty in parameter estimates while minimising the introduction of bias by grouping similar locations. 
% To this end, we desired to ascertain whether the estimates of our dependence parameters were less uncertain after clustering.
To evaluate this, we assess whether estimates of the dependence parameters are less uncertain after clustering. 
% We can use the bootstrapping scheme described in \citet{Heffernan2004} to determine the uncertainty in our parameter estimates. \todo{Do I need to describe this scheme and how it preserves the dependence structure of the data?}
Uncertainty is quantified using the bootstrap procedure of \citet{Heffernan2004}, which preserves the dependence structure of the data. 

% % As we are estimating two parameters, $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$, for which we do not know the true values, it may be that although our uncertainty is diminished post-clustering, the estimates may still be biased, with the parameters unidentifiable.
% As we are estimating two parameters, $\alpha$ and $\beta$, for which we do not know the true values, it may be that although our uncertainty is diminished post-clustering, the estimates may still be biased, with the parameters unidentifiable.
As the true values of the CE model parameters $(\alpha(s), \beta(s))$ for a location $s$ are unknown, reduced variability in estimates post-clustering could still be accompanied by bias. 
% There are also many possible combinations of $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$ that could give the same conditional estimates, and so the estimates may be biased in this sengse as well.
% There are also many possible combinations of $\alpha$ and $\beta$ that could
% give the same conditional estimates, and so the estimates may be biased in
% this sense as well.
Indeed, many combinations of $\alpha(s)$ and $\beta(s)$ can yield similar conditional estimates, making these parameters only weakly identifiable. 
% To avoid this, we adopt the approach of \citet{Richards2021-qm} in calculating the conditional expectation of one variable given the other at the $98\%$ marginal quantile $u_i$ for the conditioning variable, given by
To address this, we follow the approach of \citet{Richards2021-qm} and instead calculate the conditional expectation of one variable given another at the 98\% marginal quantile $u_i$ of the conditioning variable,  
\begin{equation}~\label{eq:boot}
  % \mathbb{E}[Y_j \mid Y_i = u_i] = \hat{\alpha}_{j \mid i} u_i + u_i^{\hat{\beta}_{j \mid i}} \hat{\mu}_{j \mid i},
  \mathbb{E}[Y_j(s) \mid Y_i(s) = u_i] = \hat{\alpha}_{j \mid i}(s) u_i + u_i^{\hat{\beta}_{j \mid i}(s)} \hat{\mu}_{j \mid i}(s),
\end{equation}
% where ($\hat{\alpha}, \hat{\beta}, \hat{\mu}$) are the estimated CE model parameters.
% where $(\hat{\alpha}_{j \mid i}^{(\CE{})}, \hat{\beta}_{j \mid i}^{(\CE{})}, \hat{\mu}_{j \mid i}^{(\CE{})})$ are the estimated CE model parameters for the $j$-th variable given the conditioning variable $Y_i$ at the $98\%$ marginal quantile $u_i$.
% where $(\hat{\alpha}_{j \mid i}, \hat{\beta}_{j \mid i}, \hat{\mu}_{j \mid i})$ are the estimated CE model parameters for the $j$-th variable given the conditioning variable $Y_i$ at the $98\%$ marginal quantile $u_i$.
where $(\hat{\alpha}_{j \mid i}(s), \hat{\beta}_{j \mid i}(s), \hat{\mu}_{j \mid i}(s))$ are the estimated CE model parameters for the $j$th variable given $Y_i(s)$. 
% We can bootstrap this conditional expectation to evaluate the reduction in uncertainty post-clustering. \todo{Tense consistency}
We then apply bootstrapping to this conditional expectation to evaluate the reduction in uncertainty achieved through clustering. 

% Results
% We take an example for which we know the clustering algorithm performs well, namely the same simulation design as in Section~\ref{subsubsec:sim_competing_methods}, with $\rhou{Gauss} = 0.5$, and $\rhou{t} = 0.1$ and $0.9$ for the two clusters.
We illustrate the effect of clustering on parameter uncertainty using the same simulation design as in Section~\ref{subsubsec:sim_competing_methods}, with $\rhou{Gauss} = 0.5$ and $\rhou{t} = 0.1$ and $0.9$ for the two clusters. 
% We estimate the CE model parameters pre- and post-clustering, and generate 500 bootstrap samples for  the conditional expectation of Equation~\eqref{eq:boot}.
CE model parameters were estimated both pre- and post-clustering, and 500 bootstrap samples were generated for the conditional expectation in Equation~\eqref{eq:boot}. 
% The results are plotted in Figure \ref{fig:04_bootstrap}.
% We can see that the uncertainty in the bootstrapped estimates has markedly reduced post-clustering, as desired.
The results, shown in Figure~\ref{fig:04_bootstrap}, demonstrate a clear reduction in the variability of the bootstrapped estimates after clustering. 
% The conditional expectation is higher for the higher t-copula correlation cluster, as expected. \todo{Anything else need to be said here? Be more specific with results?}
In addition, the conditional expectation is consistently higher for the cluster with larger t-copula correlation, reflecting stronger extremal dependence. 
These findings confirm that clustering reduces parameter uncertainty while preserving meaningful differences between clusters. 

\begin{figure}[h]
    \centering
    \includegraphics[width = 0.9\linewidth]{plots/sim_01e_bootstrap_box_man.png}
    \caption{\emph{Boxplots of 500 bootstrapped conditional expectations of one variable given the other is at the $98\%$ marginal quantile $u$, pre- and post-clustering for a simulation of 12 ``locations'', each with 1000 observations for each variable, from a mixture of Normal and Gaussian copulas, with the Gaussian correlation set to 0.5. The t-copula correlation parameters for the two clusters were set to 0.1 and 0.9, as shown on the x-axis.}}
   ~\label{fig:04_bootstrap}
\end{figure}


\section{Application to Irish meteorological data}~\label{sec:application}

% \todo{Add image of distance matrices coloured by cluster}
% \todo{Add something on robustness to choice of DQU? Have plots for this!}

% Section Intro
% \todo{Where to put first plot? And which plot to have as first?}
We now apply our clustering method to a real-world dataset of meteorological observations from the Republic of Ireland. 
% In Section~\ref{subsec:app_data}, we describe the data we have collected, and in Section~\ref{subsec:app_explore} we perform some exploratory analysis of the data, using the $\chi$ statistic to assess the asymptotic dependence between precipitation and wind speed across the country. 
In Section~\ref{subsec:app_data}, we describe the dataset of precipitation and wind speed measurements collected across the country. 
Section~\ref{subsec:app_explore} presents exploratory analysis, using the $\chi$ statistic to assess the extremal dependence between the two variables. 
% In Sections \ref{subsec:app_ce} and \ref{subsec:app_clust}, we fit the CE model to the data, and apply our clustering method to the estimated parameters, respectively, to determine the spatial dependence structure of the data.
In Section~\ref{subsec:app_ce}, we fit the CE model to the data, and in Section~\ref{subsec:app_clust}, we apply our clustering approach to the estimated parameters to identify the spatial dependence structure. 

\subsection{Ireland meteorological data}~\label{subsec:app_data}

% % Image of locations and precipitation vs wind speed for two sites
% \todo{Move elevation legend onto plot}
% \todo{If keeping this plot, need to comment on heterogeneity motivating analysis!}
% \begin{figure}[h]
%     \centering
%     % \includegraphics[width = 0.9\linewidth]{plots/02_mot_ex_plot.png}
%     \includegraphics[width = 0.9\linewidth]{plots/02_mot_ex_plot_elev.png}
%     \caption{\emph{Left: locations of 59 Met Éireann sites with precipitation data available from 1990 to 2020, with elevation in metres above sea level binned into 100m intervals. 
%     Right: scatter plots of the weekly aggregated precipitation and wind speed maxima, with the 95\% quantile of wind speed and precipitation shown as dashed lines, for Cloone Lake, County Kerry (red), and Malahide Castle, County Dublin (blue), in the Winter months (October -- March), which are the two stations with the highest and lowest mean weekly precipitation, respectively.}}
%    ~\label{fig:02_locs}
% \end{figure}

\begin{figure}[h]
    \centering
    % \includegraphics[width = 0.9\linewidth]{plots/041_chi_plots_man.png}
    \includegraphics[width = 0.9\linewidth]{plots/043_chi_elevation_man_crop.png}
    % \caption{\emph{$\chi$ and $\bar{\chi} $ statistics for each of the 59 locations in our Irish meteorological dataset, estimated at the 95th quantile. The $\bar{chi}$ statistic is shown on the left, and $\chi$ on the right. For both maps, darker colours indicate stronger asymptotic dependence.}}
    \caption{\emph{Left: elevation profile of Ireland.%, with elevation in metres above sea level binned into 100m intervals with the highest bin for elevations above 500m.
    Right: $\chi$ statistics between precipitation and wind speed at all 59 locations, estimated at the 95th quantile. Darker colours and larger points indicate stronger asymptotic dependence.}}
   ~\label{fig:chi}
\end{figure}

% We consider the joint extremal behaviour of precipitation and wind speeds at 59 sites across the Republic of Ireland from 1990 to 2020, inclusive.
We analyse the joint extremal behaviour of precipitation and wind speed at 59 sites across the Republic of Ireland from 1990 to 2020. 
% The data we have performed our clustering on is precipitation data for the Republic of Ireland, collected from the Met Éireann website at \citet{metHistoricalData}, and wind speed data from the ERA5 Reanalysis dataset at \citet{Hersbach2020}, corresponding to the closest grid point to each location.
% Precipitation data were collected from the Met Éireann website (accessed 2024-09-01), and wind speed data from the gridded ERA5 Reanalysis dataset \citep{metHistoricalData, Hersbach2020}.
Precipitation data were obtained from the Met Éireann archive (accessed 2024-09-01), and wind speed data from the ERA5 Reanalysis dataset \citep{Hersbach2020, metHistoricalData}. 
% ERA5 provides averages over a gridded box of $0.25^{\circ} \times 0.25^{\circ}$, so each site will lie within a grid cell, to which the wind speed data corresponds.
ERA5 provides spatial averages on a $0.25^{\circ} \times 0.25^{\circ}$ grid, and each site was matched to the grid cell in which it lies. 
Both datasets are available at daily resolution. \todo{Check about ERA5, available at hourly resolution right?} \todo{Also elevation data comes from ERA5}
% These data span the dates 1990-01-01 to 2020-12-31, totalling 11,323 days of consecutive precipitation readings. 
The dataset spans 11,323 consecutive days of observations between 1990-01-01 and 2020-12-31. 
% , taking the closest grid cell corresponding to each precipitation measurement location.
% While the point estimates of the precipitation data can be considered more precise, it may also be susceptible to preferential sampling, with sites likely to be more densely found in areas with higher precipitation.
% Conversely, although reanalysis data is spatially complete, it only has a certain resolution (in this case, $0.25^{\circ} \times 0.25^{\circ}$).
% The data is averaged over each grid cell, and so often underestimates extreme wind speeds \citep{Gandoin2024}.


% Preprocessing
% Sites which did not have a complete set of data for these dates were removed, leaving us with 50 sites in total, located in 20 of the 26 counties of the Republic of Ireland. \todo{Change this, no longer the case!}
% Following the procedure of \citet{Vignotto2021}, we thinned the data, looking at weekly sums of daily precipitation and weekly averages of wind speed maxima, for the winter months (November to March) only.
% Preprocessing follows the procedure of \citet{Vignotto2021}, which thinned the data to weekly sums of daily precipitation and weekly averages of wind speed maxima, respectively, for the winter months (October to March) only.
% Preprocessing follows the procedure outlined in \citet{Vignotto2021}. 
Daily precipitation totals were aggregated into weekly sums, and wind speed maxima into weekly averages, for the winter months (October to March). 
% This weekly temporal scale was chosen because in the case of storms, precipitation and wind are believed to experience a slight lag \citep{Bengtsson2009}.
% This weekly temporal scale was chosen because in the case of storms, there is often a slight lag between extreme precipitation and wind speed events \citep{Bengtsson2009}.
% This thinning may also remove some temporal dependence in the data, which is not accounted for in our model.
This weekly temporal scale reflects the typical lag between extreme precipitation and wind speed during storm events \citep{Bengtsson2009}, and reduces short-term temporal dependence not accounted for in our model. 
% Weeks with no precipitation are also ignored, leaving us with 5,022 weeks of data for the 59 sites.
Weeks with no precipitation were excluded, resulting in 5,022 weekly observations across the 59 sites. 

% Summaries of dataset
% \todo{Why have this? To highlight extremes/spatial heterogeneity?}
% The maximum weekly precipitation sum observed was 198.6mm at Cloone Lake in county Kerry. 
% % The mean precipitation across all sites for each day was 5.96mm.
% For wind speed, the highest weekly maxima is 20.2 m/s, and was recorded closest to Eskeragh in county Mayo.
% Conversely, the minimum wind speed of 0.95 m/s was recorded at Glenvickee in Kerry. %, with the mean wind speed overall being 6.83 m/s.
% The difference between the mean and maximum values for each of these marginal extremes suggests that there will be an extreme right tail to the distribution of this data, a situation well suited to the use of EVT.
% These data will be used to showcase the marginal and dependence models we have chosen to use, and illustrate some of their shortcomings, and the need for extremal clustering to address these challenges.

% \subsection{Exploratory analysis}~\label{subsec:app_explore}

% Chi statistic
% \todo{Remove reference to chibar}
% chi + chi-bar plots as quick exploratory analysis
% As a brief exploration of the dependence profile in our data, we looked at the $\chi$ and $\bar{\chi}$ statistics introduced in Section~\ref{subsec:asym_dep}.
% As a brief exploration of the dependence profile of our data, we looked at the $\chi$ statistic introduced in Section~\ref{subsec:asym_dep}.
% We begin exploring the dependence structure of the data using the $\chi$ statistic introduced in Section~\ref{subsec:asym_dep}. 
We begin exploring the dependence structure of the data using the $\chi$ statistic introduced in Section~\ref{sec:intro}. 
% Specifically, we estimated $\chi$ at the 95th quantile for each of our 59 sites, to determine the extent of asymptotic dependence between our variables, and whether any spatial patterns in this dependence emerge. 
% Specifically, as shown in Figure \ref{fig:chi}, we estimated $\chi(0.95)$ at all 59 sites, to determine the extent of asymptotic dependence between precipitation and wind speed at each, and whether any discernible spatial patterns in this dependence emerge.
% These values are shown in Figure \ref{fig:chi}.
% These values, along with an elevation profile of Ireland, are shown in Figure \ref{fig:chi}.
An elevation profile of Ireland is also shown in Figure \ref{fig:chi}, which may help further explain some of this spatial structure.
Figure~\ref{fig:chi} shows estimates of $\chi(0.95)$ at all 59 sites.
% We can see that $\chi$ is positive for almost all sites, indicating positive asymptotic dependence of varying degree between precipitation and wind speed. 
% This means that when, for example, wind speed is high, precipitation is also likely to be high, and vice versa, to a greater or lesser extent depending on the site.
Positive values are observed at almost all sites, indicating varying degrees of positive asymptotic dependence between precipitation and wind speed. 
In practical terms, this suggests that extreme wind speeds tend to be accompanied by high precipitation, and vice versa. \todo{Often too strong?}

% A clear spatial pattern emerges, whereby asymptotic dependence gets progressively stronger for locations further south and west, being highest along the west coast of Ireland, in counties such as Kerry, Galway, Mayo and Donegal. 
% Conversely, sites in Dublin, Wicklow and Wexford have lower values of $\chi$ and $\bar{\chi}$, indicating weaker extremal dependence along the east coast. \todo{And Louth?}
% Conversely, in counties such as Dublin, Wexford and Louth, lower values of $\chi$ indicate weaker extremal dependence along the east coast.
A clear spatial pattern emerges: dependence is strongest in the south and west, with the highest values along the Atlantic coast, notably in counties Kerry, Galway, Mayo, and Donegal, and weaker along the east coast, in Dublin, Wexford, and Louth. 
Specifically, the highest $\chi$ value is observed at Derryhillagh in Mayo, at 0.414, while the lowest is at Malahide Castle in Dublin, at 0.0005.
% This indicates that there are distinct spatial patterns of extremal dependence in the data, motivating an analysis using the CE model and possibly clustering to perform inference on and better understand this dependence structure.
These results indicate distinct regional patterns of extremal dependence, as well as notable outliers, motivating the use of the CE model and clustering to characterise and interpret this spatial structure more formally. 

Some spatial outliers are also apparent. 
Kilcar in Donegal has a relatively low $\chi$ value despite its west-coast location, while certain Wicklow sites show unusually high values for the east. 
The latter may be related to the influence of the Wicklow Mountains, as reflected in the elevation profile. 
It may be of interest to investigate if these outliers in $\chi$ are reflected in the CE model parameters, and whether clustering can help identify them.

\subsection{Conditional extremes model}~\label{subsec:app_ce}

% Procedure and results pre-clustering (choice of quantile)
% In our analysis, we first transformed the data to Laplace margins, using the empirical cumulative distribution function (ECDF) to approximate density functions for precipitation and wind speed at each location. \todo{reference specific equation in methods}
We transform precipitation and wind speed data at each location to Laplace margins as described in Equation~\eqref{eq:laplace_transform}, using the empirical cumulative distribution function (ECDF) to approximate marginal distributions.  
% We then fit the CE model to this transformed data.
% % Using the bootstrapping scheme of \citet{Heffernan2004}, we estimated the CE model parameters $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$ at various quantiles. \todo{Sufficient reference to bootstrapping scheme?}
% Using the bootstrapping scheme of \citet{Heffernan2004}, we estimated the CE model parameters $\alpha$ and $\beta$ at various quantiles. \todo{Sufficient reference to bootstrapping scheme?}
% The CE model was then fitted to the transformed data, and parameter estimates $(\alpha, \beta)$ were obtained at various quantiles using the bootstrap procedure of \citet{Heffernan2004}.  
\todo{Christian: Check that brief description of bootstrapping for choosing quantile is sufficient}
In order to select an appropriate quantile for fitting the CE model, we estimated the parameters $(\alpha, \beta)$ at various quantile levels using the bootstrap procedure of \citet{Heffernan2004}. 
The idea was to identify the highest quantile at which parameter estimates remained stable, balancing extremity and stability.
% This revealed that estimates in general were quite stable up to the 88th quantile.  \todo{Need to expand with more details, could include plots in Appendix?}
Estimates were generally stable for both models, i.e.\ precipitation conditional on wind speed and vice versa, up to the 85th quantile, with similar maximum likelihood estimates across bootstrap samples for $(\alpha, \beta)$. % and clustering solutions obtained when using the 85th quantile were quite similar to higher quantiles.
Therefore, the 85th quantile was used for subsequent analysis for both models.
In Section~\ref{app_clust} and Figure~\ref{fig:clust_sens}, we show that clustering solutions are also quite robust to the choice of quantile between the 85th, 88th and 90th quantiles, and so this choice does not have a major impact on results.
% Clustering sensitivity to the choice of quantile is discussed further in Section~\ref{subsec:app_clust}, surrounding Figure~\ref{fig:clust_sens}.

% Subsequent analysis was also performed at the 85th, 88th and 90th quantiles, and shown to provide similar MLEs for our CE parameters, and clustering solutions. \todo{Include in Appendix also?}

% The results of this CE analysis are shown in Figure \ref{fig:a_b_vals}.
% The estimates of the CE model parameters at the 85th quantile are shown in Figure~\ref{fig:a_b_vals}.
The estimated CE parameters at the 85th quantile for both variables are displayed in Figure~\ref{fig:a_b_vals}.  
% Interpretation of these plots is difficult, but we can see some, albeit uneven, increases in the $\alpha^{(\CE{})}$ values for both variables as we move further West and particularly South. \todo{Should I estimate/show uncertainty in these estimates somehow? these estimates have been bootstrapped!}
% Interpretation of these plots is difficult, but we can see some, albeit uneven, increases in the $\alpha$ values for both variables as we move further West and particularly South. \todo{Should I estimate/show uncertainty in these estimates somehow? these estimates have been bootstrapped!}
Although interpretation is challenging due to variability in the estimates, we observe some evidence of increasing $\alpha$ values for both variables towards the West and particularly in the South.  
% Several $\beta^{(\CE{})}$ values are negative, which is unexpected, and perhaps suggests non-identifiability in the CE regression model. 
% Several $\beta$ values are negative, which is unexpected, and perhaps suggests non-identifiability in the CE regression model. 
Several $\beta$ estimates are negative, which is unexpected and may reflect non-identifiability in the CE regression model, or problems with data. 
% \item It appears that high uncertainty is making interpretation difficult, and so we will cluster the locations based on their CE parameters, to aid in interpretation.
% We will cluster the locations based on their CE parameters, to help improve the interpretation of CE results.
To mitigate this uncertainty and aid interpretation, we next cluster the locations based on their estimated CE parameters.  

\begin{figure}[h]
    \centering
    \includegraphics[width = 0.9\linewidth]{plots/ire_ce_new.png}
    % \caption{\emph{Map of conditional extremes model parameters $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$ for precipitation conditional on wind speed (left) and wind speed conditional on precipitation (right), estimated at the 88th quantile. The $\alpha^{(\CE{})}$ parameter is shown in the top row, and $\beta^{(\CE{})}$ in the bottom row. The colour scale is the same for both variables, with darker red indicating larger positive values of $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$, and darker blue indicating larger negative values.}}
    \caption{\emph{Map of conditional extremes model parameters $\alpha$ and $\beta$ for precipitation conditional on wind speed (left) and wind speed conditional on precipitation (right), estimated at the 85th quantile. The $\alpha$ parameter is shown in the top row, and $\beta$ in the bottom row. The colour scale is the same for both variables, with darker red indicating larger positive values of $\alpha$ and $\beta$, and darker blue indicating larger negative values.}}
   ~\label{fig:a_b_vals}
\end{figure}

\subsection{Clustering}~\label{subsec:app_clust}

% Clustering (including choice of k for each measure)
% We clustered the locations using the $\JSGa{}$ dissimilarity matrix for their CE parameters, as described in Section~\ref{subsec:div}, for the CE models fit to precipitation conditional on extreme wind speed and vice versa, separately. 
% We clustered the locations using the $\JSGa{}$ dissimilarity matrix of their CE parameter estimates, as described in Section~\ref{subsec:div}, for the CE models fitted to precipitation conditional on extreme wind speed and vice versa, separately.
% We clustered the locations using the integral form of the $\JSGa{}$ dissimilarity matrix, as described in Equation~\ref{eq:jsga_integral} in Section~\ref{subsec:div}, for the CE models fitted to precipitation conditional on extreme wind speed and vice versa, separately.
\todo{Could be worded better}
We clustered the locations using the $\eJSGa{}$ dissimilarity matrix, as defined in Equation~\ref{eq:jsga_integral} and Equation~\ref{eq:distance_matrix} for the CE models fitted to precipitation conditional on extreme wind speed and vice versa, separately.
% We also combine information from both models to cluster by summing their dissimilarity matrices.
We also considered a combined solution by averaging over the two dissimilarity matrices.
The number of clusters $k$ was chosen via the method described in Section~\ref{subsubsec:choose_k}, using the elbow plot of the TWGSS. 
For all three clustering solutions, clear elbows were observed at $k = 3$, so this was the number of clusters chosen \todo{Include plots in Appendix?}.

% Our clustering solutions for each of the three dissimilarity matrices at the 85th quantile are shown in Figure \ref{fig:clust_sol}.
The resulting clustering solutions for the 85th quantile are displayed in Figure~\ref{fig:clust_sol}.  
% Clustering appears to be quite similar across dissimilarity matrices, and the three clusters appear to be quite spatially distinct. 
Across all three dissimilarity matrices, the clusterings are broadly consistent, with three spatially distinct groups emerging.  
% This is interesting given no spatial information was used in the clustering, and suggests that the CE model parameters are indeed capturing some spatial structure in the data.
This spatial coherence is notable given that no explicit spatial information was used in model fitting or in the clustering, suggesting that the CE model parameters capture meaningful spatial structure in the extremal dependence.  

% Interpreting the clusters, there appears to be a clear difference in between the east and west coasts, bordering the Atlantic Ocean and Irish Sea respectively, which are meaningfully separated by a central cluster. \todo{Could be written a lot better!}
Interpreting the clusters, a clear contrast emerges between the east and west coasts, separated by a central cluster.  
% The clustering for wind speed conditional on precipitation is somewhat less distinct and stable, with the central and eastern clusters having more members in Mayo and Donegal along the west coast.
\todo{Is this still true?}
For precipitation conditional on wind speed, the clustering is somewhat less distinct, with the central cluster extending more extensively into parts of Galway, Mayo and Donegal on the west coast.  
% The behaviour of wind speed extremes may be more complex than that of rainfall extremes. \todo{Can definitely do a better job here! See Vignotto for a better write up!}
This may indicate greater complexity in the behaviour of wind speed extremes compared to rainfall extremes.  
% This may also be due to our wind speeds coming from the ERA5 reanalysis dataset, which performs smoothing over a grid and so may underestimate wind speed extremes, and fail to capture more granular local variability.
It could also reflect the use of ERA5 reanalysis data for wind speed, which is known to smooth across grid cells, potentially underestimating local extremes and failing to capture fine-scale variability.  

\todo{Combine with post-clustering parameter estimates (legend)}
\todo{Highlight Malahide Castle as outlier? Also Kilcar?}
\begin{figure}[h]
    \centering
    \includegraphics[width = 0.9\linewidth]{plots/cluster_dqu_sep_new_crop.png}
    \caption{\emph{Clustering solutions for the conditional extremes model parameters estimated at the 85th quantile across all 59 sites, using the $\JSGa{}$ dissimilarity matrix for wind speed conditional on precipitation (centre), precipitation conditional on wind speed (right), and the combined dissimilarity matrix (left). The clusters are shown in different colours.}}
   ~\label{fig:clust_sol}
\end{figure}

\todo{Add back in site names to x-axis}
\begin{figure}[h]
    \centering
    % \includegraphics[width = 0.9\linewidth]{plots/ire_dist_heatmap.png}
    \includegraphics[width = 0.9\linewidth]{plots/ire_dist_heatmap_man.png}
    \caption{\emph{Heatmap of the $\JSGa{}$ dissimilarity matrix for the conditional extremes model parameters estimated at the 85th quantile across all 59 sites, using the combined dissimilarity matrix. The names of each site are coloured and ordered according to the clustering solution, matching that of Figure \ref{fig:clust_sol}. The dissimilarity colour scale is shown on the right, with darker colours indicating larger values.}}
    ~\label{fig:dist_heatmap}
\end{figure}

% Analysis of dissimilarity matrix
We can also visualise the dissimilarity matrix for the combined clustering solution, as shown in Figure \ref{fig:dist_heatmap}.
This shows that the clusters are quite distinct, with the dissimilarity between clusters being much larger than within-cluster dissimilarity, which rarely exceeds 0.01.
In particular, the difference between the eastern and western clusters is quite pronounced, with the central cluster being somewhat intermediate, acting as somewhat of a `buffer' between the two.
% The western and central clusters exhibit many sites with small pairwise dissimilarities, indicating that their allocations are not always clear-cut. 
% In particular, several central sites are nearly interchangeable with those in the western cluster, a pattern that is also reflected in the variable-specific clustering solutions in Figure~\ref{fig:clust_sol} and in the quantile sensitivity analysis in Figure~\ref{fig:clust_sens}. 
In constrast, the western and central clusters seem to be quite close, in that we observe many small inter-cluster pairwise dissimilarities between them. 
This indicates that allocation between the two is not always clear-cut and for several sites is nearly interchangeable, as observed in  the variable-specific clustering solutions in Figure~\ref{fig:clust_sol} and in the quantile sensitivity analysis in Figure~\ref{fig:clust_sens}, where membership between the two often switches. \todo{Mention specific sites}
Also, if we only fit two clusters, we find that most of the central cluster members are assigned to the ``western'' cluster, reflecting this less obvious separation between the two compared to the more markedly different eastern cluster. 
A noticeable outlier in the dissimilarity matrix is the site at Malahide Castle, which is located in Dublin, which even amongst its own cluster has a relatively high dissimilarity to the other sites.
% Furthermore, we go to having four clusters, Malahide Castle becomes the only member of the new cluster, and so it is quite distinct from the other sites.
Furthermore, if we set $k=4$, Malahide Castle becomes its own cluster, and so it is quite distinct from the other sites.
% This site also has the distinction of having the lowest mean weekly precipitation, and the second lowest maximum wind speed, of all sites in our dataset, which may explain its outlying behaviour, even amongst the eastern cluster, which is generally characterised by lower $\alpha^{(\CE{})}$ estimates and $\chi$ values. 
\todo{Mention Ringsend is next driest area }
% Malahide Castle has the lowest mean weekly precipitation of any site, with the second lowest being in Ringsend, which is closest in terms of $\JSGa{}$ to Malahide, which may explain its outlying behaviour, even amongst the eastern cluster, in that rainfall extremes and consequently concomitant extremes are less likely to occur, as shown by its low $\chi$ and $\alpha^{(\CE{})}$ estimates.
Malahide Castle has the lowest mean weekly precipitation of any site, with the second lowest being in Ringsend, which is closest in terms of $\JSGa{}$ to Malahide, which may explain its outlying behaviour, even amongst the eastern cluster, in that rainfall extremes and consequently concomitant extremes are less likely to occur, as shown by its low $\chi$ and $\alpha$ estimates.
We also see that Kilcar in county Donegal, which had a low $\chi$ value relative to other sites on the west coast as shown in Figure \ref{fig:chi}, is the member of the eastern cluster with the lowest dissimilarity to the members of the western cluster, reflecting its geographical location in the northwest of Ireland, despite its low $\chi$ value.

\begin{figure}[h]
    \centering
    \includegraphics[width = 0.9\linewidth]{plots/cluster_dqu_crop.png}
    % \caption{\emph{ Clustering solutions for the conditional extremes model parameters estimated at the 88th quantile across all 59 sites, using the $\JSGa{}$ dissimilarity matrix for precipitation conditional on wind speed (centre), wind speed conditional on precipitation (right), and the combined dissimilarity matrix (left). The clusters are shown in different colours.}}
    \caption{\emph{Clustering solutions for the conditional extremes model parameters estimated at the 85th, 88th and 90th quantiles across all 59 sites, using the combined $\JSGa{}$ dissimilarity matrix for precipitation conditional on wind speed and wind speed conditional on precipitation. The clusters are shown in different colours.}}
    ~\label{fig:clust_sens}
\end{figure}

% Sensitivity Analysis (to choice of DQU)
Another interesting aspect of our clustering method is its robustness to the choice of quantile.
We performed a sensitivity analysis of the clustering solution to the choice of quantile, using the 85th, 88th and 90th quantiles, the results of which are shown in Figure~\ref{fig:clust_sens}.
The clustering solutions are quite similar across the three quantiles.
There are some small differences, however. 
Noticeably, Kilcar in county Donegal is assigned to the eastern cluster for the 85th and 90th quantile, but to the central cluster for the 88th quantile.
% Also, Costelloe Fishery in county Galway is assigned to the wester cluster for the 85th quantile, but to the central cluster for the 88th and 90th quantiles.
\todo{Name some of these sites?}
Also, there are more sites in the western cluster for the 85th quantile, with some sites moving to the central cluster for the 88th quantile. 
Interestingly, some of these sites are then moved back to the western cluster for the 90th quantile.
Apart from these observations, the clustering solutions are generally quite similar across the three quantiles, and so we can conclude that our clustering method is relatively robust to the choice of quantile, as desired. 

% Results and interpretation post-clustering
% \todo{Write again to make clearer/tighter}
% \todo{Use hat(alpha) rather than alpha (and beta)?}
% Taking the clustering results for the combined dissimilarity matrices, we can combine the data from the three clusters to estimate the CE model parameters for each cluster.
% Specifically, across each cluster, we take the weekly sum of precipitation and weekly maxima of wind speeds across all locations, and assign these to the cluster centroids/medoids.
% We then refit the CE model to these cluster centroids, again at the 88th quantile, although results for other quantile levels are similar. 
% Goodness-of-fit plots, exploring the assumption of independence between the residuals and variable conditioned on, were much improved post-clustering. \todo{Include? Appendix?}
% The estimated CE parameters for each cluster are shown in Figure \ref{fig:ce_post_clust}.
% % For precipitation conditional on wind speed, $\alpha^{(\CE{})}$ gets progressively larger as we move from the eastern to western clusters, indicating that the dependence structure is stronger in the west, as expected.
% % For precipitation conditional on wind speed, $\alpha^{(\CE{})}$ gets progressively larger as we move from the eastern to western clusters, indicating that the dependence structure is stronger in the west, as expected.
% For precipitation conditional on wind speed, $\alpha$ gets progressively larger as we move from the eastern to western clusters, indicating that the dependence structure is stronger in the west, as expected.
% % Interestingly, the opposite is true for wind speed conditional on precipitation, whereby $\alpha^{(\CE{})}$ gets larger moving from west to east. \todo{Insert a good interpretation here!}
% Interestingly, the opposite is true for wind speed conditional on precipitation, whereby $\alpha$ gets larger moving from west to east. \todo{Insert a good interpretation here!}
% % For both models, $\beta^{(\CE{})}$ is much larger for the coastal clusters than the central cluster.
% For both models, $\beta$ is much larger for the coastal clusters than the central cluster.
% This suggests that the ``spread'' of the conditional distribution is larger for the coastal clusters, and greater homoskedasticity is present in the central cluster.
% This is likely because weather patterns are more consistent inland than along the coasts, where the weather is more variable.
% Interpreting these results is much easier post-clustering. \todo{More detail here!}
% 
% \todo{Switch to using hat for estimates?}
% \begin{figure}[h]
%     \centering
%     % \includegraphics[width = 0.9\linewidth]{plots/ab_map_post_clust.png}
%     \includegraphics[width = 0.9\linewidth]{plots/ab_map_post_clust_new.png}
%     % \caption{\emph{Estimated conditional extremes model parameters $\alpha^{(\CE{})}$ and $\beta{(\CE{})}$ for precipitation conditional on wind speed (left) and wind speed conditional on precipitation (right), estimated at the 88th quantile, post-clustering. The $\alpha^{(\CE{})}$ parameter is shown in the top row, and $\beta^{(\CE{})}$ in the bottom row. The colour scale is the same for both variables, with darker red indicating larger positive values of $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$, and darker blue indicating larger negative values.}}
%     \caption{\emph{Estimated conditional extremes model parameters $\alpha$ and $\beta{(\CE{})}$ for precipitation conditional on wind speed (left) and wind speed conditional on precipitation (right), estimated at the 88th quantile, post-clustering. The $\alpha$ parameter is shown in the top row, and $\beta$ in the bottom row. The colour scale is the same for both variables, with darker red indicating larger positive values of $\alpha$ and $\beta$, and darker blue indicating larger negative values.}}
%    ~\label{fig:ce_post_clust}
% \end{figure}

% \subsection{US urban air pollution data}~\label{subsec:app_us}
% 
% \begin{itemize}
%     \item 
% \end{itemize}

\section{Discussion}~\label{sec:discuss}

% Points to consider
\todo{Talk about how subsequent analyses should constrain parameters to be non-negative?}
\todo{Say something about $\alpha$!}
\todo{Laplace truncation: how truncation point may lead to errors in severly unbalanced data (empirical quantile > sitewise maxima)}
\todo{Mention mim(maxima) approach as alternative if extrapolation occurs at any site}
\todo{But min(maxima) approach for unbalanced data may also be lower than sitewise thresholds}
\todo{Could also say Laplace truncation point could be tuned further to improve clustering performance?}
\todo{Also, if empirical quantile exceeds any sitewise maxima, will perform extrapolation; if a problem use `minima of maxima' approach}
\todo{JSGa choice: why not use standard JS, Jeffreys or other measure (2-Wasserstein, one beginning with H?)?}
\todo{In multidimensional case, currently restricted to using same DQU for e.g. X2 | X1 and X3 | X1, could extend}

% Rewrite Intro: Methods
In this paper, we introduced a novel method for clustering spatial locations based on their multivariate extremal dependence structure, as estimated by the conditional extremes model.
% The $\JSGa{}$ divergence measure of \citet{Nielsen2019} is proposed as a dissimilarity measure to compare locations based on their estimated CE parameters, $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$, which together describe the strength and ``stochasticity'' of each set of bivariate extremal relationships. 
The $\JSGa{}$ divergence measure of \citet{Nielsen2019} is proposed as a dissimilarity measure to compare locations based on their estimated CE parameters, $\alpha$ and $\beta$, which together describe the strength and ``stochasticity'' of each set of bivariate extremal relationships. 
This method exploits the fact that inference based on the CE model follows a multivariate Gaussian distribution, following from the Gaussian assumption for the residual process.
This allows us to derive a closed-form for the $\JSGa{}$ divergence, making it computationally efficient to use in clustering.
K-medoids clustering is performed on the dissimilarity matrix derived from this measure.
This preserves the spatial nature of the data and ensures that actual spatial locations with fitted CE models, as opposed to some aggregation, serve as cluster centroids. 
We can also choose to cluster on some or all of the bivariate extremal relationships in our data, and can aggregate information from multiple CE models to improve clustering performance.

% Simulation study
A simulation study was performed to evaluate the effectiveness of our method.
First, a bivariate Gaussian copula simulation was used to ensure that our clustering method works as desired, since the asymptotic values of the CE parameters are known for this copula.
Clustering solutions were perfectly identified, and parameter estimates showed both reduced uncertainty and bias when the CE model was re-fit post-clustering. 
We then extended this study to a more complex simulation design using a mixture of Gaussian and t-copulas, for a variety of simple and more complex dependence structures and scenarios.
We compared our method to the leading competitor in the bivariate case, \citet{Vignotto2021}, and showed that our method outperforms theirs across a range of possible bivariate dependence scenarios. 
Our method is also shown to be uniquely extendable to three dimensions and beyond, and providing more information to the clustering solution in the absence of additional noise is shown to improve clustering performance, as expected.

% Application
We applied our method to a dataset of Irish meteorological data, consisting of 59 locations with weekly rainfall and wind speed observations.
We estimated the CE model parameters for each location, and clustered the locations based on these parameters using our $\JSGa{}$ divergence-based method. 
Three clusters were identified, with the clustering solution showing distinct spatial patterns.
Namely, fairly well separated clusters representing the east and west coasts and the centre of Ireland emerged.
This was pleasing as no spatial information was used in fitting the CE model or clustering, and suggests that the CE model parameters are capturing some of the spatial heterogeneity inherent to the extremal dependence in our data. 

% Limitations: Gaussian assumption
There are several limitations to our method as currently implemented.
Our method relies heavily on the Gaussian assumption for residual process of CE model to provide a closed-form for the $\JSGa{}$ divergence.
This may not hold in all cases, particularly for more complex dependence structures, and so the method may not be appropriate for all datasets.
For example, in \citet{Farrell2025}, the authors use the generalised and asymmetric generalised Gaussian distributions to model their residuals. 
While our closed-form would not hold in this case, the $\JSGa{}$ divergence could still be used, but would require computationally expensive Monte Carlo sampling to estimate the dissimilarity matrix.
Indeed, it may be preferable to use the standard $\JS{}$ divergence if choosing to forgo this Gaussian assumption, since it is bounded between 0 and $\log{(2)}$, while the $\JSGa{}$ is unbounded, as described in \citet{Thiagarajan2025}.

\todo{Address use of empirical residuals? How would that affect the method?}

% Limitations: Uncertainty
The other important limitation of our method is in its treatment of uncertainty. 
% We use the MLE estimates of $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$ to compute our dissimilarity matrix, and so the uncertainty in these estimates is not accounted for in the clustering solution.
We use the MLE estimates of $\alpha$ and $\beta$ to compute our dissimilarity matrix, and so the uncertainty in these estimates is not accounted for in the clustering solution.
% This is less problematic in our simulation study where we often know the true values of $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$ and can perform extensive grid searches to ensure that the clustering solution is robust.
This is less problematic in our simulation study where we often know the true values of $\alpha$ and $\beta$ and can perform extensive grid searches to ensure that the clustering solution is robust.
% However, in real applications, the uncertainty in these estimates can be quite large, and so vastly different clustering solutions may be obtained depending on the estimated values of $\alpha^{(\CE{})}$ and $\beta^{(\CE{})}$.
However, in real applications, the uncertainty in these estimates can be quite large, and so vastly different clustering solutions may be obtained depending on the estimated values of $\alpha$ and $\beta$.
To account for this uncertainty, one possible approach would be to use the bootstrapping scheme described in \citet{Heffernan2004} to generate uncertainty in the CE model parameter estimates, and then use these bootstrapped estimates to compute the dissimilarity matrix.
We could then produce a distribution of dissimilarity matrices, and use these to compute a distribution of clustering solutions.
This would allow us to account for the uncertainty in the CE model parameter estimates, and provide a more robust clustering solution.
However, this would be quite computationally expensive, and so would hurt one of the main advantages of our method, namely its computational efficiency, and may not be feasible for larger datasets.

\todo{Could have looked at Anderson-Darling, as used in Carreau2017, rather than KL?}

\todo{Add limitations for simulation study?}
% \item For simulations, could have looked at LRI as well as ARI.
% \item Could have looked at spatially varying clustering parameters, to more closely mimic spatial applications. 

% Limitations: Application
In this work, we focused on the methodological development and novelties of our method, and so we did not explore the application of our method to the Irish meteorological dataset in great detail, and so there are some limitations in this application.
  % \item Two important aspects of extremal modelling which we did not explore in detail were the choices of marginal model and dependence threshold. 
Starting with the marginal transformations, we used the ECDF to transform the data at each site to Laplace margins.
In the original \citet{Heffernan2004} model, the Generalised Pareto distribution (GPD) is used to model exceedances above an extremal threshold, combined with the ECDF for the ``bulk'' of data. 
This GPD marginal model can be extended to include covariates such as spatial location to inform the marginal distribution.
We wanted to show that our clustering could produce sensible results using just the ECDF in the early exploratory stages of modelling, where a sensible GPD or other such marginal model may not be immediately obvious.
We believe we were successful in this, showing that with no spatial information our clustering solution did indeed indicate some spatial structure in the extremal dependence in our data.
As future work, a more sophisticated marginal model could be considered, using the information we have gained from our exploratory clustering. 
\todo{Also talk about not exploring other quantiles to ascert robustness!}
\todo{Could also mention that we could weight results more towards model cond. on precipitation since wind speed data is weird!}

% Future Work: (Application, potential other uses)
There is scope for further work in this area, particularly in the application of our method to other datasets.
While we advertise our method as being uniquely applicable to any multivariate extremal dependence setting, our application is currently limited to bivariate extremal dependence.
Therefore, an obvious line of future work would be to extend this application to look at additional variables. 
The ERA5 reanalysis dataset, for example, where we get our wind speed data contains many variables that could be used in conjunction with our method, such as temperature, humidity, and pressure, all of which may have interesting extremal dependence relationships with rainfall and wind speed which could be explored.
We could also look at other datasets where multivariate extremal dependence is of interest. 
  % \item \citet{Vettori2020-fw} derived a method for extremal clustering based on max-stable processes, a competing model to CE, paired with a Reversible Jump MCMC scheme for Bayesian clustering, and applied it to California air pollution data. 
  % \item It may be interesting to compare the performance of our method versus a fully parametric approach such as this, as opposed to the semi-parametric approach of \citet{Vignotto2021} which drew comparisons to in our simulation study.
For example, \citet{Vettori2020} modelled the extremal dependence structure of California state air pollution and environmental data across 21 sites. 
It would be interesting to see if we could build on such work via exploratory spatial clustering using our method, and whether the CE model would provide greater flexibility than their max-stable process approach, which can only model asymptotic dependence. 

\section{Disclosure statement}\label{disclosure-statement}

The authors have the following conflicts of interest to declare (or
replace with a statement that no conflicts of interest exist).

\section{Data Availability Statement}\label{data-availability-statement}

Deidentified data have been made available at the following URL: XX.

\phantomsection\label{supplementary-material}
\bigskip

\begin{center}

{\large\bf SUPPLEMENTARY MATERIAL}

\end{center}

\begin{description}
\item[Title:]
Brief description. (file type)
\item[R-package for MYNEW routine:]
R-package MYNEW containing code to perform the diagnostic methods
described in the article. The package also contains all datasets used as
examples in the article. (GNU zipped tar file)
\item[HIV data set:]
Data set used in the illustration of MYNEW method in
Section~\ref{sec-verify} (.txt file).
\end{description}

\section{BibTeX}\label{bibtex}

We encourage you to use BibTeX. If you have, please feel free to use the
package natbib with any bibliography style you're comfortable with. The
.bst file agsm has been included here for your convenience. 

\bibliography{library.bib}

\end{document}
